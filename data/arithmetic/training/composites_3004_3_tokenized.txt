\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} The number <nat:40> is the multiplication of natural numbers $ <var:q> $ , $ <var:D> $ and $ <var:C> $ so that $ <var:D> $ , $ <var:q> $ , $ <var:C> $ greater than or equivalent to <nat:2> . \end{theorem} \begin{proof} Note that $ <var:D> \times <var:C> \times <var:q> = <nat:40> $ yields $ <var:q> = <nat:4> $ , $ <var:D> = <nat:5> $ , $ <var:C> = <nat:2> $ . Similarly notice $ <nat:5> $ , $ <nat:2> $ and $ <nat:4> $ are greater than or equal to <nat:2> . As a consequence , $ <var:q> \times <var:C> \times <var:D> = <nat:40> $ is verified . \end{proof} \end{document} 
~
Require Import Lia . Theorem <genP:1> : exists <var:q> <var:D> <var:C> : nat , ( <nat:40> = <var:q> * <var:D> * <var:C> ) /\ ( <var:D> >= <nat:2> ) /\ ( <var:q> >= <nat:2> ) /\ ( <var:C> >= <nat:2> ) . Proof . exists <nat:4> . exists <nat:5> . exists <nat:2> . assert ( <genH> : <nat:5> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:4> >= <nat:2> ) . { lia . } repeat split . all : try apply <genH> . all : try apply <genH> . all : try apply <genH> . Qed . 