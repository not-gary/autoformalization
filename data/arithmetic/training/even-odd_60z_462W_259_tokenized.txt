\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:z> \times <nat:60> + <var:W> \times <nat:462> + <nat:259> $ must be odd . \end{theorem} \begin{proof} We know that the totaling between even numbers with an odd element of $ \mathbb{N} $ will be by definition an odd whole number . We use the assumption the fact $ <nat:60> <var:z> + <var:W> <nat:462> $ is clearly even , since the totaling between an even natural number and an even element of $ \mathbb{Z}^+ $ is an even number in $ \mathbb{Z}^+ $ . Remember that $ <var:W> * <nat:462> $ is trivially an even natural . This fact holds because the product of an even integer with some number will be even . The product $ <nat:60> * <var:z> $ is guaranteed to be even . It can be shown that the lemma holds . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:z> <var:W> : nat , Nat . odd ( <nat:60> * <var:z> + <nat:462> * <var:W> + <nat:259> ) = true . Proof . intros . rewrite Nat . odd_add . assert ( <genH> : Nat . even ( <nat:462> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:60> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:60> * <var:z> + <nat:462> * <var:W> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 