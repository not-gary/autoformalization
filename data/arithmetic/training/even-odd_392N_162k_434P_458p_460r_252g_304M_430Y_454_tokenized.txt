\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:454> + <nat:252> \times <var:g> + <var:r> \times <nat:460> + <nat:458> \times <var:p> + <var:M> \times <nat:304> + <var:k> \times <nat:162> + <nat:434> \times <var:P> + <nat:430> \times <var:Y> + <var:N> \times <nat:392> $ is even . \end{theorem} \begin{proof} It can be justified that this result must be true . See that $ <var:N> \cdot <nat:392> + <var:k> \cdot <nat:162> + <var:P> \cdot <nat:434> + <nat:458> \cdot <var:p> + <nat:460> \cdot <var:r> + <var:g> \cdot <nat:252> + <nat:304> \cdot <var:M> + <var:Y> \cdot <nat:430> $ is guaranteed to be even . The pairs $ <var:p> \times <nat:458> $ in addition to $ <var:Y> \times <nat:430> $ are guaranteed to be even numbers in $ \mathbb{Z}^+ $ . Additionally , recall that the terms $ <var:k> \times <nat:162> $ , $ <var:M> \times <nat:304> $ , $ <nat:434> \times <var:P> $ , and likewise $ <nat:460> \times <var:r> $ are obviously even integers since the multiplication between an even integer with a natural is guaranteed to be even . $ <var:N> \times <nat:392> $ is trivially an even number in $ \mathbb{N} $ . This claim holds since taking the product of an even number in $ \mathbb{Z}^+ $ and some element of $ \mathbb{N} $ must be even . In addition , take note the pair $ <var:g> \times <nat:252> $ is obviously an even element of $ \mathbb{Z}^+ $ since the product of an even integer with a positive integer is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:g> <var:r> <var:p> <var:M> <var:k> <var:P> <var:Y> <var:N> : nat , Nat . even ( <nat:252> * <var:g> + <nat:460> * <var:r> + <nat:458> * <var:p> + <nat:304> * <var:M> + <nat:162> * <var:k> + <nat:434> * <var:P> + <nat:430> * <var:Y> + <nat:392> * <var:N> + <nat:454> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:458> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:430> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:162> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:304> * <var:M> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:434> * <var:P> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:460> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:392> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:252> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:252> * <var:g> + <nat:460> * <var:r> + <nat:458> * <var:p> + <nat:304> * <var:M> + <nat:162> * <var:k> + <nat:434> * <var:P> + <nat:430> * <var:Y> + <nat:392> * <var:N> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 