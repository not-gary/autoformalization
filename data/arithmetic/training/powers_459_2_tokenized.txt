\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} There is some natural number $ <var:C> $ such that $ <var:C> $ more than or equivalent to <nat:2> as well as satisfying the condition that $ <nat:4> $ equals $ <var:C> ^ <nat:2> $ . \end{theorem} \begin{proof} Allow $ <var:C> = <nat:2> $ . We show our formula is valid considering $ <var:C> = <nat:2> $ is more than or equivalent to <nat:2> as well as for $ <nat:4> = <var:C> ^ <nat:2> $ . As a consequence , <nat:4> is the repeated product of some whole number $ <var:C> $ repeatedly multiplied to the degree <nat:2> plus $ <var:C> $ is greater than or equal to <nat:2> is verified . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Theorem <genP:1> : exists <var:C> : nat , ( <var:C> >= <nat:2> ) /\ ( <nat:4> = <var:C> ^ <nat:2> ) . Proof . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 