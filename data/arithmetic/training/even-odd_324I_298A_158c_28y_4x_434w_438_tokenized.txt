\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For some integers $ <var:A> $ , $ <var:x> $ , $ <var:y> $ , $ <var:w> $ , $ <var:c> $ , and $ <var:I> $ $ <nat:438> + <var:A> \times <nat:298> + <var:w> \times <nat:434> + <var:I> \times <nat:324> + <var:c> \times <nat:158> + <nat:4> \times <var:x> + <var:y> \times <nat:28> $ is guaranteed to be even . \end{theorem} \begin{proof} We show that the summation $ <var:I> * <nat:324> + <var:A> * <nat:298> + <var:c> * <nat:158> + <nat:28> * <var:y> + <nat:4> * <var:x> + <var:w> * <nat:434> $ is trivially even . The term $ <nat:28> \times <var:y> $ is obviously an even element of $ \mathbb{Z}^+ $ . This fact is proven from the fact the product between an even integer and an arbitrary element of $ \mathbb{Z}^+ $ is instrinsically an even element of $ \mathbb{Z}^+ $ . Further , we know that $ <var:I> \times <nat:324> $ is known to be an even number in $ \mathbb{N} $ . This fact is true from the knowledge the product between an even natural and some number is by itself an even element of $ \mathbb{N} $ . Building on , recall that the terms $ <nat:434> \times <var:w> $ , $ <var:x> \times <nat:4> $ , and moreover $ <var:c> \times <nat:158> $ are obviously even natural numbers since multiplying between an even natural number with any positive integer will be even . See that the pair $ <var:A> \times <nat:298> $ is guaranteed to be an even natural number . Our claim must be true from the fact the multiplication of an even element of $ \mathbb{Z}^+ $ with any element of $ \mathbb{N} $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:A> <var:x> <var:y> <var:w> <var:c> <var:I> : nat , Nat . even ( <nat:298> * <var:A> + <nat:434> * <var:w> + <nat:324> * <var:I> + <nat:158> * <var:c> + <nat:4> * <var:x> + <nat:28> * <var:y> + <nat:438> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:28> * <var:y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:434> * <var:w> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:4> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:158> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:298> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:298> * <var:A> + <nat:434> * <var:w> + <nat:324> * <var:I> + <nat:158> * <var:c> + <nat:4> * <var:x> + <nat:28> * <var:y> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 