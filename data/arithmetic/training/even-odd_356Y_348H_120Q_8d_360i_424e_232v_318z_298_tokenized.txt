\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:356> \times <var:Y> + <var:H> \times <nat:348> + <var:Q> \times <nat:120> + <var:d> \times <nat:8> + <var:i> \times <nat:360> + <var:e> \times <nat:424> + <nat:298> + <nat:232> \times <var:v> + <var:z> \times <nat:318> $ must be even . \end{theorem} \begin{proof} The summation of even whole numbers and an even element of $ \mathbb{Z}^+ $ is guaranteed to be an even number in $ \mathbb{Z}^+ $ . Remember that the pairs $ <nat:318> * <var:z> $ , $ <var:v> * <nat:232> $ , $ <nat:120> * <var:Q> $ , $ <var:i> * <nat:360> $ , as well as $ <nat:348> * <var:H> $ are known as even . Notice the products $ <var:Y> * <nat:356> $ and also $ <nat:8> * <var:d> $ are obviously even numbers . This claim must be true because taking the product of an even natural with a number in $ \mathbb{N} $ is guaranteed to be even . Building on , $ <var:e> * <nat:424> $ is trivially an even element of $ \mathbb{N} $ from the fact the product between an even element of $ \mathbb{Z}^+ $ with a number must be even . It can be proven that our main result must be true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Y> <var:H> <var:Q> <var:d> <var:i> <var:e> <var:v> <var:z> : nat , Nat . even ( <nat:356> * <var:Y> + <nat:348> * <var:H> + <nat:120> * <var:Q> + <nat:8> * <var:d> + <nat:360> * <var:i> + <nat:424> * <var:e> + <nat:232> * <var:v> + <nat:318> * <var:z> + <nat:298> ) = true . Proof . intros . repeat rewrite Nat . even_add . assert ( <genH> : Nat . even ( <nat:318> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:232> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:Q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:360> * <var:i> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:348> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:356> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:8> * <var:d> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:424> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 