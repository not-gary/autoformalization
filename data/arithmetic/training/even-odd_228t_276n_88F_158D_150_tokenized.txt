\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:276> \times <var:n> + <var:F> \times <nat:88> + <nat:150> + <var:D> \times <nat:158> + <var:t> \times <nat:228> $ is guaranteed to be even . \end{theorem} \begin{proof} Recall $ <nat:150> $ is an even positive integer . It can be demonstrated that $ <var:F> \times <nat:88> + <var:n> \times <nat:276> + <nat:150> + <nat:228> \times <var:t> + <var:D> \times <nat:158> $ is guaranteed to be even . Notice that the summation $ <nat:276> \cdot <var:n> + <nat:228> \cdot <var:t> + <var:D> \cdot <nat:158> + <nat:88> \cdot <var:F> + <nat:150> $ is known to be even from the fact the summation $ <nat:158> <var:D> + <nat:276> <var:n> + <var:t> <nat:228> + <nat:88> <var:F> $ is obviously even . $ <var:t> \times <nat:228> $ is trivially an even number in $ \mathbb{N} $ . This fact holds because the positive integer $ <nat:228> $ is guaranteed to be even . What's more , observe the term $ <nat:276> \times <var:n> $ is guaranteed to be an even natural number from the knowledge the product of an even element of $ \mathbb{Z}^+ $ with a natural is an even element of $ \mathbb{N} $ and likewise the natural number $ <nat:276> $ is clearly an even natural number . Likewise , the product $ <nat:88> \times <var:F> $ is even by reason that the multiplication between an even number in $ \mathbb{Z}^+ $ with a element of $ \mathbb{Z}^+ $ must be even , and is proven as a consequence that the positive integer $ <nat:88> $ is known to be even . Likewise , see the natural number $ <nat:158> $ is known as even . As a consequence , the pair $ <nat:158> \times <var:D> $ is guaranteed to be even too . Because multiplying between an even integer with some whole number is trivially an even whole number our claim must be true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:n> <var:F> <var:D> <var:t> : nat , Nat . even ( <nat:276> * <var:n> + <nat:88> * <var:F> + <nat:158> * <var:D> + <nat:228> * <var:t> + <nat:150> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:150> = true ) . { auto . } assert ( <genH> : Nat . even <nat:228> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:228> * <var:t> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:276> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:276> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:88> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:88> * <var:F> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:158> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:158> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:276> * <var:n> + <nat:88> * <var:F> + <nat:158> * <var:D> + <nat:228> * <var:t> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 