\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} We say that assuming there exists integer $ <var:L> $ more than or equivalent to <nat:2> . Define that $ <var:i> \in \mathbb{N} $ is a <def:enneract> positive integer if $ <var:i> = <var:L> ^ <nat:9> $ . Consequently the number <nat:512> is a <def:enneract> whole number . \end{theorem} \begin{proof} Recollect that <nat:512> is a <def:enneract> positive integer with the condition <nat:512> is the repeated product between positive integer $ <var:L> $ raised to the exponential <nat:9> greater than or equal to <nat:2> . We verify <nat:512> is a <def:enneract> number . We present <nat:512> is a <def:enneract> number in view of $ <nat:2> ^ <nat:9> = <nat:512> $ plus because $ <nat:2> \geq <nat:2> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:i> : nat ) := exists <var:L> : nat , ( <var:L> >= <nat:2> ) /\ ( <var:i> = <var:L> ^ <nat:9> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 