\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:86> + <nat:458> \cdot <var:b> + <nat:386> \cdot <var:V> $ must be even using every terms $ <var:b> $ , and $ <var:V> $ . \end{theorem} \begin{proof} The positive integer $ <nat:386> $ is known to be an even number in $ \mathbb{N} $ . In consequence , $ <nat:386> \cdot <var:V> $ will be even additionally . Recall that the number $ <nat:458> $ is known as even . For this reason , $ <nat:458> \cdot <var:b> $ is even as well . It can be demonstrated that $ <nat:86> + <nat:458> \times <var:b> + <nat:386> \times <var:V> $ will be even . We know that the summing between even elements of $ \mathbb{N} $ must be even . Take note that $ <nat:86> $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:b> <var:V> : nat , Nat . even ( <nat:458> * <var:b> + <nat:386> * <var:V> + <nat:86> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:386> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:386> * <var:V> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:458> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } repeat rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:86> = true ) . { auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 