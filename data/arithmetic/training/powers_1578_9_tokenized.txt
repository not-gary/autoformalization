\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \newtheorem{definition}{Definition} \begin{document} \begin{definition} Given that we get $ <var:S> ^ <nat:9> = <var:E> $ given $ <var:E> \in \mathbb{N} $ plus so that $ <var:S> \geq <nat:2> $ assuming some $ <var:S> \in \mathbb{N} $ , take that $ <var:E> $ is a <def:enneract> . \end{definition} \begin{theorem} The positive integer <nat:512> is a <def:enneract> natural number . \end{theorem} \begin{proof} Recall <nat:512> is a <def:enneract> whole number assuming that <nat:512> is the exponential of whole number $ <var:S> $ repeatedly multiplied to the exponent <nat:9> more than or equivalent to <nat:2> . We confirm the principle is true seeing that $ <nat:2> \geq <nat:2> $ as well as considering $ <nat:2> ^ <nat:9> = <nat:512> $ . Thus , <nat:512> is a <def:enneract> natural number has been verified . Hence , <nat:512> is the raising of of $ <var:S> \in \mathbb{Z}^+ $ taken to the power <nat:9> and $ <var:S> $ is larger than or equal to <nat:2> is validated . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:E> : nat ) := exists <var:S> : nat , ( <var:S> ^ <nat:9> = <var:E> ) /\ ( <var:S> >= <nat:2> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } exists <nat:2> . split . auto . apply <genH> . Qed . 