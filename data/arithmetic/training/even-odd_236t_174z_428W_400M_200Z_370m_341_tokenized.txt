\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:400> \cdot <var:M> + <var:Z> \cdot <nat:200> + <nat:370> \cdot <var:m> + <nat:428> \cdot <var:W> + <nat:341> + <var:t> \cdot <nat:236> + <var:z> \cdot <nat:174> $ is odd with some $ <var:Z> $ , $ <var:W> $ , $ <var:M> $ , $ <var:t> $ , $ <var:m> $ , and $ <var:z> \in \mathbb{Z}^+ $ . \end{theorem} \begin{proof} As a consequence that the totaling of even naturals successively with an odd whole number is an odd natural number , as a result the sum $ <var:Z> \times <nat:200> + <var:z> \times <nat:174> + <var:t> \times <nat:236> + <var:M> \times <nat:400> + <var:m> \times <nat:370> + <nat:428> \times <var:W> + <nat:341> $ by its very nature is odd . The positive integer $ <nat:236> $ is obviously an even number in $ \mathbb{Z}^+ $ . Accordingly , the product $ <nat:236> \cdot <var:t> $ will be even as well . On top of this , $ <var:z> \cdot <nat:174> $ is trivially an even integer from the fact the multiplication of an even positive integer with some element of $ \mathbb{Z}^+ $ is obviously an even positive integer . This must be true because the natural number $ <nat:174> $ is even . In addition , the term $ <nat:428> \cdot <var:W> $ is known as even . This fact holds as a consequence that the multiplication between an even integer with an arbitrary natural must be itself an even natural number , which is true from the fact the coefficient $ <nat:428> $ is known as even . On top of this , the pair $ <var:M> \cdot <nat:400> $ is known as an even number in $ \mathbb{Z}^+ $ because the leading term $ <nat:400> $ is clearly even . In addition , the product $ <var:Z> \cdot <nat:200> $ is guaranteed to be an even number since taking the product between an even positive integer and an arbitrary natural number will be even . This must be true since the whole number $ <nat:200> $ is guaranteed to be even . Take note $ <nat:370> \cdot <var:m> $ is known as even . This is true from the fact the product of an even positive integer with a number in $ \mathbb{Z}^+ $ must be even . This fact is proven as a consequence that the positive integer $ <nat:370> $ is known to be in itself an even number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Z> <var:W> <var:M> <var:t> <var:m> <var:z> : nat , Nat . odd ( <nat:400> * <var:M> + <nat:200> * <var:Z> + <nat:370> * <var:m> + <nat:428> * <var:W> + <nat:236> * <var:t> + <nat:174> * <var:z> + <nat:341> ) = true . Proof . intros . rewrite Nat . odd_add . rewrite <- Nat . negb_even . repeat rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:236> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:236> * <var:t> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:174> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:174> * <var:z> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:428> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:W> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:400> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:400> * <var:M> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:200> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:200> * <var:Z> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:370> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:370> * <var:m> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 