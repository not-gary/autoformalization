\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:438> \cdot <var:K> + <var:o> \cdot <nat:154> + <nat:334> \cdot <var:b> + <nat:67> + <var:t> \cdot <nat:80> $ must be odd given natural numbers $ <var:b> $ , $ <var:t> $ , $ <var:K> $ , and $ <var:o> $ . \end{theorem} \begin{proof} Take note that the summation $ <var:t> <nat:80> + <nat:438> <var:K> + <nat:154> <var:o> + <var:b> <nat:334> $ is clearly even , from the knowledge the summation of an even number and an even whole number must be even . The pairs $ <var:t> * <nat:80> $ and moreover $ <nat:154> * <var:o> $ are guaranteed to be even numbers in $ \mathbb{N} $ . This must be true since the integers $ <nat:80> $ additionally $ <nat:154> $ will be even . $ <var:b> * <nat:334> $ is known as even from the knowledge the leading term $ <nat:334> $ is guaranteed to be even . Likewise , the coefficient $ <nat:438> $ is obviously even . Then , the pair $ <var:K> * <nat:438> $ is guaranteed to be even additionally . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:b> <var:t> <var:K> <var:o> : nat , Nat . odd ( <nat:438> * <var:K> + <nat:154> * <var:o> + <nat:334> * <var:b> + <nat:80> * <var:t> + <nat:67> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:80> = true ) . { auto . } assert ( <genH> : Nat . even <nat:154> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:80> * <var:t> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:154> * <var:o> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:334> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:334> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:438> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:438> * <var:K> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:438> * <var:K> + <nat:154> * <var:o> + <nat:334> * <var:b> + <nat:80> * <var:t> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 