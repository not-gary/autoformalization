\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:a> \cdot <nat:274> + <nat:306> \cdot <var:p> + <nat:432> \cdot <var:i> + <var:o> \cdot <nat:422> + <nat:412> + <var:S> \cdot <nat:120> + <nat:498> \cdot <var:r> $ is even assuming $ <var:r> $ , $ <var:o> $ , $ <var:p> $ , $ <var:a> $ , $ <var:S> $ , and $ <var:i> \in \mathbb{N} $ . \end{theorem} \begin{proof} See the whole number $ <nat:422> $ is obviously an even number in $ \mathbb{N} $ . Accordingly , the pair $ <nat:422> * <var:o> $ is even additionally . The term $ <var:i> * <nat:432> $ is obviously an even number . This holds from the knowledge the multiplication between an even number in $ \mathbb{Z}^+ $ and any number is even . This claim is proven because the natural number $ <nat:432> $ is clearly even . Furthermore , the pair $ <var:S> * <nat:120> $ is clearly an even whole number from the knowledge multiplying between an even natural and a number in $ \mathbb{N} $ will be obviously an even integer , which must be true from the fact the whole number $ <nat:120> $ is guaranteed to be even . Likewise , recall $ <var:r> * <nat:498> $ is guaranteed to be an even number in $ \mathbb{Z}^+ $ by reason that the positive integer $ <nat:498> $ is obviously even . The leading term $ <nat:274> $ is trivially even . For this reason , the term $ <nat:274> * <var:a> $ is even also . Remember the product $ <var:p> * <nat:306> $ is even . This claim is true from the knowledge the multiplication of an even integer with any whole number will be by definition an even element of $ \mathbb{Z}^+ $ , and must be true because the positive integer $ <nat:306> $ is obviously even . It can be proven that $ <var:a> \times <nat:274> + <var:r> \times <nat:498> + <nat:422> \times <var:o> + <nat:412> + <nat:120> \times <var:S> + <var:p> \times <nat:306> + <nat:432> \times <var:i> $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:r> <var:o> <var:p> <var:a> <var:S> <var:i> : nat , Nat . even ( <nat:274> * <var:a> + <nat:306> * <var:p> + <nat:432> * <var:i> + <nat:422> * <var:o> + <nat:120> * <var:S> + <nat:498> * <var:r> + <nat:412> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:422> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:422> * <var:o> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:432> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:432> * <var:i> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:120> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:498> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:498> * <var:r> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:274> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:274> * <var:a> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:306> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:306> * <var:p> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 