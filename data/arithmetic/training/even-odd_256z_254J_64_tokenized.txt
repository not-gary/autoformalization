\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Taking any integers $ <var:z> $ , and $ <var:J> $ the expression $ <nat:254> \cdot <var:J> + <nat:64> + <var:z> \cdot <nat:256> $ will be even . \end{theorem} \begin{proof} It can be shown that the formula $ <nat:256> * <var:z> + <nat:64> + <nat:254> * <var:J> $ will be even making use of the knowledge the summing between an even natural and an even natural number must be even . Take note that $ <nat:64> $ is trivially an even number in $ \mathbb{N} $ . We use that $ <var:J> \times <nat:254> + <var:z> \times <nat:256> $ is known to be even . See the pair $ <nat:256> \times <var:z> $ is guaranteed to be even . Adding on , remember that the product $ <var:J> \times <nat:254> $ is known to be even from the knowledge multiplying of an even whole number with any natural number is even . $ <var:z> \times <nat:256> + <var:J> \times <nat:254> $ will be itself even from the fact the totaling between even numbers and an even natural is even in addition to from the knowledge the product between an even number in $ \mathbb{Z}^+ $ with an arbitrary number in $ \mathbb{N} $ is trivially an even integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:z> <var:J> : nat , Nat . even ( <nat:254> * <var:J> + <nat:256> * <var:z> + <nat:64> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:64> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:254> * <var:J> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:254> * <var:J> + <nat:256> * <var:z> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 