\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:89> + <var:z> \times <nat:380> + <var:r> \times <nat:122> $ will be odd taking all natural numbers $ <var:z> $ , and $ <var:r> $ . \end{theorem} \begin{proof} We use the assumption the fact that $ <nat:89> + <nat:380> \times <var:z> + <var:r> \times <nat:122> $ is odd from the fact the formula $ <var:r> \cdot <nat:122> + <var:z> \cdot <nat:380> $ is guaranteed to be even . Notice that the leading term $ <nat:380> $ is guaranteed to be even . As a consequence , the term $ <nat:380> \times <var:z> $ must be even also . The integer $ <nat:122> $ is known as an even number in $ \mathbb{N} $ . Hence , the pair $ <var:r> \times <nat:122> $ must be even in addition . This result is true by reason that the multiplication of an even positive integer and any number is obviously an even element of $ \mathbb{Z}^+ $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:z> <var:r> : nat , Nat . odd ( <nat:380> * <var:z> + <nat:122> * <var:r> + <nat:89> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:380> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:380> * <var:z> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:122> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:122> * <var:r> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:380> * <var:z> + <nat:122> * <var:r> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 