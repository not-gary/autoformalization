\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Let $ <var:W> $ , $ <var:T> $ , $ <var:e> \in \mathbb{Z}^+ $ satisfying the condition that $ <var:T> $ , $ <var:W> $ , $ <var:e> \geq <nat:2> $ as well as so that the number <nat:252> is the multiplication of $ <var:W> $ , $ <var:T> $ , and $ <var:e> \in \mathbb{N} $ . \end{theorem} \begin{proof} Take $ <var:T> = <nat:9> $ , $ <var:W> = <nat:7> $ and $ <var:e> = <nat:4> $ . Notice $ <nat:252> = <nat:7> \times <nat:9> \times <nat:4> $ . Plus notice $ <nat:9> $ , $ <nat:7> $ and $ <nat:4> $ are larger than or equal to <nat:2> . So , <nat:252> is the multiplication of some $ <var:T> $ , $ <var:e> $ and $ <var:W> $ as well as $ <var:e> $ , $ <var:T> $ , and $ <var:W> $ are more than or equivalent to <nat:2> is proven . \end{proof} \end{document} 
~
Require Import Lia . Theorem <genP:1> : exists <var:W> <var:T> <var:e> : nat , ( <var:T> >= <nat:2> ) /\ ( <var:W> >= <nat:2> ) /\ ( <var:e> >= <nat:2> ) /\ ( <nat:252> = <var:W> * <var:T> * <var:e> ) . Proof . exists <nat:7> . exists <nat:9> . exists <nat:4> . assert ( <genH> : <nat:9> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:7> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:4> >= <nat:2> ) . { lia . } repeat split . all : try apply <genH> . all : try apply <genH> . all : try apply <genH> . Qed . 