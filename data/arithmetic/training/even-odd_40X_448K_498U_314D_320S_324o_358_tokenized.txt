\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:358> + <nat:40> \times <var:X> + <nat:498> \times <var:U> + <nat:448> \times <var:K> + <nat:324> \times <var:o> + <nat:320> \times <var:S> + <nat:314> \times <var:D> $ is guaranteed to be even . \end{theorem} \begin{proof} The summing of even numbers with an even positive integer is guaranteed to be even . Accordingly , the summation $ <nat:40> \times <var:X> + <nat:314> \times <var:D> + <var:K> \times <nat:448> + <nat:320> \times <var:S> + <var:U> \times <nat:498> + <var:o> \times <nat:324> + <nat:358> $ is even . Notice that $ <nat:358> $ is obviously even . We utilize the knowledge $ <var:o> \cdot <nat:324> + <var:X> \cdot <nat:40> + <var:S> \cdot <nat:320> + <var:U> \cdot <nat:498> + <nat:358> + <nat:448> \cdot <var:K> + <var:D> \cdot <nat:314> $ is known as even because $ <nat:324> \cdot <var:o> + <nat:314> \cdot <var:D> + <var:S> \cdot <nat:320> + <var:U> \cdot <nat:498> + <nat:40> \cdot <var:X> + <nat:448> \cdot <var:K> $ is trivially even . The terms $ <var:D> * <nat:314> $ , $ <nat:324> * <var:o> $ , $ <nat:448> * <var:K> $ , $ <var:S> * <nat:320> $ , as well as $ <var:U> * <nat:498> $ are known to be even naturals as a consequence that the product between an even natural number with an arbitrary whole number will be even . Take note the pair $ <nat:40> * <var:X> $ is known as even because the product of an even integer and any number in $ \mathbb{Z}^+ $ is by itself an even number . By reason that taking the product between even numbers with an arbitrary element of $ \mathbb{N} $ is an even natural number the expression $ <nat:40> <var:X> + <nat:448> <var:K> + <nat:498> <var:U> + <var:D> <nat:314> + <nat:320> <var:S> + <var:o> <nat:324> $ must be clearly even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:X> <var:U> <var:K> <var:o> <var:S> <var:D> : nat , Nat . even ( <nat:40> * <var:X> + <nat:498> * <var:U> + <nat:448> * <var:K> + <nat:324> * <var:o> + <nat:320> * <var:S> + <nat:314> * <var:D> + <nat:358> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:358> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:314> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:448> * <var:K> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:320> * <var:S> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:498> * <var:U> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:X> + <nat:498> * <var:U> + <nat:448> * <var:K> + <nat:324> * <var:o> + <nat:320> * <var:S> + <nat:314> * <var:D> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 