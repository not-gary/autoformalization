\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:140> * <var:q> + <nat:122> + <var:V> * <nat:324> + <nat:452> * <var:B> + <var:O> * <nat:434> + <var:t> * <nat:120> + <nat:406> * <var:R> + <nat:14> * <var:U> + <nat:404> * <var:l> $ is guaranteed to be even using all $ <var:U> $ , $ <var:R> $ , $ <var:l> $ , $ <var:V> $ , $ <var:t> $ , $ <var:B> $ , $ <var:O> $ , and $ <var:q> $ . \end{theorem} \begin{proof} We check that $ <var:V> * <nat:324> + <nat:120> * <var:t> + <var:O> * <nat:434> + <nat:404> * <var:l> + <var:U> * <nat:14> + <nat:140> * <var:q> + <nat:452> * <var:B> + <var:R> * <nat:406> $ is clearly even . We know that the totaling of even positive integers collectively is an even element of $ \mathbb{Z}^+ $ . The products $ <var:V> \times <nat:324> $ , $ <var:U> \times <nat:14> $ , $ <var:O> \times <nat:434> $ , $ <var:t> \times <nat:120> $ , in addition to $ <nat:452> \times <var:B> $ are clearly even elements of $ \mathbb{N} $ because multiplying between an even whole number and an arbitrary number in $ \mathbb{Z}^+ $ must be an even integer . What's more , observe that the terms $ <nat:140> \times <var:q> $ plus $ <var:R> \times <nat:406> $ are obviously even . Adding on , the pair $ <nat:404> \times <var:l> $ is guaranteed to be even . This is true since multiplying between an even positive integer with an arbitrary positive integer is an even whole number . and since taking the product of an even natural with a element of $ \mathbb{N} $ is guaranteed to be even . We know the addition of even numbers in $ \mathbb{Z}^+ $ concurrently must be an even number in $ \mathbb{Z}^+ $ . Observe $ <nat:122> $ is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:U> <var:R> <var:l> <var:V> <var:t> <var:B> <var:O> <var:q> : nat , Nat . even ( <nat:140> * <var:q> + <nat:324> * <var:V> + <nat:452> * <var:B> + <nat:434> * <var:O> + <nat:120> * <var:t> + <nat:406> * <var:R> + <nat:14> * <var:U> + <nat:404> * <var:l> + <nat:122> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:324> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:14> * <var:U> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:434> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:t> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:452> * <var:B> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:406> * <var:R> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:404> * <var:l> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:q> + <nat:324> * <var:V> + <nat:452> * <var:B> + <nat:434> * <var:O> + <nat:120> * <var:t> + <nat:406> * <var:R> + <nat:14> * <var:U> + <nat:404> * <var:l> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:122> = true ) . { auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 