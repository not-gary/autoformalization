\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:298> <var:n> + <nat:67> + <var:V> <nat:146> + <var:e> <nat:78> + <nat:120> <var:c> + <var:T> <nat:42> + <nat:140> <var:I> $ is guaranteed to be odd assuming every terms $ <var:T> $ , $ <var:n> $ , $ <var:c> $ , $ <var:I> $ , $ <var:e> $ , and $ <var:V> $ . \end{theorem} \begin{proof} Notice that the sum $ <var:V> * <nat:146> + <nat:67> + <nat:78> * <var:e> + <var:T> * <nat:42> + <var:c> * <nat:120> + <nat:140> * <var:I> + <var:n> * <nat:298> $ is known to be odd by reason that $ <var:T> \cdot <nat:42> + <nat:298> \cdot <var:n> + <var:e> \cdot <nat:78> + <var:V> \cdot <nat:146> + <var:c> \cdot <nat:120> + <nat:140> \cdot <var:I> $ is known as even . The coefficient $ <nat:140> $ is known to be an even whole number . Therefore , the pair $ <var:I> * <nat:140> $ is even at the same time . The number $ <nat:42> $ is clearly even . Hence , $ <var:T> * <nat:42> $ is even as well . In addition , notice the leading term $ <nat:120> $ is even . So , the product $ <nat:120> * <var:c> $ is guaranteed to be even as well . The integer $ <nat:298> $ is obviously even . Therefore , the term $ <nat:298> * <var:n> $ must be even as well . Further , see that the number $ <nat:78> $ is known to be even . In consequence , the term $ <nat:78> * <var:e> $ is even at the same time . The product $ <nat:146> * <var:V> $ is an even element of $ \mathbb{N} $ from the knowledge the positive integer $ <nat:146> $ is known to be even . From the fact the summation of even elements of $ \mathbb{Z}^+ $ and an even number in $ \mathbb{N} $ must be an even number , our lemma is proven . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:T> <var:n> <var:c> <var:I> <var:e> <var:V> : nat , Nat . odd ( <nat:298> * <var:n> + <nat:146> * <var:V> + <nat:78> * <var:e> + <nat:120> * <var:c> + <nat:42> * <var:T> + <nat:140> * <var:I> + <nat:67> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:140> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:I> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:42> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:42> * <var:T> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:120> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:c> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:298> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:298> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:78> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:78> * <var:e> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:146> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:146> * <var:V> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:298> * <var:n> + <nat:146> * <var:V> + <nat:78> * <var:e> + <nat:120> * <var:c> + <nat:42> * <var:T> + <nat:140> * <var:I> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 