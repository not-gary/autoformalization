\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Given some natural terms $ <var:T> $ , $ <var:c> $ , $ <var:b> $ , $ <var:p> $ , $ <var:z> $ , and $ <var:d> $ $ <nat:104> \cdot <var:b> + <var:T> \cdot <nat:466> + <nat:287> + <var:c> \cdot <nat:36> + <var:z> \cdot <nat:346> + <var:p> \cdot <nat:348> + <var:d> \cdot <nat:302> $ is odd . \end{theorem} \begin{proof} We justify the sum $ <nat:36> \cdot <var:c> + <nat:302> \cdot <var:d> + <nat:346> \cdot <var:z> + <nat:287> + <var:p> \cdot <nat:348> + <nat:466> \cdot <var:T> + <nat:104> \cdot <var:b> $ is odd . We know $ <nat:287> $ is known as odd . It can be demonstrated that that $ <nat:466> <var:T> + <var:d> <nat:302> + <var:c> <nat:36> + <var:z> <nat:346> + <var:p> <nat:348> + <nat:104> <var:b> $ is clearly even . See the addition between even elements of $ \mathbb{Z}^+ $ concurrently with an even number is by definition an even element of $ \mathbb{Z}^+ $ . The pairs $ <nat:348> \cdot <var:p> $ , $ <nat:346> \cdot <var:z> $ , plus $ <nat:466> \cdot <var:T> $ are known to be even . This claim is proven because taking the product of an even element of $ \mathbb{N} $ with any element of $ \mathbb{Z}^+ $ will be even . Likewise , take note the term $ <nat:36> \cdot <var:c> $ is clearly even since the product of an even integer with any number will be an even number . We know the pair $ <var:d> \cdot <nat:302> $ is known to be an even whole number . This claim is true since the product between an even number in $ \mathbb{Z}^+ $ and a natural is even . Take note the term $ <nat:104> \cdot <var:b> $ is guaranteed to be even . This holds from the fact taking the product between an even positive integer and any number in $ \mathbb{N} $ is an even element of $ \mathbb{Z}^+ $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:T> <var:c> <var:b> <var:p> <var:z> <var:d> : nat , Nat . odd ( <nat:104> * <var:b> + <nat:466> * <var:T> + <nat:36> * <var:c> + <nat:346> * <var:z> + <nat:348> * <var:p> + <nat:302> * <var:d> + <nat:287> ) = true . Proof . intros . assert ( <genH> : Nat . odd <nat:287> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:348> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:346> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:466> * <var:T> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:36> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:302> * <var:d> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:104> * <var:b> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:104> * <var:b> + <nat:466> * <var:T> + <nat:36> * <var:c> + <nat:346> * <var:z> + <nat:348> * <var:p> + <nat:302> * <var:d> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 