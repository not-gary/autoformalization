\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Take $ <var:A> $ are whole numbers so that $ <var:A> \geq <nat:2> $ . Take as given that $ <var:C> \in \mathbb{N} $ is named a <def:enneract> number given $ <var:A> ^ <nat:9> = <var:C> $ . Then $ <nat:512> = <var:C> $ is a <def:enneract> . \end{theorem} \begin{proof} Assume $ <var:A> = <nat:2> $ . We verify $ <var:A> \geq <nat:2> $ and $ <nat:512> = <var:A> ^ <nat:9> $ . We confirm our formula is valid for $ <nat:512> = <nat:2> ^ <nat:9> = <var:A> ^ <nat:9> $ and by reason of $ <nat:2> \geq <nat:2> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:C> : nat ) := exists <var:A> : nat , ( <var:A> >= <nat:2> ) /\ ( <var:A> ^ <nat:9> = <var:C> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 