\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:C> ^ <nat:9> $ is the same as $ <nat:512> $ satisfying the condition that $ <var:C> $ larger than or equal to <nat:2> . \end{theorem} \begin{proof} Allow $ <var:C> = <nat:2> $ . We confirm $ <var:C> \geq <nat:2> $ and <nat:512> is the exponential of some integer $ <var:C> $ exponentiated to the exponent <nat:9> . Note $ <nat:2> ^ <nat:9> = <var:C> ^ <nat:9> = <nat:512> $ . Plus notice $ <var:C> = <nat:2> $ is more than or equivalent to <nat:2> . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Theorem <genP:1> : exists <var:C> : nat , ( <var:C> ^ <nat:9> = <nat:512> ) /\ ( <var:C> >= <nat:2> ) . Proof . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . auto . apply <genH> . Qed . 