\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Taking any natural numbers $ <var:q> $ , $ <var:g> $ , $ <var:B> $ , $ <var:A> $ , $ <var:j> $ , and $ <var:D> $ the expression $ <nat:328> \times <var:j> + <var:A> \times <nat:206> + <nat:232> + <nat:334> \times <var:q> + <nat:40> \times <var:B> + <nat:458> \times <var:D> + <nat:460> \times <var:g> $ is guaranteed to be even . \end{theorem} \begin{proof} The addition of even integers successively will be even . Take note that $ <nat:232> $ is an even number in $ \mathbb{N} $ . Remember the formula $ <nat:232> + <var:j> \times <nat:328> + <nat:206> \times <var:A> + <nat:40> \times <var:B> + <var:g> \times <nat:460> + <var:q> \times <nat:334> + <nat:458> \times <var:D> $ is obviously even from the fact the formula $ <nat:206> * <var:A> + <var:D> * <nat:458> + <var:B> * <nat:40> + <var:j> * <nat:328> + <var:g> * <nat:460> + <var:q> * <nat:334> $ is even . Remember that the positive integer $ <nat:40> $ is an even natural number . Therefore , $ <var:B> <nat:40> $ must be even also . Observe the pair $ <nat:328> <var:j> $ is obviously even because the leading term $ <nat:328> $ is even . The pair $ <nat:206> <var:A> $ is even . This fact is proven because the whole number $ <nat:206> $ is trivially an even number . We know that the product $ <var:q> <nat:334> $ is known to be an even number in $ \mathbb{Z}^+ $ as a consequence that taking the product between an even natural number with a number in $ \mathbb{N} $ will be by its very nature an even integer and the number $ <nat:334> $ is known as even . Remember the number $ <nat:458> $ is known to be even . Consequently , the pair $ <nat:458> <var:D> $ will be even also . What's more , recall the term $ <nat:460> <var:g> $ is clearly an even natural number . This fact holds since the multiplication of an even number in $ \mathbb{Z}^+ $ and an arbitrary number is guaranteed to be even . This claim is true from the fact the leading term $ <nat:460> $ is obviously instrinsically an even number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:q> <var:g> <var:B> <var:A> <var:j> <var:D> : nat , Nat . even ( <nat:328> * <var:j> + <nat:206> * <var:A> + <nat:334> * <var:q> + <nat:40> * <var:B> + <nat:458> * <var:D> + <nat:460> * <var:g> + <nat:232> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:232> = true ) . { auto . } assert ( <genH> : Nat . even <nat:40> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:B> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:328> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:328> * <var:j> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:206> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:206> * <var:A> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:334> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:334> * <var:q> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:458> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:460> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:460> * <var:g> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:328> * <var:j> + <nat:206> * <var:A> + <nat:334> * <var:q> + <nat:40> * <var:B> + <nat:458> * <var:D> + <nat:460> * <var:g> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 