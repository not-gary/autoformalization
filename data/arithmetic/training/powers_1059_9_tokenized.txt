\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Let that $ <var:t> $ is a <def:enneract> natural number only if assuming some $ <var:Z> \in \mathbb{N} $ we have $ <var:Z> ^ <nat:9> = <var:t> $ with some whole number $ <var:t> $ as well as where $ <var:Z> \geq <nat:2> $ . Then $ <nat:512> = <var:t> $ is a <def:enneract> number . \end{theorem} \begin{proof} Remember <nat:512> is a <def:enneract> natural number granted that <nat:512> is the exponent between whole number $ <var:Z> $ repeatedly multiplied to the exponent <nat:9> larger than or equal to <nat:2> . Notice $ <nat:512> = <var:Z> ^ <nat:9> $ suggests $ <var:Z> = <nat:2> $ . In addition note that $ <nat:2> \geq <nat:2> $ . This implies <nat:512> is a <def:enneract> positive integer . Because of this , <nat:512> is a <def:enneract> positive integer is true has been proven . Thus , <nat:512> is the raising of between some number $ <var:Z> $ raised to the exponential <nat:9> is proven . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:t> : nat ) := exists <var:Z> : nat , ( <var:Z> ^ <nat:9> = <var:t> ) /\ ( <var:Z> >= <nat:2> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . auto . apply <genH> . Qed . 