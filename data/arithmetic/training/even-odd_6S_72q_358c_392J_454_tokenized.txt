\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:392> <var:J> + <nat:358> <var:c> + <var:q> <nat:72> + <nat:454> + <var:S> <nat:6> $ is even for any positive integers $ <var:J> $ , $ <var:q> $ , $ <var:S> $ , and $ <var:c> $ . \end{theorem} \begin{proof} Take note the totaling between an even natural number and an even natural is trivially an even number in $ \mathbb{Z}^+ $ . The summation $ <var:S> <nat:6> + <nat:72> <var:q> + <nat:454> + <nat:358> <var:c> + <nat:392> <var:J> $ is known to be even from the fact $ <nat:6> * <var:S> + <nat:72> * <var:q> + <nat:358> * <var:c> + <nat:392> * <var:J> $ is obviously even , from the knowledge the addition of an even number in $ \mathbb{N} $ with an even natural is an even natural number . What's more , observe the products $ <nat:6> * <var:S> $ , $ <var:q> * <nat:72> $ , $ <nat:358> * <var:c> $ , and moreover $ <nat:392> * <var:J> $ are known to be even . This fact holds as a consequence that the integers $ <nat:6> $ , $ <nat:72> $ , $ <nat:358> $ , and also $ <nat:392> $ are known as even numbers in $ \mathbb{N} $ . additionally from the fact multiplying between an even integer and any number in $ \mathbb{N} $ must be an even whole number this claim holds . It can be proven that the theorem is proven . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:J> <var:q> <var:S> <var:c> : nat , Nat . even ( <nat:392> * <var:J> + <nat:358> * <var:c> + <nat:72> * <var:q> + <nat:6> * <var:S> + <nat:454> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:6> = true ) . { auto . } assert ( <genH> : Nat . even <nat:72> = true ) . { auto . } assert ( <genH> : Nat . even <nat:358> = true ) . { auto . } assert ( <genH> : Nat . even <nat:392> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:6> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:72> * <var:q> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:358> * <var:c> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:392> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:392> * <var:J> + <nat:358> * <var:c> + <nat:72> * <var:q> + <nat:6> * <var:S> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . auto . Qed . 