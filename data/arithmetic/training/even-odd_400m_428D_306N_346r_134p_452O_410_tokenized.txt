\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:452> \times <var:O> + <nat:428> \times <var:D> + <var:N> \times <nat:306> + <nat:410> + <var:p> \times <nat:134> + <var:r> \times <nat:346> + <nat:400> \times <var:m> $ must be even with $ <var:N> $ , $ <var:m> $ , $ <var:r> $ , $ <var:D> $ , $ <var:p> $ , and $ <var:O> \in \mathbb{Z}^+ $ . \end{theorem} \begin{proof} The formula $ <var:m> * <nat:400> + <nat:428> * <var:D> + <var:N> * <nat:306> + <nat:346> * <var:r> + <var:p> * <nat:134> + <var:O> * <nat:452> $ is obviously even . Observe the summation between even positive integers collectively with an even integer is guaranteed to be in itself an even number in $ \mathbb{Z}^+ $ . Remember that the term $ <nat:134> \times <var:p> $ is known as an even natural number . Further , the product $ <nat:452> \times <var:O> $ is clearly even . The term $ <nat:400> \times <var:m> $ is obviously even . Additionally , the products $ <var:D> \times <nat:428> $ and likewise $ <var:r> \times <nat:346> $ are known to be even since the multiplication of an even number in $ \mathbb{N} $ with any integer will be even . Building on , see $ <nat:306> \times <var:N> $ is trivially even . This fact must be true from the fact taking the product between an even whole number and an arbitrary integer is even . as well as from the knowledge the product of an even number in $ \mathbb{Z}^+ $ with a number will be even . Notice that $ <nat:410> $ is even . Observe that the addition of even naturals concurrently with an even number in $ \mathbb{N} $ must be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:N> <var:m> <var:r> <var:D> <var:p> <var:O> : nat , Nat . even ( <nat:452> * <var:O> + <nat:428> * <var:D> + <nat:306> * <var:N> + <nat:134> * <var:p> + <nat:346> * <var:r> + <nat:400> * <var:m> + <nat:410> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:134> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:452> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:400> * <var:m> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:346> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:306> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:452> * <var:O> + <nat:428> * <var:D> + <nat:306> * <var:N> + <nat:134> * <var:p> + <nat:346> * <var:r> + <nat:400> * <var:m> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:410> = true ) . { auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 