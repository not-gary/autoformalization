\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:416> \times <var:J> + <nat:137> + <var:s> \times <nat:156> + <var:f> \times <nat:318> + <nat:366> \times <var:V> $ must be odd taking every integers $ <var:J> $ , $ <var:f> $ , $ <var:s> $ , and $ <var:V> $ . \end{theorem} \begin{proof} We use the understanding that the formula $ <var:V> * <nat:366> + <nat:416> * <var:J> + <var:s> * <nat:156> + <var:f> * <nat:318> + <nat:137> $ is obviously odd because the summation $ <var:J> \times <nat:416> + <var:f> \times <nat:318> + <var:V> \times <nat:366> + <var:s> \times <nat:156> $ is even . Recall the summation of even numbers concurrently and an even number in $ \mathbb{Z}^+ $ will be even . See that the term $ <var:s> \cdot <nat:156> $ is clearly an even natural as a consequence that taking the product between an even whole number and any integer is by definition an even number . This holds because the coefficient $ <nat:156> $ is clearly even . Notice the leading term $ <nat:318> $ is guaranteed to be even . Therefore , the pair $ <var:f> \cdot <nat:318> $ is guaranteed to be even at the same time . The coefficient $ <nat:416> $ is known as an even number in $ \mathbb{N} $ . In consequence , the pair $ <var:J> \cdot <nat:416> $ must be even in addition . Observe that the pair $ <var:V> \cdot <nat:366> $ is even by reason that multiplying between an even element of $ \mathbb{N} $ and some integer will be itself an even number . This fact is proven by reason that the whole number $ <nat:366> $ is guaranteed to be an even element of $ \mathbb{N} $ . and also as a consequence that taking the product of an even natural with any integer is an even number this theorem holds . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:J> <var:f> <var:s> <var:V> : nat , Nat . odd ( <nat:416> * <var:J> + <nat:156> * <var:s> + <nat:318> * <var:f> + <nat:366> * <var:V> + <nat:137> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:156> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:156> * <var:s> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:318> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:318> * <var:f> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:416> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:416> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:366> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:366> * <var:V> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:416> * <var:J> + <nat:156> * <var:s> + <nat:318> * <var:f> + <nat:366> * <var:V> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 