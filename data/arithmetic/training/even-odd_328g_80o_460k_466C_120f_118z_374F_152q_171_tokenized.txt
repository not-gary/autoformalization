\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:466> * <var:C> + <nat:328> * <var:g> + <nat:460> * <var:k> + <nat:171> + <var:q> * <nat:152> + <var:F> * <nat:374> + <nat:118> * <var:z> + <nat:120> * <var:f> + <nat:80> * <var:o> $ is guaranteed to be odd given all $ <var:o> $ , $ <var:g> $ , $ <var:F> $ , $ <var:C> $ , $ <var:q> $ , $ <var:k> $ , $ <var:f> $ , and $ <var:z> \in \mathbb{N} $ . \end{theorem} \begin{proof} We utilize that $ <var:F> <nat:374> + <nat:118> <var:z> + <var:k> <nat:460> + <var:q> <nat:152> + <nat:120> <var:f> + <var:g> <nat:328> + <nat:466> <var:C> + <nat:80> <var:o> + <nat:171> $ is clearly odd because the expression $ <nat:328> \times <var:g> + <nat:80> \times <var:o> + <var:k> \times <nat:460> + <var:C> \times <nat:466> + <nat:120> \times <var:f> + <var:z> \times <nat:118> + <var:F> \times <nat:374> + <nat:152> \times <var:q> $ is guaranteed to be even . Recall the adding of even numbers collectively and an even number in $ \mathbb{Z}^+ $ must be even . The product $ <var:q> \times <nat:152> $ is obviously even as a consequence that taking the product of an even number in $ \mathbb{Z}^+ $ and an arbitrary natural is guaranteed to be even . Adding on , the terms $ <nat:460> \times <var:k> $ , $ <var:C> \times <nat:466> $ , additionally $ <var:f> \times <nat:120> $ are trivially even integers . This fact is proven by reason that multiplying between an even number and a number in $ \mathbb{N} $ must be even . The products $ <var:o> \times <nat:80> $ and $ <var:z> \times <nat:118> $ are even . We know the product $ <var:F> \times <nat:374> $ is known to be even because multiplying between an even element of $ \mathbb{Z}^+ $ with some number in $ \mathbb{Z}^+ $ is guaranteed to be an even number in $ \mathbb{N} $ . Furthermore , see that the product $ <var:g> \times <nat:328> $ is even . This is proven by reason that multiplying between an even natural and a number in $ \mathbb{N} $ is guaranteed to be fundamentally an even number in $ \mathbb{N} $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:o> <var:g> <var:F> <var:C> <var:q> <var:k> <var:f> <var:z> : nat , Nat . odd ( <nat:466> * <var:C> + <nat:328> * <var:g> + <nat:460> * <var:k> + <nat:152> * <var:q> + <nat:374> * <var:F> + <nat:118> * <var:z> + <nat:120> * <var:f> + <nat:80> * <var:o> + <nat:171> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:152> * <var:q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:460> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:466> * <var:C> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:f> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:80> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:118> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:374> * <var:F> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:328> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:466> * <var:C> + <nat:328> * <var:g> + <nat:460> * <var:k> + <nat:152> * <var:q> + <nat:374> * <var:F> + <nat:118> * <var:z> + <nat:120> * <var:f> + <nat:80> * <var:o> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 