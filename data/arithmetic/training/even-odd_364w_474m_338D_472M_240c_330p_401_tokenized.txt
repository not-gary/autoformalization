\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Assuming every natural terms $ <var:c> $ , $ <var:D> $ , $ <var:w> $ , $ <var:p> $ , $ <var:M> $ , and $ <var:m> $ $ <var:w> * <nat:364> + <nat:472> * <var:M> + <nat:474> * <var:m> + <nat:401> + <nat:338> * <var:D> + <nat:240> * <var:c> + <nat:330> * <var:p> $ will be odd . \end{theorem} \begin{proof} Take note that $ <nat:401> $ is clearly an odd number . From the fact the adding of even natural numbers successively and an odd integer will be odd , thus our result holds . The summation $ <var:m> <nat:474> + <nat:240> <var:c> + <nat:472> <var:M> + <var:w> <nat:364> + <nat:401> + <var:D> <nat:338> + <var:p> <nat:330> $ is guaranteed to be odd by reason that the summation $ <var:m> * <nat:474> + <var:p> * <nat:330> + <nat:472> * <var:M> + <nat:364> * <var:w> + <nat:240> * <var:c> + <var:D> * <nat:338> $ is even , from the fact the totaling of an even integer and an even element of $ \mathbb{N} $ is guaranteed to be even . Recall the pairs $ <var:p> \cdot <nat:330> $ and also $ <var:c> \cdot <nat:240> $ must be even . The pairs $ <nat:364> \cdot <var:w> $ as well as $ <nat:474> \cdot <var:m> $ are known to be even whole numbers . Observe $ <nat:338> \cdot <var:D> $ is trivially even by reason that the multiplication between an even positive integer with some element of $ \mathbb{Z}^+ $ is even . Likewise , notice that the pair $ <var:M> \cdot <nat:472> $ is clearly even . and because the product of an even positive integer and any element of $ \mathbb{N} $ must be an even number in $ \mathbb{N} $ the formula $ <var:w> \times <nat:364> + <var:m> \times <nat:474> + <var:D> \times <nat:338> + <nat:472> \times <var:M> + <nat:240> \times <var:c> + <nat:330> \times <var:p> $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:c> <var:D> <var:w> <var:p> <var:M> <var:m> : nat , Nat . odd ( <nat:364> * <var:w> + <nat:472> * <var:M> + <nat:474> * <var:m> + <nat:338> * <var:D> + <nat:240> * <var:c> + <nat:330> * <var:p> + <nat:401> ) = true . Proof . intros . assert ( <genH> : Nat . odd <nat:401> = true ) . { auto . } rewrite Nat . odd_add . assert ( <genH> : Nat . even ( <nat:330> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:240> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:w> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:474> * <var:m> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:338> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:472> * <var:M> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:w> + <nat:472> * <var:M> + <nat:474> * <var:m> + <nat:338> * <var:D> + <nat:240> * <var:c> + <nat:330> * <var:p> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 