\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With every $ <var:n> $ , $ <var:U> $ , $ <var:S> $ , $ <var:w> $ , $ <var:b> $ , and $ <var:A> \in \mathbb{N} $ $ <nat:50> <var:U> + <nat:302> + <nat:428> <var:S> + <nat:140> <var:A> + <nat:416> <var:b> + <nat:164> <var:n> + <var:w> <nat:126> $ is guaranteed to be even . \end{theorem} \begin{proof} The product $ <var:b> \times <nat:416> $ is guaranteed to be an even natural from the fact the whole number $ <nat:416> $ is clearly itself an even positive integer . In addition , the pair $ <nat:126> \times <var:w> $ is obviously an even positive integer . This claim must be true from the fact the number $ <nat:126> $ is clearly trivially an even number in $ \mathbb{Z}^+ $ . The product $ <var:n> \times <nat:164> $ is known to be even by reason that the number $ <nat:164> $ is obviously even . Observe the natural number $ <nat:428> $ is obviously even . As a consequence , the product $ <nat:428> \times <var:S> $ will be even in addition . The pair $ <nat:140> \times <var:A> $ is guaranteed to be an even number in $ \mathbb{Z}^+ $ because the leading term $ <nat:140> $ is known to be even . Recall that the natural number $ <nat:50> $ is an even natural . Accordingly , $ <nat:50> \times <var:U> $ is even at the same time . The adding of even whole numbers successively with an even natural number must be even . Hence , the sum $ <var:A> * <nat:140> + <nat:126> * <var:w> + <nat:164> * <var:n> + <nat:302> + <var:b> * <nat:416> + <var:U> * <nat:50> + <var:S> * <nat:428> $ will be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:n> <var:U> <var:S> <var:w> <var:b> <var:A> : nat , Nat . even ( <nat:50> * <var:U> + <nat:428> * <var:S> + <nat:140> * <var:A> + <nat:416> * <var:b> + <nat:164> * <var:n> + <nat:126> * <var:w> + <nat:302> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:416> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:416> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:126> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:126> * <var:w> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:164> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:164> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:428> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:140> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:A> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:50> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:50> * <var:U> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 