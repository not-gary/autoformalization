\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} We denote that $ <var:R> $ is a <def:cube> positive integer assuming that given some $ <var:c> $ we have $ <var:R> = <var:c> ^ <nat:3> $ assuming $ <var:R> \in \mathbb{N} $ and satisfying the condition that $ <var:c> \geq <nat:2> $ . Thus the integer <nat:8> is a <def:cube> integer . \end{theorem} \begin{proof} We demonstrate <nat:8> is the raising of between some $ <var:c> $ exponentiated to the exponent <nat:3> plus $ <var:c> $ is larger than or equal to <nat:2> . Notice $ <nat:2> $ is larger than or equivalent to <nat:2> . Furthermore note that likewise $ <var:c> ^ <nat:3> = <nat:2> ^ <nat:3> = <nat:8> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:cube> ( <var:R> : nat ) := exists <var:c> : nat , ( <var:R> = <var:c> ^ <nat:3> ) /\ ( <var:c> >= <nat:2> ) . Theorem <genP:1> : <def:cube> <nat:8> . Proof . unfold <def:cube> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } exists <nat:2> . split . auto . apply <genH> . Qed . 