\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:y> <nat:156> + <nat:262> <var:f> + <nat:318> <var:d> + <nat:192> + <var:b> <nat:86> $ will be even taking every positive integers $ <var:f> $ , $ <var:d> $ , $ <var:b> $ , and $ <var:y> $ . \end{theorem} \begin{proof} Observe that the summation of even numbers in $ \mathbb{N} $ is guaranteed to be even . The expression $ <var:y> \cdot <nat:156> + <nat:262> \cdot <var:f> + <var:d> \cdot <nat:318> + <var:b> \cdot <nat:86> $ is clearly even . $ <nat:262> \times <var:f> $ is clearly an even whole number because the coefficient $ <nat:262> $ is trivially clearly an even natural . The pair $ <var:y> \times <nat:156> $ is guaranteed to be an even natural from the fact the positive integer $ <nat:156> $ is known to be by itself an even number in $ \mathbb{Z}^+ $ . In addition to this , we know that the term $ <nat:86> \times <var:b> $ is trivially even . This claim is true since the multiplication between an even element of $ \mathbb{Z}^+ $ and an arbitrary number is an even positive integer , which is true from the fact the leading term $ <nat:86> $ is known to be even . In addition , the coefficient $ <nat:318> $ is known to be even . Then , the pair $ <nat:318> \times <var:d> $ is guaranteed to be even additionally . The summation $ <var:b> \times <nat:86> + <var:d> \times <nat:318> + <var:f> \times <nat:262> + <var:y> \times <nat:156> $ will be even because the addition of an even whole number and even numbers in $ \mathbb{Z}^+ $ is even . It can be verified that our claim is true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:f> <var:d> <var:b> <var:y> : nat , Nat . even ( <nat:156> * <var:y> + <nat:262> * <var:f> + <nat:318> * <var:d> + <nat:86> * <var:b> + <nat:192> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:262> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:262> * <var:f> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:156> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:156> * <var:y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:86> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:86> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:318> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:318> * <var:d> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:156> * <var:y> + <nat:262> * <var:f> + <nat:318> * <var:d> + <nat:86> * <var:b> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . auto . Qed . 