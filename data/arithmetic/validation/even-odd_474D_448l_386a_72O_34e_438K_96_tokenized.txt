\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:474> \cdot <var:D> + <var:l> \cdot <nat:448> + <nat:386> \cdot <var:a> + <nat:96> + <nat:72> \cdot <var:O> + <var:e> \cdot <nat:34> + <var:K> \cdot <nat:438> $ is guaranteed to be even . \end{theorem} \begin{proof} Take note $ <nat:96> $ is obviously an even integer . Remember the adding of even positive integers concurrently with an even number is guaranteed to be by its very nature an even element of $ \mathbb{Z}^+ $ . Notice the terms $ <var:e> \cdot <nat:34> $ , $ <nat:448> \cdot <var:l> $ , additionally $ <var:K> \cdot <nat:438> $ are guaranteed to be even because multiplying of an even element of $ \mathbb{Z}^+ $ with an arbitrary positive integer is by its very nature an even natural number . Notice the product $ <var:a> \cdot <nat:386> $ is clearly even . This fact holds because taking the product between an even number in $ \mathbb{N} $ and an arbitrary element of $ \mathbb{Z}^+ $ is guaranteed to be even . In addition , $ <var:O> \cdot <nat:72> $ is guaranteed to be an even element of $ \mathbb{N} $ . In addition to this , $ <nat:474> \cdot <var:D> $ is trivially an even natural number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:D> <var:l> <var:a> <var:O> <var:e> <var:K> : nat , Nat . even ( <nat:474> * <var:D> + <nat:448> * <var:l> + <nat:386> * <var:a> + <nat:72> * <var:O> + <nat:34> * <var:e> + <nat:438> * <var:K> + <nat:96> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:96> = true ) . { auto . } repeat rewrite Nat . even_add . assert ( <genH> : Nat . even ( <nat:34> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:448> * <var:l> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:438> * <var:K> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:386> * <var:a> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:72> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:474> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 