\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For some unknowns $ <var:l> $ , $ <var:P> $ , $ <var:q> $ , $ <var:a> $ , $ <var:I> $ , $ <var:u> $ , $ <var:V> $ , and $ <var:S> $ $ <var:l> * <nat:126> + <var:S> * <nat:488> + <nat:58> + <var:u> * <nat:156> + <nat:70> * <var:V> + <nat:342> * <var:P> + <nat:34> * <var:q> + <nat:208> * <var:I> + <nat:104> * <var:a> $ must be even . \end{theorem} \begin{proof} Observe the coefficients $ <nat:208> $ , $ <nat:34> $ , plus $ <nat:156> $ are known as even numbers in $ \mathbb{Z}^+ $ . Consequently , the pairs $ <var:I> \times <nat:208> $ , $ <var:q> \times <nat:34> $ , and likewise $ <nat:156> \times <var:u> $ must be even at the same time . In addition to this , recall that the terms $ <var:l> \times <nat:126> $ , $ <var:a> \times <nat:104> $ , and also $ <nat:342> \times <var:P> $ are known to be even by reason that taking the product between an even whole number and any whole number is guaranteed to be an even integer . This fact holds by reason that the positive integers $ <nat:126> $ , $ <nat:104> $ , and also $ <nat:342> $ are trivially even elements of $ \mathbb{N} $ . Likewise , the coefficient $ <nat:488> $ is clearly even . Consequently , the term $ <nat:488> \times <var:S> $ is guaranteed to be even in addition . Likewise , remember that the coefficient $ <nat:70> $ is known as even . Therefore , the product $ <var:V> \times <nat:70> $ is guaranteed to be even additionally . We prove the expression $ <var:q> * <nat:34> + <var:I> * <nat:208> + <nat:70> * <var:V> + <nat:488> * <var:S> + <var:u> * <nat:156> + <nat:126> * <var:l> + <nat:104> * <var:a> + <nat:342> * <var:P> + <nat:58> $ will be even . Recall that the addition between an even element of $ \mathbb{N} $ with an even number in $ \mathbb{N} $ must be in itself an even natural number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:l> <var:P> <var:q> <var:a> <var:I> <var:u> <var:V> <var:S> : nat , Nat . even ( <nat:126> * <var:l> + <nat:488> * <var:S> + <nat:156> * <var:u> + <nat:70> * <var:V> + <nat:342> * <var:P> + <nat:34> * <var:q> + <nat:208> * <var:I> + <nat:104> * <var:a> + <nat:58> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:208> = true ) . { auto . } assert ( <genH> : Nat . even <nat:34> = true ) . { auto . } assert ( <genH> : Nat . even <nat:156> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:208> * <var:I> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:34> * <var:q> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:156> * <var:u> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:126> = true ) . { auto . } assert ( <genH> : Nat . even <nat:104> = true ) . { auto . } assert ( <genH> : Nat . even <nat:342> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:126> * <var:l> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:104> * <var:a> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:P> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:488> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:488> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:70> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:70> * <var:V> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 