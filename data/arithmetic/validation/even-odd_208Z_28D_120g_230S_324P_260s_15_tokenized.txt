\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For every variables $ <var:P> $ , $ <var:D> $ , $ <var:g> $ , $ <var:s> $ , $ <var:Z> $ , and $ <var:S> $ the formula $ <nat:230> * <var:S> + <nat:15> + <nat:120> * <var:g> + <var:Z> * <nat:208> + <var:D> * <nat:28> + <nat:324> * <var:P> + <var:s> * <nat:260> $ must be odd . \end{theorem} \begin{proof} Our proposition must be true by reason that $ <var:Z> * <nat:208> + <var:D> * <nat:28> + <var:g> * <nat:120> + <nat:230> * <var:S> + <var:P> * <nat:324> + <var:s> * <nat:260> $ is trivially even . Notice that the term $ <var:s> <nat:260> $ is known to be even . Observe the pairs $ <nat:120> <var:g> $ , $ <var:P> <nat:324> $ , as well as $ <nat:208> <var:Z> $ will be even integers by reason that the product between an even natural and an arbitrary integer must be even . In addition , recall the pair $ <var:S> <nat:230> $ is known to be an even element of $ \mathbb{Z}^+ $ . Further , take note that $ <nat:28> <var:D> $ is known as even . We check the result holds . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:P> <var:D> <var:g> <var:s> <var:Z> <var:S> : nat , Nat . odd ( <nat:230> * <var:S> + <nat:120> * <var:g> + <nat:208> * <var:Z> + <nat:28> * <var:D> + <nat:324> * <var:P> + <nat:260> * <var:s> + <nat:15> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:260> * <var:s> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:120> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:P> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:208> * <var:Z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:230> * <var:S> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:28> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:230> * <var:S> + <nat:120> * <var:g> + <nat:208> * <var:Z> + <nat:28> * <var:D> + <nat:324> * <var:P> + <nat:260> * <var:s> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 