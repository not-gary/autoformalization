\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:u> \times <nat:40> + <nat:84> \times <var:s> + <nat:52> + <nat:210> \times <var:n> + <var:Z> \times <nat:446> + <nat:134> \times <var:x> + <var:R> \times <nat:310> $ is even taking unknowns $ <var:n> $ , $ <var:s> $ , $ <var:u> $ , $ <var:R> $ , $ <var:x> $ , and $ <var:Z> $ . \end{theorem} \begin{proof} Remember the sum $ <var:R> \times <nat:310> + <nat:446> \times <var:Z> + <nat:52> + <nat:40> \times <var:u> + <var:n> \times <nat:210> + <nat:84> \times <var:s> + <nat:134> \times <var:x> $ is even by reason that $ <nat:134> <var:x> + <nat:84> <var:s> + <nat:40> <var:u> + <nat:446> <var:Z> + <var:n> <nat:210> + <var:R> <nat:310> $ is trivially even . The terms $ <var:Z> * <nat:446> $ additionally $ <var:s> * <nat:84> $ must be even . This fact is proven from the fact the multiplication of an even integer and an arbitrary number is even . Further , recall that the pair $ <var:n> * <nat:210> $ is known to be even . This fact is proven since taking the product between an even number in $ \mathbb{Z}^+ $ with any whole number is even . Building on , the product $ <nat:134> * <var:x> $ is clearly an even element of $ \mathbb{N} $ . What's more , $ <nat:310> * <var:R> $ is trivially an even integer . Additionally , remember the pair $ <var:u> * <nat:40> $ is even as a consequence that taking the product between an even element of $ \mathbb{N} $ and an arbitrary number in $ \mathbb{Z}^+ $ will be even . From the knowledge taking the product between even naturals with some number in $ \mathbb{N} $ is even the formula $ <var:n> * <nat:210> + <nat:40> * <var:u> + <nat:84> * <var:s> + <var:Z> * <nat:446> + <var:x> * <nat:134> + <nat:310> * <var:R> $ must be obviously even . It can be verified that that $ <var:s> * <nat:84> + <var:R> * <nat:310> + <nat:134> * <var:x> + <var:u> * <nat:40> + <nat:52> + <nat:210> * <var:n> + <var:Z> * <nat:446> $ is guaranteed to be even . Take note the adding of even elements of $ \mathbb{Z}^+ $ collectively and an even integer is even . Notice that $ <nat:52> $ is guaranteed to be an even positive integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:n> <var:s> <var:u> <var:R> <var:x> <var:Z> : nat , Nat . even ( <nat:40> * <var:u> + <nat:84> * <var:s> + <nat:210> * <var:n> + <nat:446> * <var:Z> + <nat:134> * <var:x> + <nat:310> * <var:R> + <nat:52> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:446> * <var:Z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:84> * <var:s> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:210> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:134> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:310> * <var:R> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:u> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:u> + <nat:84> * <var:s> + <nat:210> * <var:n> + <nat:446> * <var:Z> + <nat:134> * <var:x> + <nat:310> * <var:R> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:52> = true ) . { auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 