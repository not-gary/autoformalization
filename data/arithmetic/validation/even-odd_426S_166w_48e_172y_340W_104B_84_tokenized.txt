\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:48> * <var:e> + <var:w> * <nat:166> + <nat:172> * <var:y> + <var:W> * <nat:340> + <var:S> * <nat:426> + <nat:84> + <var:B> * <nat:104> $ is even taking every whole numbers $ <var:y> $ , $ <var:B> $ , $ <var:S> $ , $ <var:w> $ , $ <var:e> $ , and $ <var:W> $ . \end{theorem} \begin{proof} Notice that the expression $ <var:B> * <nat:104> + <var:e> * <nat:48> + <nat:340> * <var:W> + <var:y> * <nat:172> + <var:w> * <nat:166> + <nat:84> + <var:S> * <nat:426> $ is known as even by reason that the expression $ <var:S> \times <nat:426> + <var:w> \times <nat:166> + <nat:48> \times <var:e> + <nat:104> \times <var:B> + <var:y> \times <nat:172> + <nat:340> \times <var:W> $ is trivially even . Observe the leading terms $ <nat:104> $ and also $ <nat:48> $ are obviously even elements of $ \mathbb{N} $ Recall that the natural numbers $ <nat:172> $ in addition to $ <nat:340> $ are even We know that the number $ <nat:166> $ is known to be even Furthermore , see the leading term $ <nat:426> $ is guaranteed to be even The summation $ <nat:426> * <var:S> + <nat:166> * <var:w> + <var:e> * <nat:48> + <nat:172> * <var:y> + <var:W> * <nat:340> + <var:B> * <nat:104> $ is guaranteed to be by itself even from the fact the summing between an even natural with even natural numbers is fundamentally an even integer . The addition of an even number in $ \mathbb{N} $ with an even natural number will be an even positive integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:y> <var:B> <var:S> <var:w> <var:e> <var:W> : nat , Nat . even ( <nat:48> * <var:e> + <nat:166> * <var:w> + <nat:172> * <var:y> + <nat:340> * <var:W> + <nat:426> * <var:S> + <nat:104> * <var:B> + <nat:84> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:104> = true ) . { auto . } assert ( <genH> : Nat . even <nat:48> = true ) . { auto . } assert ( <genH> : Nat . even <nat:172> = true ) . { auto . } assert ( <genH> : Nat . even <nat:340> = true ) . { auto . } assert ( <genH> : Nat . even <nat:166> = true ) . { auto . } assert ( <genH> : Nat . even <nat:426> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:48> * <var:e> + <nat:166> * <var:w> + <nat:172> * <var:y> + <nat:340> * <var:W> + <nat:426> * <var:S> + <nat:104> * <var:B> ) = true ) . { repeat rewrite Nat . even_add . repeat rewrite Nat . even_mul . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 