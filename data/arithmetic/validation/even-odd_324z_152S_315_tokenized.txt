\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:152> * <var:S> + <nat:315> + <var:z> * <nat:324> $ is odd assuming every positive numbers $ <var:z> $ , and $ <var:S> $ . \end{theorem} \begin{proof} Take note that the addition between even numbers in $ \mathbb{N} $ concurrently and an odd whole number is guaranteed to be by its very nature an odd natural . Take note that $ <nat:315> $ is known as an odd integer . We use the assumption $ <var:z> \cdot <nat:324> + <var:S> \cdot <nat:152> + <nat:315> $ is trivially odd from the fact the sum $ <var:z> \cdot <nat:324> + <var:S> \cdot <nat:152> $ is known to be even . The pair $ <var:z> <nat:324> $ is known as an even element of $ \mathbb{Z}^+ $ . In addition , recall that $ <nat:152> <var:S> $ is guaranteed to be even since multiplying of an even element of $ \mathbb{Z}^+ $ with an arbitrary number in $ \mathbb{Z}^+ $ will be even . The summation $ <nat:324> \times <var:z> + <var:S> \times <nat:152> $ will be even from the knowledge the addition of an even positive integer and even whole numbers is guaranteed to be an even integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:z> <var:S> : nat , Nat . odd ( <nat:152> * <var:S> + <nat:324> * <var:z> + <nat:315> ) = true . Proof . intros . rewrite Nat . odd_add . assert ( <genH> : Nat . odd <nat:315> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:152> * <var:S> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:152> * <var:S> + <nat:324> * <var:z> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 