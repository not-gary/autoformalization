\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:140> <var:a> + <var:r> <nat:196> + <nat:158> + <nat:452> <var:y> $ must be even . \end{theorem} \begin{proof} Notice $ <nat:140> * <var:a> + <var:r> * <nat:196> + <nat:158> + <nat:452> * <var:y> $ is obviously even since the sum $ <var:r> \times <nat:196> + <nat:452> \times <var:y> + <nat:140> \times <var:a> $ is known as even . Recall that the coefficient $ <nat:196> $ is trivially an even integer . In consequence , the pair $ <nat:196> * <var:r> $ will be even also . Adding on , the pair $ <var:y> * <nat:452> $ is trivially an even number in $ \mathbb{N} $ as a consequence that the whole number $ <nat:452> $ is clearly by its very nature an even positive integer . Recall the product $ <var:a> * <nat:140> $ is clearly even . This claim holds since the multiplication of an even positive integer and an arbitrary number in $ \mathbb{Z}^+ $ is an even positive integer , which must be true from the fact the number $ <nat:140> $ is known as by its very nature an even number in $ \mathbb{Z}^+ $ . The expression $ <nat:452> \cdot <var:y> + <nat:140> \cdot <var:a> + <nat:196> \cdot <var:r> $ is guaranteed to be obviously even since the totaling of an even number in $ \mathbb{N} $ and even whole numbers is an even positive integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:a> <var:r> <var:y> : nat , Nat . even ( <nat:140> * <var:a> + <nat:196> * <var:r> + <nat:452> * <var:y> + <nat:158> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:196> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:196> * <var:r> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:452> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:452> * <var:y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:140> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:a> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:a> + <nat:196> * <var:r> + <nat:452> * <var:y> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 