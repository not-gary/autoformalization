\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:196> * <var:y> + <nat:494> * <var:b> + <nat:438> * <var:Y> + <var:X> * <nat:336> + <nat:342> * <var:E> + <nat:50> + <nat:192> * <var:D> + <nat:256> * <var:c> $ will be even assuming every natural terms $ <var:y> $ , $ <var:b> $ , $ <var:Y> $ , $ <var:c> $ , $ <var:X> $ , $ <var:E> $ , and $ <var:D> $ . \end{theorem} \begin{proof} The adding of even elements of $ \mathbb{N} $ and an even integer must be an even number . The formula $ <nat:342> <var:E> + <nat:336> <var:X> + <nat:196> <var:y> + <nat:50> + <nat:256> <var:c> + <var:Y> <nat:438> + <nat:494> <var:b> + <var:D> <nat:192> $ is trivially even from the fact the formula $ <nat:192> \times <var:D> + <nat:336> \times <var:X> + <nat:256> \times <var:c> + <var:E> \times <nat:342> + <nat:196> \times <var:y> + <var:b> \times <nat:494> + <nat:438> \times <var:Y> $ is guaranteed to be even . We know that the terms $ <nat:192> \times <var:D> $ , $ <var:X> \times <nat:336> $ , $ <nat:256> \times <var:c> $ , $ <var:E> \times <nat:342> $ , $ <nat:196> \times <var:y> $ , $ <nat:494> \times <var:b> $ , and moreover $ <nat:438> \times <var:Y> $ are clearly even elements of $ \mathbb{N} $ . This fact holds from the knowledge the coefficients $ <nat:192> $ , $ <nat:336> $ , $ <nat:256> $ , $ <nat:342> $ , $ <nat:196> $ , $ <nat:494> $ , and likewise $ <nat:438> $ are even . The sum $ <var:D> \cdot <nat:192> + <nat:342> \cdot <var:E> + <nat:256> \cdot <var:c> + <var:y> \cdot <nat:196> + <nat:336> \cdot <var:X> + <nat:438> \cdot <var:Y> + <var:b> \cdot <nat:494> $ is guaranteed to be even by reason that the adding between an even natural with even elements of $ \mathbb{Z}^+ $ is fundamentally an even number plus as a consequence that taking the product between an even element of $ \mathbb{Z}^+ $ with a whole number is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:y> <var:b> <var:Y> <var:c> <var:X> <var:E> <var:D> : nat , Nat . even ( <nat:196> * <var:y> + <nat:494> * <var:b> + <nat:438> * <var:Y> + <nat:336> * <var:X> + <nat:342> * <var:E> + <nat:192> * <var:D> + <nat:256> * <var:c> + <nat:50> ) = true . Proof . intros . rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:192> = true ) . { auto . } assert ( <genH> : Nat . even <nat:336> = true ) . { auto . } assert ( <genH> : Nat . even <nat:256> = true ) . { auto . } assert ( <genH> : Nat . even <nat:342> = true ) . { auto . } assert ( <genH> : Nat . even <nat:196> = true ) . { auto . } assert ( <genH> : Nat . even <nat:494> = true ) . { auto . } assert ( <genH> : Nat . even <nat:438> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:192> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:336> * <var:X> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:c> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:E> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:196> * <var:y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:494> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:438> * <var:Y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:196> * <var:y> + <nat:494> * <var:b> + <nat:438> * <var:Y> + <nat:336> * <var:X> + <nat:342> * <var:E> + <nat:192> * <var:D> + <nat:256> * <var:c> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . auto . Qed . 