\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:2> <var:T> + <var:A> <nat:32> + <nat:454> <var:S> + <nat:300> <var:v> + <nat:473> $ will be odd using any positive integers $ <var:T> $ , $ <var:S> $ , $ <var:v> $ , and $ <var:A> $ . \end{theorem} \begin{proof} We know the addition between even naturals with an odd natural must be fundamentally an odd whole number . Notice $ <nat:473> $ is trivially odd . We use the assumption the understanding that the sum $ <var:S> * <nat:454> + <var:T> * <nat:2> + <var:A> * <nat:32> + <nat:300> * <var:v> $ is clearly even . Notice the pairs $ <nat:2> <var:T> $ plus $ <nat:32> <var:A> $ are known to be even . This fact is proven by reason that the multiplication of an even positive integer and any element of $ \mathbb{N} $ is even . Notice that $ <var:v> <nat:300> $ is obviously even . Additionally , we know the term $ <var:S> <nat:454> $ is guaranteed to be even . Since the totaling of an even natural number and even natural numbers is guaranteed to be an even integer , the sum $ <var:S> \times <nat:454> + <var:T> \times <nat:2> + <nat:300> \times <var:v> + <var:A> \times <nat:32> $ is guaranteed to be clearly even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:T> <var:S> <var:v> <var:A> : nat , Nat . odd ( <nat:2> * <var:T> + <nat:32> * <var:A> + <nat:454> * <var:S> + <nat:300> * <var:v> + <nat:473> ) = true . Proof . intros . rewrite Nat . odd_add . assert ( <genH> : Nat . odd <nat:473> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:2> * <var:T> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:32> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:300> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:454> * <var:S> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:2> * <var:T> + <nat:32> * <var:A> + <nat:454> * <var:S> + <nat:300> * <var:v> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 