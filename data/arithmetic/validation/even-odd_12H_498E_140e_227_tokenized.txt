\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With some $ <var:e> $ , $ <var:H> $ , and $ <var:E> \in \mathbb{N} $ $ <nat:498> \times <var:E> + <nat:227> + <nat:140> \times <var:e> + <nat:12> \times <var:H> $ must be odd . \end{theorem} \begin{proof} We apply the assumption the understanding that $ <nat:12> <var:H> + <nat:498> <var:E> + <nat:140> <var:e> $ is obviously even . Take note the summation between even integers and an even natural number is guaranteed to be even . Notice the product $ <nat:498> \times <var:E> $ is guaranteed to be even . Furthermore , take note that the pair $ <var:e> \times <nat:140> $ is obviously an even integer . This holds from the knowledge the product between an even number with an arbitrary natural is guaranteed to be an even number in $ \mathbb{N} $ . We know the pair $ <var:H> \times <nat:12> $ is guaranteed to be an even positive integer . in addition to by reason that the product of even numbers with any element of $ \mathbb{N} $ is even this proposition holds . The addition between an even element of $ \mathbb{Z}^+ $ with an odd integer must be by itself an odd number . Remember $ <nat:227> $ is odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:e> <var:H> <var:E> : nat , Nat . odd ( <nat:498> * <var:E> + <nat:140> * <var:e> + <nat:12> * <var:H> + <nat:227> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:498> * <var:E> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:12> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:498> * <var:E> + <nat:140> * <var:e> + <nat:12> * <var:H> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . assert ( <genH> : Nat . odd <nat:227> = true ) . { auto . } rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 