\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:232> + <var:S> \times <nat:204> + <nat:302> \times <var:D> + <var:d> \times <nat:70> $ is even . \end{theorem} \begin{proof} The proposition must be true from the knowledge $ <nat:70> \times <var:d> + <nat:204> \times <var:S> + <nat:302> \times <var:D> $ is known as even . $ <var:d> \cdot <nat:70> $ is clearly even . This claim is proven because the positive integer $ <nat:70> $ is guaranteed to be an even positive integer . The natural number $ <nat:302> $ is an even natural . For this reason , the pair $ <var:D> \cdot <nat:302> $ will be even additionally . Notice the product $ <var:S> \cdot <nat:204> $ is even . This fact is proven because the product between an even positive integer with some whole number is guaranteed to be an even element of $ \mathbb{Z}^+ $ , which holds from the knowledge the whole number $ <nat:204> $ is guaranteed to be an even number in $ \mathbb{N} $ . The summation $ <nat:204> * <var:S> + <nat:70> * <var:d> + <var:D> * <nat:302> $ is even from the knowledge taking the product between an even integer with a number in $ \mathbb{Z}^+ $ is even . We know $ <nat:232> $ is guaranteed to be even . It can be proven that this main result must be true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:S> <var:D> <var:d> : nat , Nat . even ( <nat:204> * <var:S> + <nat:302> * <var:D> + <nat:70> * <var:d> + <nat:232> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:70> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:70> * <var:d> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:302> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:302> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:204> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:204> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:204> * <var:S> + <nat:302> * <var:D> + <nat:70> * <var:d> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:232> = true ) . { auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 