\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:368> * <var:N> + <nat:476> * <var:o> + <nat:128> * <var:A> + <nat:315> + <nat:254> * <var:J> + <nat:296> * <var:z> + <var:k> * <nat:314> $ is odd given terms $ <var:k> $ , $ <var:A> $ , $ <var:z> $ , $ <var:J> $ , $ <var:o> $ , and $ <var:N> $ . \end{theorem} \begin{proof} Take note that the formula $ <var:z> \cdot <nat:296> + <var:N> \cdot <nat:368> + <nat:128> \cdot <var:A> + <var:k> \cdot <nat:314> + <var:o> \cdot <nat:476> + <var:J> \cdot <nat:254> $ is obviously even . The terms $ <nat:296> <var:z> $ , $ <nat:254> <var:J> $ , plus $ <nat:476> <var:o> $ must be even . This holds from the fact the multiplication of an even integer with a element of $ \mathbb{Z}^+ $ will be instrinsically an even integer . Observe the pairs $ <var:k> <nat:314> $ as well as $ <var:N> <nat:368> $ will be even . $ <var:A> <nat:128> $ is guaranteed to be even since the multiplication between an even element of $ \mathbb{N} $ with a number in $ \mathbb{Z}^+ $ is guaranteed to be even . From the fact the addition between an even positive integer and an even number in $ \mathbb{Z}^+ $ is guaranteed to be by definition an even number , and from the fact the multiplication between even natural numbers and a element of $ \mathbb{N} $ will be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:k> <var:A> <var:z> <var:J> <var:o> <var:N> : nat , Nat . odd ( <nat:368> * <var:N> + <nat:476> * <var:o> + <nat:128> * <var:A> + <nat:254> * <var:J> + <nat:296> * <var:z> + <nat:314> * <var:k> + <nat:315> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:296> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:254> * <var:J> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:476> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:314> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:368> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:128> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:368> * <var:N> + <nat:476> * <var:o> + <nat:128> * <var:A> + <nat:254> * <var:J> + <nat:296> * <var:z> + <nat:314> * <var:k> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 