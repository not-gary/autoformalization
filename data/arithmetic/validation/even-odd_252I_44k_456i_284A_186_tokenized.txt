\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:456> \times <var:i> + <var:k> \times <nat:44> + <var:I> \times <nat:252> + <nat:284> \times <var:A> + <nat:186> $ is guaranteed to be even . \end{theorem} \begin{proof} Observe that $ <nat:186> $ is clearly an even element of $ \mathbb{Z}^+ $ . It can be proven that $ <nat:44> \times <var:k> + <var:I> \times <nat:252> + <var:A> \times <nat:284> + <nat:186> + <nat:456> \times <var:i> $ will be even . We apply the expression $ <var:I> * <nat:252> + <nat:44> * <var:k> + <nat:456> * <var:i> + <var:A> * <nat:284> $ is clearly even . The products $ <var:k> \cdot <nat:44> $ as well as $ <var:A> \cdot <nat:284> $ are trivially even because taking the product between an even number with an arbitrary integer is guaranteed to be even . Notice the pair $ <nat:456> \cdot <var:i> $ is obviously even . This claim is proven from the knowledge the multiplication of an even element of $ \mathbb{Z}^+ $ with an arbitrary natural is guaranteed to be even . We know the term $ <var:I> \cdot <nat:252> $ is guaranteed to be even . This claim is proven because the multiplication between an even number in $ \mathbb{Z}^+ $ and an arbitrary element of $ \mathbb{Z}^+ $ must be even . From the knowledge the summation of even numbers in $ \mathbb{Z}^+ $ and an even element of $ \mathbb{N} $ will be instrinsically an even whole number , additionally as a consequence that the multiplication of an even positive integer with an arbitrary natural will be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:i> <var:k> <var:I> <var:A> : nat , Nat . even ( <nat:456> * <var:i> + <nat:44> * <var:k> + <nat:252> * <var:I> + <nat:284> * <var:A> + <nat:186> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:186> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:44> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:284> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:456> * <var:i> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:252> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:456> * <var:i> + <nat:44> * <var:k> + <nat:252> * <var:I> + <nat:284> * <var:A> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 