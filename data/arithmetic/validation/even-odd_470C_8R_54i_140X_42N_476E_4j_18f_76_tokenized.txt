\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:E> \times <nat:476> + <var:N> \times <nat:42> + <nat:470> \times <var:C> + <nat:4> \times <var:j> + <nat:8> \times <var:R> + <var:i> \times <nat:54> + <var:f> \times <nat:18> + <nat:76> + <var:X> \times <nat:140> $ is even . \end{theorem} \begin{proof} We check that the expression $ <var:R> \cdot <nat:8> + <nat:4> \cdot <var:j> + <var:C> \cdot <nat:470> + <var:N> \cdot <nat:42> + <nat:76> + <var:i> \cdot <nat:54> + <var:f> \cdot <nat:18> + <var:X> \cdot <nat:140> + <var:E> \cdot <nat:476> $ is clearly even since $ <nat:470> \times <var:C> + <nat:8> \times <var:R> + <nat:54> \times <var:i> + <nat:140> \times <var:X> + <nat:42> \times <var:N> + <var:E> \times <nat:476> + <nat:4> \times <var:j> + <var:f> \times <nat:18> $ is guaranteed to be even . The pairs $ <nat:8> <var:R> $ , $ <nat:4> <var:j> $ , $ <nat:476> <var:E> $ , $ <nat:42> <var:N> $ , and also $ <var:X> <nat:140> $ are obviously even naturals from the knowledge the multiplication of an even natural number and an arbitrary number in $ \mathbb{Z}^+ $ is guaranteed to be even . The pair $ <var:i> <nat:54> $ is obviously even . Building on , we know that $ <nat:18> <var:f> $ is known as an even number in $ \mathbb{Z}^+ $ . What's more , we know that the pair $ <var:C> <nat:470> $ is known to be an even number in $ \mathbb{N} $ as a consequence that multiplying between an even number in $ \mathbb{N} $ with any integer is by itself an even element of $ \mathbb{N} $ . Because the addition of an even natural and an even whole number must be even , our result is true . We prove the result is proven . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:E> <var:N> <var:C> <var:j> <var:R> <var:i> <var:f> <var:X> : nat , Nat . even ( <nat:476> * <var:E> + <nat:42> * <var:N> + <nat:470> * <var:C> + <nat:4> * <var:j> + <nat:8> * <var:R> + <nat:54> * <var:i> + <nat:18> * <var:f> + <nat:140> * <var:X> + <nat:76> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:8> * <var:R> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:4> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:476> * <var:E> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:42> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:54> * <var:i> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:18> * <var:f> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:470> * <var:C> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:476> * <var:E> + <nat:42> * <var:N> + <nat:470> * <var:C> + <nat:4> * <var:j> + <nat:8> * <var:R> + <nat:54> * <var:i> + <nat:18> * <var:f> + <nat:140> * <var:X> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 