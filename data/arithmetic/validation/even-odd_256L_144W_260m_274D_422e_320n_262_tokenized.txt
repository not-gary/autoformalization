\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For variables $ <var:W> $ , $ <var:D> $ , $ <var:m> $ , $ <var:e> $ , $ <var:n> $ , and $ <var:L> $ the summation $ <var:W> * <nat:144> + <var:m> * <nat:260> + <nat:262> + <nat:422> * <var:e> + <nat:256> * <var:L> + <nat:320> * <var:n> + <var:D> * <nat:274> $ is guaranteed to be even . \end{theorem} \begin{proof} It can be proven that $ <nat:262> + <nat:260> \cdot <var:m> + <var:W> \cdot <nat:144> + <var:L> \cdot <nat:256> + <var:n> \cdot <nat:320> + <nat:422> \cdot <var:e> + <nat:274> \cdot <var:D> $ is guaranteed to be even . Recall $ <nat:262> $ is clearly an even natural . Take note that the expression $ <var:m> <nat:260> + <nat:274> <var:D> + <nat:144> <var:W> + <nat:262> + <var:n> <nat:320> + <nat:256> <var:L> + <nat:422> <var:e> $ is clearly even from the knowledge the expression $ <var:W> <nat:144> + <var:n> <nat:320> + <nat:256> <var:L> + <nat:260> <var:m> + <nat:274> <var:D> + <nat:422> <var:e> $ is obviously even . We know the products $ <var:n> * <nat:320> $ , $ <var:W> * <nat:144> $ , $ <var:m> * <nat:260> $ , $ <nat:274> * <var:D> $ , in addition to $ <nat:422> * <var:e> $ are known as even numbers . This claim is proven from the knowledge taking the product between an even element of $ \mathbb{N} $ with an arbitrary whole number must be even . Further , the product $ <nat:256> * <var:L> $ is trivially even . The sum $ <nat:256> * <var:L> + <var:W> * <nat:144> + <var:m> * <nat:260> + <var:D> * <nat:274> + <var:e> * <nat:422> + <var:n> * <nat:320> $ will be even as a consequence that the summing between an even number in $ \mathbb{N} $ and an even whole number is guaranteed to be an even natural number as well as because the multiplication between even numbers in $ \mathbb{N} $ with a number must be an even natural number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:W> <var:D> <var:m> <var:e> <var:n> <var:L> : nat , Nat . even ( <nat:144> * <var:W> + <nat:260> * <var:m> + <nat:422> * <var:e> + <nat:256> * <var:L> + <nat:320> * <var:n> + <nat:274> * <var:D> + <nat:262> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:262> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:320> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:144> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:260> * <var:m> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:274> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:422> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:L> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:144> * <var:W> + <nat:260> * <var:m> + <nat:422> * <var:e> + <nat:256> * <var:L> + <nat:320> * <var:n> + <nat:274> * <var:D> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 