\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For any integers $ <var:F> $ , $ <var:B> $ , $ <var:D> $ , $ <var:V> $ , $ <var:Y> $ , $ <var:X> $ , $ <var:G> $ , and $ <var:o> $ $ <nat:345> + <nat:368> \times <var:G> + <var:X> \times <nat:350> + <var:F> \times <nat:238> + <var:B> \times <nat:144> + <nat:52> \times <var:o> + <var:V> \times <nat:210> + <nat:190> \times <var:D> + <nat:18> \times <var:Y> $ is odd . \end{theorem} \begin{proof} It can be demonstrated that the sum $ <nat:238> \cdot <var:F> + <nat:190> \cdot <var:D> + <nat:368> \cdot <var:G> + <nat:210> \cdot <var:V> + <var:B> \cdot <nat:144> + <var:Y> \cdot <nat:18> + <var:X> \cdot <nat:350> + <nat:52> \cdot <var:o> $ is obviously even . We know that the summation of even naturals concurrently with an even number in $ \mathbb{Z}^+ $ is an even whole number . We know the products $ <var:V> \times <nat:210> $ and moreover $ <var:X> \times <nat:350> $ are guaranteed to be even since the product between an even positive integer and a element of $ \mathbb{N} $ will be an even positive integer . Notice that the pairs $ <var:o> \times <nat:52> $ as well as $ <nat:238> \times <var:F> $ are obviously even elements of $ \mathbb{Z}^+ $ . This is proven from the fact the product of an even element of $ \mathbb{N} $ with some number in $ \mathbb{Z}^+ $ will be even . Furthermore , we know that the products $ <var:G> \times <nat:368> $ , $ <nat:190> \times <var:D> $ , and moreover $ <nat:18> \times <var:Y> $ are guaranteed to be even . Further , $ <nat:144> \times <var:B> $ is trivially even . additionally since the multiplication of an even integer with a number in $ \mathbb{N} $ is guaranteed to be obviously an even whole number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:F> <var:B> <var:D> <var:V> <var:Y> <var:X> <var:G> <var:o> : nat , Nat . odd ( <nat:368> * <var:G> + <nat:350> * <var:X> + <nat:238> * <var:F> + <nat:144> * <var:B> + <nat:52> * <var:o> + <nat:210> * <var:V> + <nat:190> * <var:D> + <nat:18> * <var:Y> + <nat:345> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:210> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:350> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:52> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:238> * <var:F> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:368> * <var:G> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:190> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:18> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:144> * <var:B> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:368> * <var:G> + <nat:350> * <var:X> + <nat:238> * <var:F> + <nat:144> * <var:B> + <nat:52> * <var:o> + <nat:210> * <var:V> + <nat:190> * <var:D> + <nat:18> * <var:Y> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 