\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:c> \cdot <nat:426> + <var:o> \cdot <nat:358> + <var:e> \cdot <nat:40> + <var:r> \cdot <nat:78> + <var:P> \cdot <nat:318> + <var:N> \cdot <nat:380> + <nat:140> \cdot <var:Y> + <nat:135> + <var:H> \cdot <nat:46> $ must be odd . \end{theorem} \begin{proof} Remember $ <nat:135> $ is known to be an odd natural . It can be checked that our proposition holds . We apply the fact the summation $ <var:H> \cdot <nat:46> + <nat:318> \cdot <var:P> + <nat:135> + <nat:426> \cdot <var:c> + <var:Y> \cdot <nat:140> + <var:N> \cdot <nat:380> + <nat:78> \cdot <var:r> + <nat:358> \cdot <var:o> + <var:e> \cdot <nat:40> $ is guaranteed to be odd as a consequence that the expression $ <var:P> \cdot <nat:318> + <var:N> \cdot <nat:380> + <var:H> \cdot <nat:46> + <nat:78> \cdot <var:r> + <var:Y> \cdot <nat:140> + <var:e> \cdot <nat:40> + <nat:358> \cdot <var:o> + <nat:426> \cdot <var:c> $ is clearly even . Remember that the products $ <var:H> <nat:46> $ , $ <var:Y> <nat:140> $ , $ <var:e> <nat:40> $ , $ <var:o> <nat:358> $ , $ <nat:318> <var:P> $ , and also $ <nat:380> <var:N> $ are known to be even integers . Further , remember $ <nat:426> <var:c> $ is known to be even . This holds as a consequence that taking the product between an even number in $ \mathbb{N} $ and any natural must be an even number in $ \mathbb{Z}^+ $ . The term $ <var:r> <nat:78> $ is guaranteed to be an even positive integer . This main result is true because the product between even numbers in $ \mathbb{N} $ with any number is guaranteed to be itself an even positive integer . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:c> <var:o> <var:e> <var:r> <var:P> <var:N> <var:Y> <var:H> : nat , Nat . odd ( <nat:426> * <var:c> + <nat:358> * <var:o> + <nat:40> * <var:e> + <nat:78> * <var:r> + <nat:318> * <var:P> + <nat:380> * <var:N> + <nat:140> * <var:Y> + <nat:46> * <var:H> + <nat:135> ) = true . Proof . intros . assert ( <genH> : Nat . odd <nat:135> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:46> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:140> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:358> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:318> * <var:P> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:380> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:426> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:78> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:426> * <var:c> + <nat:358> * <var:o> + <nat:40> * <var:e> + <nat:78> * <var:r> + <nat:318> * <var:P> + <nat:380> * <var:N> + <nat:140> * <var:Y> + <nat:46> * <var:H> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 