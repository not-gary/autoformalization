\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For all positive numbers $ <var:r> $ , $ <var:t> $ , and $ <var:k> $ the summation $ <nat:458> * <var:r> + <var:k> * <nat:302> + <nat:451> + <nat:114> * <var:t> $ is odd . \end{theorem} \begin{proof} We know the adding of even integers successively and an odd integer is guaranteed to be odd . It can be demonstrated that the formula $ <nat:458> <var:r> + <var:k> <nat:302> + <nat:114> <var:t> $ is trivially even . The pairs $ <var:k> * <nat:302> $ and likewise $ <var:r> * <nat:458> $ are guaranteed to be even as a consequence that taking the product between an even integer with an arbitrary element of $ \mathbb{Z}^+ $ will be in itself an even whole number . In addition , the pair $ <nat:114> * <var:t> $ is even . This claim holds from the knowledge the multiplication between an even natural with a natural number is even . From the fact the adding between an even integer and even whole numbers is guaranteed to be even , $ <nat:458> \cdot <var:r> + <nat:302> \cdot <var:k> + <nat:114> \cdot <var:t> $ will be by itself even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:r> <var:t> <var:k> : nat , Nat . odd ( <nat:458> * <var:r> + <nat:302> * <var:k> + <nat:114> * <var:t> + <nat:451> ) = true . Proof . intros . rewrite Nat . odd_add . assert ( <genH> : Nat . even ( <nat:302> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:114> * <var:t> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:r> + <nat:302> * <var:k> + <nat:114> * <var:t> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 