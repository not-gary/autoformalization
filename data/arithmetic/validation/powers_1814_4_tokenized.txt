\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} If and only if we have $ <var:z> ^ <nat:4> = <var:e> $ assuming some number $ <var:e> $ as well as such that $ <var:z> \geq <nat:2> $ with some $ <var:z> \in \mathbb{N} $ , then take as given that $ <var:e> $ is a <def:tesseract> number . Therefore $ <nat:16> = <var:e> $ is a <def:tesseract> positive integer . \end{theorem} \begin{proof} Recall <nat:16> is a <def:tesseract> natural number given it is the repeated multiplication between $ <var:z> $ taken to the exponential <nat:4> greater than or equivalent to <nat:2> . Note that $ <nat:2> ^ <nat:4> = <nat:16> $ . Furthermore observe $ <nat:2> $ is larger than or equal to <nat:2> . Because of this , <nat:16> is a <def:tesseract> whole number is verified . Hence , $ <var:z> ^ <nat:4> = <nat:16> $ is proven . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:tesseract> ( <var:e> : nat ) := exists <var:z> : nat , ( <var:z> ^ <nat:4> = <var:e> ) /\ ( <var:z> >= <nat:2> ) . Theorem <genP:1> : <def:tesseract> <nat:16> . Proof . unfold <def:tesseract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . auto . apply <genH> . Qed . 