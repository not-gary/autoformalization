\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:61> + <var:m> * <nat:80> + <var:y> * <nat:48> + <nat:384> * <var:d> + <nat:460> * <var:h> + <var:S> * <nat:232> + <var:J> * <nat:450> + <nat:210> * <var:e> $ must be odd using any $ <var:d> $ , $ <var:e> $ , $ <var:J> $ , $ <var:S> $ , $ <var:h> $ , $ <var:y> $ , and $ <var:m> \in \mathbb{Z}^+ $ . \end{theorem} \begin{proof} The number $ <nat:384> $ is obviously an even number in $ \mathbb{N} $ . Hence , the product $ <var:d> \times <nat:384> $ is even in addition . Furthermore , the integer $ <nat:48> $ is clearly even . So , the term $ <var:y> \times <nat:48> $ is guaranteed to be even too . Additionally , observe the positive integer $ <nat:210> $ is an even positive integer . So , the product $ <nat:210> \times <var:e> $ is guaranteed to be even at the same time . Notice $ <var:h> \times <nat:460> $ is known as even . This is true as a consequence that the leading term $ <nat:460> $ is even . Further , recall that the coefficient $ <nat:450> $ is guaranteed to be even . Accordingly , the product $ <var:J> \times <nat:450> $ is guaranteed to be even additionally . Adding on , recall that the product $ <nat:232> \times <var:S> $ is known as an even integer . This must be true from the fact the multiplication between an even element of $ \mathbb{Z}^+ $ and some integer will be an even natural number and also the coefficient $ <nat:232> $ is clearly fundamentally an even natural number . Adding on , the product $ <var:m> \times <nat:80> $ is even from the fact taking the product between an even number in $ \mathbb{Z}^+ $ and any element of $ \mathbb{Z}^+ $ is guaranteed to be even plus the leading term $ <nat:80> $ is obviously even . Notice $ <nat:61> $ is obviously odd . It can be verified that $ <var:S> \times <nat:232> + <nat:61> + <var:h> \times <nat:460> + <var:m> \times <nat:80> + <nat:210> \times <var:e> + <nat:450> \times <var:J> + <nat:48> \times <var:y> + <var:d> \times <nat:384> $ is odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:d> <var:e> <var:J> <var:S> <var:h> <var:y> <var:m> : nat , Nat . odd ( <nat:80> * <var:m> + <nat:48> * <var:y> + <nat:384> * <var:d> + <nat:460> * <var:h> + <nat:232> * <var:S> + <nat:450> * <var:J> + <nat:210> * <var:e> + <nat:61> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:384> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:384> * <var:d> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:48> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:48> * <var:y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:210> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:210> * <var:e> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:460> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:460> * <var:h> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:450> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:450> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:232> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:232> * <var:S> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:80> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:80> * <var:m> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . odd <nat:61> = true ) . { auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 