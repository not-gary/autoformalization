\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Define that supposing that $ \exists <var:d> \in \mathbb{Z}^+ $ larger than or equivalent to <nat:2> . Let that some $ <var:Z> $ will be considered as a <def:square> given $ <var:Z> = <var:d> ^ <nat:2> $ . Then the whole number <nat:4> is a <def:square> number . \end{theorem} \begin{proof} Remember <nat:4> is a <def:square> whole number whenever it is the repeated multiplication of natural number $ <var:d> $ exponentiated to the exponential <nat:2> so that $ <var:d> \geq <nat:2> $ . Notice $ <nat:2> \geq <nat:2> $ . What's more notice as well $ <nat:4> = <var:d> ^ <nat:2> $ gives $ <var:d> = <nat:2> $ . This shows <nat:4> is a <def:square> . Thus , <nat:4> is a <def:square> number is validated . Thus , $ <var:d> $ is more than or equal to <nat:2> and <nat:4> is the repeated multiplication between some number $ <var:d> $ taken to the exponential <nat:2> has been validated . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:square> ( <var:Z> : nat ) := exists <var:d> : nat , ( <var:d> >= <nat:2> ) /\ ( <var:Z> = <var:d> ^ <nat:2> ) . Theorem <genP:1> : <def:square> <nat:4> . Proof . unfold <def:square> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } exists <nat:2> . split . apply <genH> . auto . Qed . 