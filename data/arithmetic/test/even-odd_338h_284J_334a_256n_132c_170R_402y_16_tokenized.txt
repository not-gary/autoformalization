\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Assuming all variables $ <var:R> $ , $ <var:y> $ , $ <var:J> $ , $ <var:n> $ , $ <var:h> $ , $ <var:c> $ , and $ <var:a> $ the summation $ <var:h> \times <nat:338> + <var:y> \times <nat:402> + <var:c> \times <nat:132> + <var:J> \times <nat:284> + <nat:16> + <var:R> \times <nat:170> + <nat:334> \times <var:a> + <nat:256> \times <var:n> $ is even . \end{theorem} \begin{proof} See that the adding between even natural numbers will be an even element of $ \mathbb{N} $ . Notice the number $ <nat:338> $ is trivially even . Then , the pair $ <var:h> \times <nat:338> $ must be even also . Remember that the product $ <var:J> \times <nat:284> $ is obviously even . This claim is true because the integer $ <nat:284> $ is obviously an even number . Remember that the coefficient $ <nat:334> $ is clearly an even positive integer . Thus , the pair $ <var:a> \times <nat:334> $ must be even additionally . The natural number $ <nat:256> $ is obviously an even natural number . As a consequence , the product $ <var:n> \times <nat:256> $ will be even as well . See that the integer $ <nat:132> $ is guaranteed to be even . Thus , the product $ <var:c> \times <nat:132> $ is guaranteed to be even in addition . The term $ <nat:170> \times <var:R> $ is even by reason that the integer $ <nat:170> $ is known to be an even element of $ \mathbb{Z}^+ $ . Observe $ <var:y> \times <nat:402> $ is known as even as a consequence that the natural number $ <nat:402> $ is in itself an even whole number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:R> <var:y> <var:J> <var:n> <var:h> <var:c> <var:a> : nat , Nat . even ( <nat:338> * <var:h> + <nat:402> * <var:y> + <nat:132> * <var:c> + <nat:284> * <var:J> + <nat:170> * <var:R> + <nat:334> * <var:a> + <nat:256> * <var:n> + <nat:16> ) = true . Proof . intros . repeat rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:338> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:338> * <var:h> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:284> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:284> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:334> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:334> * <var:a> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:256> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:132> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:132> * <var:c> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:170> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:170> * <var:R> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:402> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:402> * <var:y> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 