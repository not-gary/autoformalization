\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} We claim that $ <var:V> $ is a <def:enneract> whole number given $ \exists <var:g> \in \mathbb{Z}^+ $ so that $ <var:V> = <var:g> ^ <nat:9> $ for $ <var:V> \in \mathbb{Z}^+ $ plus so that $ <var:g> \geq <nat:2> $ . So $ <nat:512> = <var:V> $ is a <def:enneract> whole number . \end{theorem} \begin{proof} Take $ <var:g> = <nat:2> $ . We show <nat:512> is the repeated multiplication of some $ <var:g> $ exponentiated to the exponential <nat:9> as well as $ <var:g> $ is larger than or equal to <nat:2> . Observe that $ <nat:2> $ is more than or equivalent to <nat:2> . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:V> : nat ) := exists <var:g> : nat , ( <var:V> = <var:g> ^ <nat:9> ) /\ ( <var:g> >= <nat:2> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . auto . apply <genH> . Qed . 