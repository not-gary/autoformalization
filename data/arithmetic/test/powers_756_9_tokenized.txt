\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \newtheorem{definition}{Definition} \begin{document} \begin{definition} Take that $ <var:N> $ is a <def:enneract> given that for some $ <var:C> \in \mathbb{Z}^+ $ we get $ <var:C> \geq <nat:2> $ and taking some positive integer $ <var:N> $ , $ <var:N> = <var:C> ^ <nat:9> $ . \end{definition} \begin{theorem} <nat:512> is a <def:enneract> number . \end{theorem} \begin{proof} Remember a <def:enneract> positive integer is the repeated multiplication between natural number $ <var:C> $ repeatedly multiplied to the exponential <nat:9> larger than or equal to <nat:2> . Note that $ <var:C> ^ <nat:9> = <nat:512> $ yields $ <var:C> = <nat:2> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:N> : nat ) := exists <var:C> : nat , ( <var:C> >= <nat:2> ) /\ ( <var:N> = <var:C> ^ <nat:9> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . exists <nat:2> . split . lia . auto . Qed . 