\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Using any integers $ <var:Y> $ , $ <var:y> $ , $ <var:k> $ , $ <var:n> $ , and $ <var:X> $ $ <nat:364> \cdot <var:n> + <nat:301> + <var:k> \cdot <nat:72> + <nat:56> \cdot <var:Y> + <var:y> \cdot <nat:424> + <nat:2> \cdot <var:X> $ must be odd . \end{theorem} \begin{proof} The summation $ <nat:2> <var:X> + <nat:424> <var:y> + <nat:364> <var:n> + <var:Y> <nat:56> + <var:k> <nat:72> + <nat:301> $ is known as odd because the formula $ <nat:56> <var:Y> + <var:n> <nat:364> + <nat:72> <var:k> + <nat:2> <var:X> + <nat:424> <var:y> $ is clearly even . Remember that $ <var:X> \times <nat:2> $ is clearly an even positive integer . This holds because multiplying of an even number in $ \mathbb{N} $ and any element of $ \mathbb{Z}^+ $ will be clearly an even positive integer . The pair $ <nat:56> \times <var:Y> $ is guaranteed to be an even whole number as a consequence that the product between an even number in $ \mathbb{Z}^+ $ with any element of $ \mathbb{Z}^+ $ is instrinsically an even number in $ \mathbb{Z}^+ $ . Building on , the product $ <var:n> \times <nat:364> $ is trivially an even number as a consequence that the multiplication between an even element of $ \mathbb{N} $ with some number in $ \mathbb{N} $ is even . On top of this , the pair $ <nat:424> \times <var:y> $ is known as even from the knowledge the multiplication between an even element of $ \mathbb{N} $ and some element of $ \mathbb{Z}^+ $ is guaranteed to be an even whole number . In addition , we know that the pair $ <nat:72> \times <var:k> $ is trivially even . As a consequence that the adding of an even whole number and even naturals must be even , the expression $ <var:n> \times <nat:364> + <var:Y> \times <nat:56> + <nat:72> \times <var:k> + <nat:424> \times <var:y> + <var:X> \times <nat:2> $ will be trivially even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Y> <var:y> <var:k> <var:n> <var:X> : nat , Nat . odd ( <nat:364> * <var:n> + <nat:72> * <var:k> + <nat:56> * <var:Y> + <nat:424> * <var:y> + <nat:2> * <var:X> + <nat:301> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:2> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:56> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:424> * <var:y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:72> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:n> + <nat:72> * <var:k> + <nat:56> * <var:Y> + <nat:424> * <var:y> + <nat:2> * <var:X> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 