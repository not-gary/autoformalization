\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:256> \times <var:G> + <nat:14> + <nat:452> \times <var:D> + <nat:2> \times <var:V> + <var:A> \times <nat:28> + <nat:168> \times <var:W> $ must be even given $ <var:G> $ , $ <var:D> $ , $ <var:A> $ , $ <var:V> $ , and $ <var:W> \in \mathbb{N} $ . \end{theorem} \begin{proof} Our main result must be true because $ <nat:256> \times <var:G> + <nat:452> \times <var:D> + <nat:2> \times <var:V> + <nat:168> \times <var:W> + <nat:28> \times <var:A> $ is obviously even . We know that the totaling of an even whole number with an even number in $ \mathbb{Z}^+ $ is guaranteed to be an even number . Notice the pairs $ <nat:168> \times <var:W> $ , $ <nat:2> \times <var:V> $ , $ <var:G> \times <nat:256> $ , and $ <var:A> \times <nat:28> $ are guaranteed to be even . This claim is true from the fact taking the product between an even number in $ \mathbb{Z}^+ $ and some natural number will be even . Take note the product $ <nat:452> \times <var:D> $ is trivially an even element of $ \mathbb{Z}^+ $ . and likewise because multiplying between an even positive integer and a element of $ \mathbb{Z}^+ $ is guaranteed to be even our proposition holds . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:G> <var:D> <var:A> <var:V> <var:W> : nat , Nat . even ( <nat:256> * <var:G> + <nat:452> * <var:D> + <nat:2> * <var:V> + <nat:28> * <var:A> + <nat:168> * <var:W> + <nat:14> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:168> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:2> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:G> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:28> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:452> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:256> * <var:G> + <nat:452> * <var:D> + <nat:2> * <var:V> + <nat:28> * <var:A> + <nat:168> * <var:W> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 