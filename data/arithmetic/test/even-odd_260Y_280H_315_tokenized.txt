\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:260> \times <var:Y> + <var:H> \times <nat:280> + <nat:315> $ must be odd with every whole numbers $ <var:Y> $ , and $ <var:H> $ . \end{theorem} \begin{proof} We demonstrate the expression $ <nat:260> * <var:Y> + <nat:315> + <nat:280> * <var:H> $ is trivially odd since the sum $ <var:Y> \cdot <nat:260> + <nat:280> \cdot <var:H> $ is known to be even . The pair $ <nat:260> * <var:Y> $ is trivially even . This fact must be true from the knowledge the multiplication of an even number in $ \mathbb{N} $ and a natural is guaranteed to be an even natural number . Building on , the pair $ <var:H> * <nat:280> $ is guaranteed to be an even number in $ \mathbb{Z}^+ $ . This holds as a consequence that the multiplication between an even number with some integer is even . Remember that $ <nat:315> $ is clearly odd . We demonstrate the summation $ <nat:315> + <nat:260> <var:Y> + <var:H> <nat:280> $ is guaranteed to be odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Y> <var:H> : nat , Nat . odd ( <nat:260> * <var:Y> + <nat:280> * <var:H> + <nat:315> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:260> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:280> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:260> * <var:Y> + <nat:280> * <var:H> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . odd <nat:315> = true ) . { auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 