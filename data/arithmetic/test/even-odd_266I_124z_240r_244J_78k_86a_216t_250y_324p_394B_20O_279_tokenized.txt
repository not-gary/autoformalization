\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:I> \times <nat:266> + <var:z> \times <nat:124> + <var:r> \times <nat:240> + <nat:279> + <nat:244> \times <var:J> + <var:k> \times <nat:78> + <nat:86> \times <var:a> + <nat:216> \times <var:t> + <nat:250> \times <var:y> + <nat:324> \times <var:p> + <nat:394> \times <var:B> + <nat:20> \times <var:O> $ is odd . \end{theorem} \begin{proof} Notice the products $ <nat:216> \times <var:t> $ as well as $ <var:O> \times <nat:20> $ will be even positive integers . This must be true from the fact multiplying between an even positive integer with a number is an even natural number . On top of this , the terms $ <var:r> \times <nat:240> $ as well as $ <nat:244> \times <var:J> $ must be even . This fact holds as a consequence that taking the product of an even number with any number in $ \mathbb{N} $ is even . See the products $ <nat:324> \times <var:p> $ and moreover $ <nat:124> \times <var:z> $ are known to be even numbers in $ \mathbb{Z}^+ $ . This claim is proven from the knowledge taking the product of an even natural and some element of $ \mathbb{N} $ is guaranteed to be even . Further , the terms $ <nat:86> \times <var:a> $ , $ <nat:394> \times <var:B> $ , $ <nat:78> \times <var:k> $ , and also $ <var:I> \times <nat:266> $ will be even numbers in $ \mathbb{Z}^+ $ . This claim is proven because taking the product between an even element of $ \mathbb{N} $ and any whole number will be even . The term $ <var:y> \times <nat:250> $ is trivially an even element of $ \mathbb{N} $ . This claim holds from the fact the multiplication of an even number in $ \mathbb{N} $ and any number in $ \mathbb{N} $ is guaranteed to be even . Observe that the summing of even elements of $ \mathbb{N} $ successively with an odd integer is guaranteed to be an odd number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:I> <var:z> <var:r> <var:J> <var:k> <var:a> <var:t> <var:y> <var:p> <var:B> <var:O> : nat , Nat . odd ( <nat:266> * <var:I> + <nat:124> * <var:z> + <nat:240> * <var:r> + <nat:244> * <var:J> + <nat:78> * <var:k> + <nat:86> * <var:a> + <nat:216> * <var:t> + <nat:250> * <var:y> + <nat:324> * <var:p> + <nat:394> * <var:B> + <nat:20> * <var:O> + <nat:279> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:216> * <var:t> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:20> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:240> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:244> * <var:J> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:124> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:86> * <var:a> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:394> * <var:B> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:78> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:266> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:250> * <var:y> ) = true ) . { rewrite Nat . even_mul . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 