\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For all positive terms $ <var:N> $ , $ <var:c> $ , $ <var:a> $ , $ <var:f> $ , $ <var:j> $ , and $ <var:w> $ the sum $ <var:a> <nat:324> + <nat:262> <var:c> + <nat:398> <var:N> + <var:w> <nat:212> + <nat:296> <var:j> + <nat:142> <var:f> + <nat:162> $ is guaranteed to be even . \end{theorem} \begin{proof} Remember the term $ <nat:296> * <var:j> $ is obviously even . This holds because the integer $ <nat:296> $ is clearly an even number in $ \mathbb{Z}^+ $ . The term $ <var:a> * <nat:324> $ is obviously even . This claim must be true because the product between an even positive integer with any number in $ \mathbb{Z}^+ $ will be an even natural number . This fact holds from the knowledge the leading term $ <nat:324> $ is known as an even whole number . We know the pair $ <nat:398> * <var:N> $ is known as an even element of $ \mathbb{N} $ . This fact is proven because the whole number $ <nat:398> $ is guaranteed to be even . Furthermore , the term $ <var:f> * <nat:142> $ is known as even . This fact holds because the product of an even number in $ \mathbb{Z}^+ $ and some integer is guaranteed to be even and also the natural number $ <nat:142> $ is known to be even . What's more , the leading term $ <nat:212> $ is clearly an even element of $ \mathbb{N} $ . As a consequence , $ <nat:212> * <var:w> $ must be even additionally . The integer $ <nat:262> $ is trivially even . As a consequence , the pair $ <nat:262> * <var:c> $ is even at the same time . Remember $ <nat:162> $ is known to be an even number in $ \mathbb{N} $ . We justify the summation $ <nat:162> + <var:j> * <nat:296> + <nat:142> * <var:f> + <nat:324> * <var:a> + <nat:398> * <var:N> + <var:w> * <nat:212> + <nat:262> * <var:c> $ must be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:N> <var:c> <var:a> <var:f> <var:j> <var:w> : nat , Nat . even ( <nat:324> * <var:a> + <nat:262> * <var:c> + <nat:398> * <var:N> + <nat:212> * <var:w> + <nat:296> * <var:j> + <nat:142> * <var:f> + <nat:162> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:296> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:296> * <var:j> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:324> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:a> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:398> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:398> * <var:N> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:142> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:142> * <var:f> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:212> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:212> * <var:w> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:262> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:262> * <var:c> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:162> = true ) . { auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 