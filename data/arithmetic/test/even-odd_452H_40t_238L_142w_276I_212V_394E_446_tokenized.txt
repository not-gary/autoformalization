\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Assuming every positive terms $ <var:w> $ , $ <var:V> $ , $ <var:t> $ , $ <var:E> $ , $ <var:L> $ , $ <var:I> $ , and $ <var:H> $ the formula $ <var:w> \times <nat:142> + <nat:446> + <var:L> \times <nat:238> + <var:V> \times <nat:212> + <var:t> \times <nat:40> + <var:H> \times <nat:452> + <var:E> \times <nat:394> + <nat:276> \times <var:I> $ is guaranteed to be even . \end{theorem} \begin{proof} The theorem is proven since $ <nat:452> * <var:H> + <nat:40> * <var:t> + <nat:238> * <var:L> + <nat:142> * <var:w> + <var:I> * <nat:276> + <nat:212> * <var:V> + <var:E> * <nat:394> $ is even . Observe the addition of even elements of $ \mathbb{Z}^+ $ and an even positive integer must be even . Notice that the products $ <nat:452> \times <var:H> $ plus $ <var:I> \times <nat:276> $ are clearly even . Notice that the product $ <nat:394> \times <var:E> $ is known to be even . Furthermore , the products $ <var:L> \times <nat:238> $ and also $ <nat:212> \times <var:V> $ must be even . See that $ <var:t> \times <nat:40> $ is obviously even . What's more , recall that $ <nat:142> \times <var:w> $ is guaranteed to be even . plus since the product of even naturals with some natural is guaranteed to be an even number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:w> <var:V> <var:t> <var:E> <var:L> <var:I> <var:H> : nat , Nat . even ( <nat:142> * <var:w> + <nat:238> * <var:L> + <nat:212> * <var:V> + <nat:40> * <var:t> + <nat:452> * <var:H> + <nat:394> * <var:E> + <nat:276> * <var:I> + <nat:446> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:452> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:276> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:394> * <var:E> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:238> * <var:L> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:212> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:t> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:142> * <var:w> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:142> * <var:w> + <nat:238> * <var:L> + <nat:212> * <var:V> + <nat:40> * <var:t> + <nat:452> * <var:H> + <nat:394> * <var:E> + <nat:276> * <var:I> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 