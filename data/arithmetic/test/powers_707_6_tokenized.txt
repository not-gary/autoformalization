\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With the condition we get $ <var:f> \geq <nat:2> $ such that for $ <var:T> \in \mathbb{N} $ , $ <var:T> = <var:f> ^ <nat:6> $ , then we say that $ <var:T> $ is a <def:hexeract> whole number given some $ <var:f> $ . For this reason the integer <nat:64> is a <def:hexeract> number . \end{theorem} \begin{proof} Remember that <nat:64> is <def:hexeract> if <nat:64> is the exponential of $ <var:f> $ exponentiated to the degree <nat:6> satisfying the condition that $ <var:f> \geq <nat:2> $ . Observe $ <nat:64> = <var:f> ^ <nat:6> = <nat:2> ^ <nat:6> $ . Plus note $ <nat:2> \geq <nat:2> $ . Therefore , <nat:64> is a <def:hexeract> number is proven . As a result , <nat:64> is the repeated multiplication between some integer $ <var:f> $ exponentiated to the exponent <nat:6> is validated . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:hexeract> ( <var:T> : nat ) := exists <var:f> : nat , ( <var:f> >= <nat:2> ) /\ ( <var:T> = <var:f> ^ <nat:6> ) . Theorem <genP:1> : <def:hexeract> <nat:64> . Proof . unfold <def:hexeract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 