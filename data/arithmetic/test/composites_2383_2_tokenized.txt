\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Let that $ <var:g> $ is a <def:two-composite> positive integer given $ \exists <var:r> $ , $ <var:A> \in \mathbb{N} $ such that $ <var:g> = <var:A> \times <var:r> $ assuming some integer $ <var:g> $ and satisfying the condition that $ <var:r> $ , and $ <var:A> \geq <nat:2> $ . For this reason $ <var:g> = <nat:60> $ is a <def:two-composite> number . \end{theorem} \begin{proof} Recollect that <nat:60> is a <def:two-composite> integer granted <nat:60> is the product of whole numbers $ <var:r> $ and $ <var:A> $ larger than or equivalent to <nat:2> . Observe $ <nat:10> * <nat:6> = <nat:60> $ . Plus notice $ <nat:10> $ and $ <nat:6> $ are more than or equivalent to <nat:2> . This implies our theory holds . \end{proof} \end{document} 
~
Require Import Lia . Definition <def:two-composite> ( <var:g> : nat ) := exists <var:r> <var:A> : nat , ( <var:g> = <var:A> * <var:r> ) /\ ( <var:r> >= <nat:2> ) /\ ( <var:A> >= <nat:2> ) . Theorem <genP:1> : <def:two-composite> <nat:60> . Proof . unfold <def:two-composite> . exists <nat:6> . exists <nat:10> . assert ( <genH> : <nat:10> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:6> >= <nat:2> ) . { lia . } repeat split . all : try apply <genH> . all : try apply <genH> . Qed . 