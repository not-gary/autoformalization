\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} If and only if we get $ <var:P> \geq <nat:2> $ so that given some positive integer $ <var:G> $ , $ <var:P> ^ <nat:6> = <var:G> $ , then say that $ <var:G> $ is a <def:hexeract> positive integer taking some \in \mathbb{N} $ . Thus $ <nat:64> = <var:G> $ is a <def:hexeract> natural number . \end{theorem} \begin{proof} Remember that a <def:hexeract> is the repeated multiplication of whole number $ <var:P> $ exponentiated to the exponential <nat:6> greater than or equal to <nat:2> . Observe that $ <var:P> ^ <nat:6> = <nat:64> = <nat:2> ^ <nat:6> $ . As a consequence , <nat:64> is a <def:hexeract> number is validated . Consequently , <nat:64> is the repeated product between some $ <var:P> $ exponentiated to the degree <nat:6> is validated . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:hexeract> ( <var:G> : nat ) := exists <var:P> : nat , ( <var:P> >= <nat:2> ) /\ ( <var:P> ^ <nat:6> = <var:G> ) . Theorem <genP:1> : <def:hexeract> <nat:64> . Proof . unfold <def:hexeract> . exists <nat:2> . split . lia . auto . Qed . 