\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \newtheorem{definition}{Definition} \begin{document} \begin{definition} With the condition we get $ <var:u> = <var:I> ^ <nat:6> $ with $ <var:u> \in \mathbb{N} $ plus such that $ <var:I> \geq <nat:2> $ , then we claim that $ <var:u> $ is a <def:hexeract> integer assuming some $ <var:I> $ . \end{definition} \begin{theorem} $ <var:u> = <nat:64> $ is a <def:hexeract> whole number . \end{theorem} \begin{proof} Remember that <nat:64> is <def:hexeract> supposing that <nat:64> is the repeated product between $ <var:I> $ raised to the degree <nat:6> such that $ <var:I> \geq <nat:2> $ . We demonstrate <nat:64> is a <def:hexeract> natural number . Note $ <nat:2> $ is more than or equal to <nat:2> . Plus note that also $ <nat:64> = <var:I> ^ <nat:6> $ gives $ <var:I> = <nat:2> $ . This shows <nat:64> is a <def:hexeract> number . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:hexeract> ( <var:u> : nat ) := exists <var:I> : nat , ( <var:u> = <var:I> ^ <nat:6> ) /\ ( <var:I> >= <nat:2> ) . Theorem <genP:1> : <def:hexeract> <nat:64> . Proof . unfold <def:hexeract> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } exists <nat:2> . split . auto . apply <genH> . Qed . 