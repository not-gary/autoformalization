\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For all integers $ <var:n> $ , $ <var:v> $ , $ <var:I> $ , $ <var:o> $ , $ <var:O> $ , and $ <var:c> $ $ <nat:334> <var:O> + <nat:342> <var:o> + <nat:206> <var:c> + <var:I> <nat:374> + <nat:304> + <var:v> <nat:44> + <var:n> <nat:386> $ is even . \end{theorem} \begin{proof} This proposition holds because $ <nat:334> <var:O> + <nat:342> <var:o> + <var:I> <nat:374> + <nat:386> <var:n> + <nat:44> <var:v> + <var:c> <nat:206> $ is clearly even . Observe that the addition of even positive integers concurrently is guaranteed to be an even positive integer . The pair $ <nat:334> * <var:O> $ is even . This holds because taking the product between an even whole number and an arbitrary natural is guaranteed to be even . Take note that the pair $ <var:I> * <nat:374> $ is an even number in $ \mathbb{Z}^+ $ . Observe that the pairs $ <nat:342> * <var:o> $ and moreover $ <var:n> * <nat:386> $ are even . The product $ <nat:206> * <var:c> $ is an even number in $ \mathbb{N} $ because the product of an even element of $ \mathbb{Z}^+ $ and an arbitrary number will be an even natural number . Likewise , the term $ <nat:44> * <var:v> $ is guaranteed to be an even number in $ \mathbb{N} $ . in addition to since multiplying between an even number in $ \mathbb{Z}^+ $ and a integer is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:n> <var:v> <var:I> <var:o> <var:O> <var:c> : nat , Nat . even ( <nat:334> * <var:O> + <nat:342> * <var:o> + <nat:206> * <var:c> + <nat:374> * <var:I> + <nat:44> * <var:v> + <nat:386> * <var:n> + <nat:304> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:334> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:374> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:386> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:206> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:44> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:334> * <var:O> + <nat:342> * <var:o> + <nat:206> * <var:c> + <nat:374> * <var:I> + <nat:44> * <var:v> + <nat:386> * <var:n> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 