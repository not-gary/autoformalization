\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:s> \times <nat:118> + <nat:98> \times <var:v> + <var:j> \times <nat:266> + <var:O> \times <nat:472> + <var:A> \times <nat:200> + <nat:422> + <var:X> \times <nat:356> $ will be even . \end{theorem} \begin{proof} We know the summation $ <var:v> \cdot <nat:98> + <var:O> \cdot <nat:472> + <nat:266> \cdot <var:j> + <nat:422> + <var:A> \cdot <nat:200> + <nat:356> \cdot <var:X> + <var:s> \cdot <nat:118> $ is known to be even since the sum $ <nat:472> * <var:O> + <var:X> * <nat:356> + <nat:98> * <var:v> + <nat:118> * <var:s> + <var:j> * <nat:266> + <var:A> * <nat:200> $ is known as even . See the addition between an even element of $ \mathbb{N} $ and an even number must be even . The products $ <var:s> <nat:118> $ , $ <var:v> <nat:98> $ , and also $ <nat:472> <var:O> $ must be even as a consequence that taking the product between an even positive integer and a positive integer is guaranteed to be an even integer . Notice the terms $ <nat:266> <var:j> $ in addition to $ <nat:356> <var:X> $ will be even . This is proven because multiplying between an even element of $ \mathbb{Z}^+ $ with a number in $ \mathbb{Z}^+ $ is guaranteed to be an even number . Recall the product $ <var:A> <nat:200> $ is known as even . as well as from the knowledge the multiplication between an even element of $ \mathbb{N} $ and an arbitrary element of $ \mathbb{Z}^+ $ will be an even number in $ \mathbb{N} $ the expression $ <nat:118> <var:s> + <var:A> <nat:200> + <var:j> <nat:266> + <nat:98> <var:v> + <var:X> <nat:356> + <nat:472> <var:O> $ will be obviously even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:s> <var:v> <var:j> <var:O> <var:A> <var:X> : nat , Nat . even ( <nat:118> * <var:s> + <nat:98> * <var:v> + <nat:266> * <var:j> + <nat:472> * <var:O> + <nat:200> * <var:A> + <nat:356> * <var:X> + <nat:422> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:118> * <var:s> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:98> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:472> * <var:O> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:266> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:356> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:200> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:118> * <var:s> + <nat:98> * <var:v> + <nat:266> * <var:j> + <nat:472> * <var:O> + <nat:200> * <var:A> + <nat:356> * <var:X> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 