\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:n> <nat:178> + <nat:428> <var:X> + <nat:332> <var:S> + <nat:406> <var:c> + <var:s> <nat:320> + <nat:390> <var:F> + <nat:215> + <nat:472> <var:d> + <nat:272> <var:v> $ is odd . \end{theorem} \begin{proof} We make use the knowledge that the summation $ <nat:390> \cdot <var:F> + <var:s> \cdot <nat:320> + <var:v> \cdot <nat:272> + <nat:428> \cdot <var:X> + <var:c> \cdot <nat:406> + <nat:472> \cdot <var:d> + <nat:215> + <var:n> \cdot <nat:178> + <var:S> \cdot <nat:332> $ is trivially odd from the knowledge the summation $ <var:d> \cdot <nat:472> + <nat:178> \cdot <var:n> + <nat:428> \cdot <var:X> + <var:c> \cdot <nat:406> + <var:S> \cdot <nat:332> + <nat:390> \cdot <var:F> + <var:s> \cdot <nat:320> + <nat:272> \cdot <var:v> $ is obviously even . The terms $ <var:n> <nat:178> $ as well as $ <var:d> <nat:472> $ are even whole numbers as a consequence that the product of an even number and a element of $ \mathbb{N} $ is even . The pair $ <var:s> <nat:320> $ is guaranteed to be even . This claim is true from the fact the multiplication of an even element of $ \mathbb{Z}^+ $ and some natural number will be an even number . The pairs $ <nat:390> <var:F> $ , $ <nat:406> <var:c> $ , $ <var:v> <nat:272> $ , and moreover $ <var:X> <nat:428> $ are trivially even natural numbers . On top of this , the term $ <var:S> <nat:332> $ is known as an even natural . Our main result is proven as a consequence that the totaling of an even integer and an even element of $ \mathbb{Z}^+ $ must be an even element of $ \mathbb{Z}^+ $ and likewise because taking the product of even numbers in $ \mathbb{N} $ and an arbitrary natural will be instrinsically an even number in $ \mathbb{N} $ . Take note $ <nat:215> $ is clearly odd . Remember that the summation between even positive integers collectively with an odd integer will be odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:n> <var:X> <var:S> <var:c> <var:s> <var:F> <var:d> <var:v> : nat , Nat . odd ( <nat:178> * <var:n> + <nat:428> * <var:X> + <nat:332> * <var:S> + <nat:406> * <var:c> + <nat:320> * <var:s> + <nat:390> * <var:F> + <nat:472> * <var:d> + <nat:272> * <var:v> + <nat:215> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:178> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:472> * <var:d> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:320> * <var:s> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:390> * <var:F> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:406> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:272> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:332> * <var:S> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:178> * <var:n> + <nat:428> * <var:X> + <nat:332> * <var:S> + <nat:406> * <var:c> + <nat:320> * <var:s> + <nat:390> * <var:F> + <nat:472> * <var:d> + <nat:272> * <var:v> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . odd <nat:215> = true ) . { auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 