\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:338> * <var:f> + <var:y> * <nat:290> + <var:j> * <nat:486> + <var:b> * <nat:216> + <nat:242> * <var:V> + <var:A> * <nat:428> + <var:q> * <nat:282> + <var:n> * <nat:150> + <nat:371> + <nat:264> * <var:g> + <nat:316> * <var:x> $ must be odd . \end{theorem} \begin{proof} The summing between even positive integers with an odd natural number must be clearly an odd number . Notice that the pairs $ <nat:264> \times <var:g> $ and also $ <nat:216> \times <var:b> $ are obviously even . Observe the pairs $ <nat:150> \times <var:n> $ plus $ <nat:290> \times <var:y> $ must be even elements of $ \mathbb{N} $ . Take note that the term $ <var:V> \times <nat:242> $ is obviously even . This claim is true from the fact multiplying of an even number in $ \mathbb{N} $ with a number in $ \mathbb{Z}^+ $ must be even . In addition , observe that the products $ <nat:428> \times <var:A> $ , $ <var:j> \times <nat:486> $ , and moreover $ <var:x> \times <nat:316> $ are trivially even integers from the knowledge the product between an even element of $ \mathbb{N} $ and an arbitrary element of $ \mathbb{Z}^+ $ must be even . In addition , observe the pair $ <var:q> \times <nat:282> $ is even . This claim holds from the fact the product of an even natural number and some element of $ \mathbb{Z}^+ $ is even . We know that the pair $ <nat:338> \times <var:f> $ is trivially even . This is true by reason that multiplying between an even integer and some positive integer is guaranteed to be even . It can be shown that the expression $ <nat:216> <var:b> + <var:V> <nat:242> + <nat:316> <var:x> + <var:n> <nat:150> + <nat:264> <var:g> + <nat:486> <var:j> + <var:y> <nat:290> + <nat:338> <var:f> + <nat:282> <var:q> + <nat:371> + <nat:428> <var:A> $ must be odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:f> <var:y> <var:j> <var:b> <var:V> <var:A> <var:q> <var:n> <var:g> <var:x> : nat , Nat . odd ( <nat:338> * <var:f> + <nat:290> * <var:y> + <nat:486> * <var:j> + <nat:216> * <var:b> + <nat:242> * <var:V> + <nat:428> * <var:A> + <nat:282> * <var:q> + <nat:150> * <var:n> + <nat:264> * <var:g> + <nat:316> * <var:x> + <nat:371> ) = true . Proof . intros . rewrite Nat . odd_add . rewrite <- Nat . negb_even . repeat rewrite Nat . even_add . assert ( <genH> : Nat . even ( <nat:264> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:216> * <var:b> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:150> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:290> * <var:y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:242> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:486> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:316> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:282> * <var:q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:338> * <var:f> ) = true ) . { rewrite Nat . even_mul . auto . } rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 