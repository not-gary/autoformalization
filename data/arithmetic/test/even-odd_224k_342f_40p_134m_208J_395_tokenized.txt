\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With all variables $ <var:f> $ , $ <var:m> $ , $ <var:k> $ , $ <var:J> $ , and $ <var:p> $ the sum $ <var:k> * <nat:224> + <nat:342> * <var:f> + <var:m> * <nat:134> + <nat:208> * <var:J> + <nat:40> * <var:p> + <nat:395> $ is guaranteed to be odd . \end{theorem} \begin{proof} We demonstrate our result is proven . The claim is proven as a consequence that $ <nat:224> <var:k> + <var:f> <nat:342> + <var:m> <nat:134> + <nat:208> <var:J> + <nat:40> <var:p> $ is known to be even . Remember that the term $ <var:k> \times <nat:224> $ is obviously an even integer from the knowledge taking the product of an even number in $ \mathbb{Z}^+ $ with an arbitrary whole number will be an even positive integer and the integer $ <nat:224> $ is obviously even . Adding on , the term $ <nat:40> \times <var:p> $ is obviously even . This is true because the natural number $ <nat:40> $ is trivially even . On top of this , take note that the product $ <nat:342> \times <var:f> $ is known to be even . This fact is proven as a consequence that the natural number $ <nat:342> $ is guaranteed to be an even natural number . Additionally , the product $ <var:m> \times <nat:134> $ is trivially an even positive integer . This must be true from the knowledge taking the product between an even number in $ \mathbb{Z}^+ $ with some number in $ \mathbb{Z}^+ $ is even as well as the coefficient $ <nat:134> $ is trivially even . Additionally , take note that the pair $ <var:J> \times <nat:208> $ is guaranteed to be an even integer . This fact holds from the fact the integer $ <nat:208> $ is guaranteed to be an even number in $ \mathbb{N} $ . From the fact the adding between even natural numbers and an even number must be an even element of $ \mathbb{N} $ , the sum $ <nat:224> \times <var:k> + <nat:342> \times <var:f> + <var:p> \times <nat:40> + <var:m> \times <nat:134> + <var:J> \times <nat:208> $ is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:f> <var:m> <var:k> <var:J> <var:p> : nat , Nat . odd ( <nat:224> * <var:k> + <nat:342> * <var:f> + <nat:134> * <var:m> + <nat:208> * <var:J> + <nat:40> * <var:p> + <nat:395> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:224> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:224> * <var:k> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:40> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:40> * <var:p> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:342> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:f> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:134> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:134> * <var:m> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:208> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:208> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:224> * <var:k> + <nat:342> * <var:f> + <nat:134> * <var:m> + <nat:208> * <var:J> + <nat:40> * <var:p> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 