\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:366> + <var:Q> * <nat:126> + <nat:354> * <var:B> + <var:v> * <nat:208> + <var:W> * <nat:430> + <var:X> * <nat:298> + <nat:96> * <var:Z> + <var:M> * <nat:390> + <var:k> * <nat:152> $ is even for every $ <var:B> $ , $ <var:W> $ , $ <var:Z> $ , $ <var:v> $ , $ <var:X> $ , $ <var:M> $ , $ <var:k> $ , and $ <var:Q> $ . \end{theorem} \begin{proof} We justify the expression $ <var:k> * <nat:152> + <var:X> * <nat:298> + <var:v> * <nat:208> + <nat:366> + <var:M> * <nat:390> + <var:B> * <nat:354> + <nat:126> * <var:Q> + <nat:430> * <var:W> + <nat:96> * <var:Z> $ is obviously even by reason that $ <nat:354> \cdot <var:B> + <var:X> \cdot <nat:298> + <nat:152> \cdot <var:k> + <var:v> \cdot <nat:208> + <nat:126> \cdot <var:Q> + <nat:430> \cdot <var:W> + <nat:96> \cdot <var:Z> + <var:M> \cdot <nat:390> $ is obviously even . The terms $ <var:Q> <nat:126> $ as well as $ <var:W> <nat:430> $ are obviously even natural numbers . Observe that the pairs $ <var:M> <nat:390> $ , $ <var:v> <nat:208> $ , $ <var:X> <nat:298> $ , in addition to $ <nat:152> <var:k> $ are known as even as a consequence that multiplying between an even element of $ \mathbb{N} $ with any element of $ \mathbb{Z}^+ $ will be even . We know the term $ <var:Z> <nat:96> $ is clearly even from the fact multiplying of an even number in $ \mathbb{Z}^+ $ and some number must be an even natural . The pair $ <var:B> <nat:354> $ is known to be an even positive integer . Because the totaling of even elements of $ \mathbb{N} $ with an even positive integer is guaranteed to be an even natural number , the summation $ <nat:152> * <var:k> + <var:B> * <nat:354> + <nat:208> * <var:v> + <nat:126> * <var:Q> + <nat:430> * <var:W> + <nat:298> * <var:X> + <nat:96> * <var:Z> + <nat:390> * <var:M> $ must be even . Take note that the totaling of even naturals will be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:B> <var:W> <var:Z> <var:v> <var:X> <var:M> <var:k> <var:Q> : nat , Nat . even ( <nat:126> * <var:Q> + <nat:354> * <var:B> + <nat:208> * <var:v> + <nat:430> * <var:W> + <nat:298> * <var:X> + <nat:96> * <var:Z> + <nat:390> * <var:M> + <nat:152> * <var:k> + <nat:366> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:126> * <var:Q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:430> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:390> * <var:M> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:208> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:298> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:152> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:96> * <var:Z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:354> * <var:B> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:126> * <var:Q> + <nat:354> * <var:B> + <nat:208> * <var:v> + <nat:430> * <var:W> + <nat:298> * <var:X> + <nat:96> * <var:Z> + <nat:390> * <var:M> + <nat:152> * <var:k> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 