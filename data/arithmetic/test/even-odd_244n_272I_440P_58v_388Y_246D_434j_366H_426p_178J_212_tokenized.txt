\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:244> * <var:n> + <nat:272> * <var:I> + <var:P> * <nat:440> + <var:v> * <nat:58> + <var:Y> * <nat:388> + <nat:246> * <var:D> + <var:j> * <nat:434> + <nat:366> * <var:H> + <var:p> * <nat:426> + <nat:212> + <nat:178> * <var:J> $ must be even with all positive terms $ <var:v> $ , $ <var:n> $ , $ <var:P> $ , $ <var:D> $ , $ <var:Y> $ , $ <var:J> $ , $ <var:H> $ , $ <var:I> $ , $ <var:j> $ , and $ <var:p> $ . \end{theorem} \begin{proof} Recall the products $ <var:H> \times <nat:366> $ as well as $ <var:p> \times <nat:426> $ are known to be even . This fact is true from the fact the product between an even natural and some natural will be clearly an even whole number . The products $ <nat:440> \times <var:P> $ , $ <nat:272> \times <var:I> $ , $ <var:j> \times <nat:434> $ , plus $ <nat:58> \times <var:v> $ are guaranteed to be even . This must be true as a consequence that taking the product of an even number with some natural will be even . Remember the pair $ <nat:178> \times <var:J> $ is clearly an even element of $ \mathbb{Z}^+ $ since the product of an even positive integer and some number in $ \mathbb{Z}^+ $ will be fundamentally an even element of $ \mathbb{Z}^+ $ . On top of this , the pair $ <nat:246> \times <var:D> $ is clearly an even natural number . This claim is true as a consequence that taking the product between an even natural number and a integer is guaranteed to be by itself an even whole number . Take note the product $ <nat:244> \times <var:n> $ is guaranteed to be even since multiplying between an even number in $ \mathbb{N} $ and some natural is clearly an even positive integer . On top of this , the product $ <nat:388> \times <var:Y> $ is obviously even . We check this claim must be true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:v> <var:n> <var:P> <var:D> <var:Y> <var:J> <var:H> <var:I> <var:j> <var:p> : nat , Nat . even ( <nat:244> * <var:n> + <nat:272> * <var:I> + <nat:440> * <var:P> + <nat:58> * <var:v> + <nat:388> * <var:Y> + <nat:246> * <var:D> + <nat:434> * <var:j> + <nat:366> * <var:H> + <nat:426> * <var:p> + <nat:178> * <var:J> + <nat:212> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:366> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:426> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:440> * <var:P> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:272> * <var:I> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:434> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:58> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:178> * <var:J> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:246> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:244> * <var:n> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:388> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 