\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:462> <var:e> + <nat:432> <var:v> + <nat:114> <var:c> + <var:g> <nat:456> + <nat:219> $ will be odd taking some unknowns $ <var:e> $ , $ <var:v> $ , $ <var:g> $ , and $ <var:c> $ . \end{theorem} \begin{proof} Recall that $ <nat:219> $ is obviously odd . We verify that the sum $ <var:e> \times <nat:462> + <nat:432> \times <var:v> + <nat:219> + <var:c> \times <nat:114> + <var:g> \times <nat:456> $ must be odd . Remember that the totaling between an even natural number and an odd whole number is an odd positive integer . We employ the fact $ <var:v> * <nat:432> + <var:c> * <nat:114> + <nat:456> * <var:g> + <var:e> * <nat:462> $ is trivially even . Remember the products $ <var:v> <nat:432> $ in addition to $ <var:c> <nat:114> $ are clearly even as a consequence that the product between an even positive integer with some integer must be by itself an even natural . Further , see that the pair $ <var:g> <nat:456> $ is even . Notice $ <nat:462> <var:e> $ is trivially an even natural . This is true as a consequence that the multiplication between an even whole number with some element of $ \mathbb{Z}^+ $ will be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:e> <var:v> <var:g> <var:c> : nat , Nat . odd ( <nat:462> * <var:e> + <nat:432> * <var:v> + <nat:114> * <var:c> + <nat:456> * <var:g> + <nat:219> ) = true . Proof . intros . assert ( <genH> : Nat . odd <nat:219> = true ) . { auto . } rewrite Nat . odd_add . assert ( <genH> : Nat . even ( <nat:432> * <var:v> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:114> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:456> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:462> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:462> * <var:e> + <nat:432> * <var:v> + <nat:114> * <var:c> + <nat:456> * <var:g> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 