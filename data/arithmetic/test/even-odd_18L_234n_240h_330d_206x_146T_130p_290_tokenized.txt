\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:146> \times <var:T> + <var:d> \times <nat:330> + <nat:290> + <nat:206> \times <var:x> + <var:L> \times <nat:18> + <var:n> \times <nat:234> + <nat:240> \times <var:h> + <nat:130> \times <var:p> $ must be even given every whole numbers $ <var:x> $ , $ <var:h> $ , $ <var:p> $ , $ <var:L> $ , $ <var:d> $ , $ <var:T> $ , and $ <var:n> $ . \end{theorem} \begin{proof} We know that the term $ <nat:18> <var:L> $ is an even number in $ \mathbb{Z}^+ $ . This must be true as a consequence that the positive integer $ <nat:18> $ is guaranteed to be by definition an even positive integer . The leading term $ <nat:234> $ is obviously even . As a consequence , the product $ <nat:234> <var:n> $ is even in addition . Notice that the product $ <var:h> <nat:240> $ is guaranteed to be even . This is proven by reason that multiplying between an even whole number with some element of $ \mathbb{Z}^+ $ will be even , and is true because the natural number $ <nat:240> $ is known as an even whole number . Recall that the pair $ <var:d> <nat:330> $ is obviously even . This claim is true since taking the product between an even integer and some natural number is guaranteed to be an even number , and is true since the number $ <nat:330> $ is clearly trivially an even natural number . Likewise , recall the pair $ <var:x> <nat:206> $ is guaranteed to be even . This must be true from the fact the integer $ <nat:206> $ is an even natural . In addition , the term $ <var:T> <nat:146> $ is obviously even . This fact must be true since the product of an even positive integer and an arbitrary element of $ \mathbb{Z}^+ $ is guaranteed to be even in addition to the whole number $ <nat:146> $ is an even integer . What's more , remember the natural number $ <nat:130> $ is even . Accordingly , the product $ <var:p> <nat:130> $ will be even too . The totaling between even numbers concurrently with an even integer will be trivially an even element of $ \mathbb{Z}^+ $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:x> <var:h> <var:p> <var:L> <var:d> <var:T> <var:n> : nat , Nat . even ( <nat:146> * <var:T> + <nat:330> * <var:d> + <nat:206> * <var:x> + <nat:18> * <var:L> + <nat:234> * <var:n> + <nat:240> * <var:h> + <nat:130> * <var:p> + <nat:290> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:18> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:18> * <var:L> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:234> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:234> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:240> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:240> * <var:h> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:330> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:330> * <var:d> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:206> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:206> * <var:x> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:146> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:146> * <var:T> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:130> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:130> * <var:p> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 