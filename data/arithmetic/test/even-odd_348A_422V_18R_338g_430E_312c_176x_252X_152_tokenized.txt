\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With any positive terms $ <var:X> $ , $ <var:A> $ , $ <var:x> $ , $ <var:c> $ , $ <var:E> $ , $ <var:V> $ , $ <var:R> $ , and $ <var:g> $ the summation $ <var:X> * <nat:252> + <var:E> * <nat:430> + <nat:176> * <var:x> + <var:R> * <nat:18> + <nat:422> * <var:V> + <var:A> * <nat:348> + <nat:312> * <var:c> + <nat:338> * <var:g> + <nat:152> $ is even . \end{theorem} \begin{proof} Recall that $ <nat:430> \times <var:E> + <nat:18> \times <var:R> + <nat:312> \times <var:c> + <var:g> \times <nat:338> + <var:A> \times <nat:348> + <var:V> \times <nat:422> + <var:X> \times <nat:252> + <nat:176> \times <var:x> $ is obviously even . The terms $ <var:X> \times <nat:252> $ as well as $ <var:g> \times <nat:338> $ are obviously even elements of $ \mathbb{Z}^+ $ . This fact is proven by reason that multiplying of an even natural number with a number in $ \mathbb{Z}^+ $ must be even . Further , $ <var:R> \times <nat:18> $ is trivially an even number from the fact multiplying between an even number in $ \mathbb{Z}^+ $ and a natural is guaranteed to be even . What's more , we know that the terms $ <var:x> \times <nat:176> $ plus $ <var:E> \times <nat:430> $ are clearly even . Additionally , see that $ <var:A> \times <nat:348> $ is trivially even . This must be true by reason that the product of an even positive integer and some whole number is guaranteed to be even . $ <var:c> \times <nat:312> $ is clearly an even number in $ \mathbb{N} $ . Observe that the term $ <var:V> \times <nat:422> $ is clearly an even whole number . Observe that $ <nat:152> $ is known as an even number in $ \mathbb{Z}^+ $ . We verify the formula $ <nat:152> + <nat:348> \times <var:A> + <nat:422> \times <var:V> + <nat:18> \times <var:R> + <var:g> \times <nat:338> + <var:E> \times <nat:430> + <var:c> \times <nat:312> + <var:x> \times <nat:176> + <var:X> \times <nat:252> $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:X> <var:A> <var:x> <var:c> <var:E> <var:V> <var:R> <var:g> : nat , Nat . even ( <nat:252> * <var:X> + <nat:430> * <var:E> + <nat:176> * <var:x> + <nat:18> * <var:R> + <nat:422> * <var:V> + <nat:348> * <var:A> + <nat:312> * <var:c> + <nat:338> * <var:g> + <nat:152> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:252> * <var:X> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:338> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:18> * <var:R> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:176> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:430> * <var:E> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:348> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:312> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:422> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:252> * <var:X> + <nat:430> * <var:E> + <nat:176> * <var:x> + <nat:18> * <var:R> + <nat:422> * <var:V> + <nat:348> * <var:A> + <nat:312> * <var:c> + <nat:338> * <var:g> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:152> = true ) . { auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 