\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:324> * <var:Q> + <nat:428> * <var:n> + <nat:338> + <var:A> * <nat:60> + <nat:206> * <var:J> + <var:K> * <nat:476> + <nat:130> * <var:f> $ is even . \end{theorem} \begin{proof} See the sum $ <var:f> <nat:130> + <nat:60> <var:A> + <nat:428> <var:n> + <nat:324> <var:Q> + <nat:476> <var:K> + <nat:206> <var:J> + <nat:338> $ is known to be even from the fact the expression $ <nat:428> \cdot <var:n> + <var:f> \cdot <nat:130> + <nat:60> \cdot <var:A> + <nat:324> \cdot <var:Q> + <nat:206> \cdot <var:J> + <var:K> \cdot <nat:476> $ is obviously even . See the positive integer $ <nat:206> $ is clearly even . Hence , the product $ <var:J> \times <nat:206> $ is even additionally . In addition , recall that the product $ <var:K> \times <nat:476> $ is obviously even by reason that the natural number $ <nat:476> $ is clearly an even number . The positive integer $ <nat:60> $ is guaranteed to be an even natural . For this reason , the product $ <var:A> \times <nat:60> $ will be even also . The term $ <nat:428> \times <var:n> $ is known to be an even natural number from the knowledge the number $ <nat:428> $ is trivially an even element of $ \mathbb{Z}^+ $ . In addition , remember that the coefficient $ <nat:130> $ is an even positive integer . Thus , the product $ <nat:130> \times <var:f> $ is even additionally . The natural number $ <nat:324> $ is trivially an even element of $ \mathbb{Z}^+ $ . Therefore , $ <var:Q> \times <nat:324> $ will be even at the same time . Because the summing between even integers and even positive integers is an even positive integer , the sum $ <var:n> \times <nat:428> + <nat:60> \times <var:A> + <var:K> \times <nat:476> + <var:f> \times <nat:130> + <var:Q> \times <nat:324> + <nat:206> \times <var:J> $ is itself even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Q> <var:n> <var:A> <var:J> <var:K> <var:f> : nat , Nat . even ( <nat:324> * <var:Q> + <nat:428> * <var:n> + <nat:60> * <var:A> + <nat:206> * <var:J> + <nat:476> * <var:K> + <nat:130> * <var:f> + <nat:338> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:206> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:206> * <var:J> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:476> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:476> * <var:K> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:60> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:60> * <var:A> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:428> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:n> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:130> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:130> * <var:f> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:324> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:Q> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:Q> + <nat:428> * <var:n> + <nat:60> * <var:A> + <nat:206> * <var:J> + <nat:476> * <var:K> + <nat:130> * <var:f> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 