\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Take as given that under the condition that there exists integer $ <var:O> $ assume that some whole number $ <var:t> $ is said to be a <def:enneract> when $ <var:t> = <var:O> ^ <nat:9> $ and more than or equivalent to <nat:2> . For this reason $ <nat:512> = <var:t> $ is a <def:enneract> positive integer . \end{theorem} \begin{proof} Recall that a <def:enneract> is the exponential between number $ <var:O> $ exponentiated to the degree <nat:9> greater than or equivalent to <nat:2> . We confirm <nat:512> is a <def:enneract> integer by reason of $ <nat:2> $ is more than or equivalent to <nat:2> and on account of $ <nat:512> = <nat:2> ^ <nat:9> $ . As a result , <nat:512> is a <def:enneract> whole number is verified . So , $ <nat:512> = <var:O> ^ <nat:9> $ and $ <var:O> \geq <nat:2> $ has been validated . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:enneract> ( <var:t> : nat ) := exists <var:O> : nat , ( <var:t> = <var:O> ^ <nat:9> ) /\ ( <var:O> >= <nat:2> ) . Theorem <genP:1> : <def:enneract> <nat:512> . Proof . unfold <def:enneract> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } exists <nat:2> . split . auto . apply <genH> . Qed . 