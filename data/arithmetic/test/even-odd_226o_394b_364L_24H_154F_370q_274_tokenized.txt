\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Given every $ <var:H> $ , $ <var:b> $ , $ <var:L> $ , $ <var:o> $ , $ <var:F> $ , and $ <var:q> \in \mathbb{N} $ the sum $ <var:F> \cdot <nat:154> + <nat:274> + <nat:394> \cdot <var:b> + <nat:364> \cdot <var:L> + <nat:226> \cdot <var:o> + <nat:370> \cdot <var:q> + <var:H> \cdot <nat:24> $ is even . \end{theorem} \begin{proof} We know that the sum $ <nat:364> \times <var:L> + <nat:370> \times <var:q> + <nat:274> + <nat:154> \times <var:F> + <nat:24> \times <var:H> + <nat:394> \times <var:b> + <var:o> \times <nat:226> $ is known to be even by reason that $ <nat:226> \times <var:o> + <var:q> \times <nat:370> + <var:H> \times <nat:24> + <nat:154> \times <var:F> + <nat:364> \times <var:L> + <nat:394> \times <var:b> $ is clearly even , from the fact the addition of even integers together will be an even positive integer . We know that the product $ <var:H> \cdot <nat:24> $ is an even natural number . This must be true since the number $ <nat:24> $ is obviously in itself an even number in $ \mathbb{Z}^+ $ . The integers $ <nat:394> $ , $ <nat:154> $ , $ <nat:370> $ , and $ <nat:364> $ are clearly even . Therefore , the products $ <var:b> \cdot <nat:394> $ , $ <var:F> \cdot <nat:154> $ , $ <nat:370> \cdot <var:q> $ , additionally $ <nat:364> \cdot <var:L> $ must be even too . Take note that the product $ <nat:226> \cdot <var:o> $ is guaranteed to be even . This holds because the product between an even whole number with an arbitrary integer must be an even number . This claim is proven by reason that the positive integer $ <nat:226> $ is guaranteed to be even . From the fact the addition between an even number in $ \mathbb{Z}^+ $ and an even whole number will be even , then the summation $ <nat:226> <var:o> + <nat:274> + <nat:394> <var:b> + <var:L> <nat:364> + <nat:24> <var:H> + <var:F> <nat:154> + <var:q> <nat:370> $ is guaranteed to be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:H> <var:b> <var:L> <var:o> <var:F> <var:q> : nat , Nat . even ( <nat:154> * <var:F> + <nat:394> * <var:b> + <nat:364> * <var:L> + <nat:226> * <var:o> + <nat:370> * <var:q> + <nat:24> * <var:H> + <nat:274> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:24> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:24> * <var:H> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:394> = true ) . { auto . } assert ( <genH> : Nat . even <nat:154> = true ) . { auto . } assert ( <genH> : Nat . even <nat:370> = true ) . { auto . } assert ( <genH> : Nat . even <nat:364> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:394> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:154> * <var:F> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:370> * <var:q> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:L> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:226> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:226> * <var:o> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:154> * <var:F> + <nat:394> * <var:b> + <nat:364> * <var:L> + <nat:226> * <var:o> + <nat:370> * <var:q> + <nat:24> * <var:H> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 