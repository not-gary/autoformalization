\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:20> + <nat:374> \times <var:o> + <var:m> \times <nat:436> + <nat:392> \times <var:e> + <nat:412> \times <var:V> + <nat:134> \times <var:Y> + <nat:128> \times <var:Z> + <var:b> \times <nat:456> + <nat:118> \times <var:x> $ will be even for variables $ <var:Z> $ , $ <var:V> $ , $ <var:m> $ , $ <var:b> $ , $ <var:o> $ , $ <var:x> $ , $ <var:e> $ , and $ <var:Y> $ . \end{theorem} \begin{proof} We employ the knowledge $ <nat:128> * <var:Z> + <var:e> * <nat:392> + <nat:436> * <var:m> + <var:V> * <nat:412> + <nat:374> * <var:o> + <var:b> * <nat:456> + <var:x> * <nat:118> + <nat:134> * <var:Y> $ is obviously even . Remember that the pairs $ <var:m> <nat:436> $ , $ <var:Y> <nat:134> $ , and moreover $ <var:V> <nat:412> $ are obviously even integers . The products $ <var:o> <nat:374> $ , $ <var:e> <nat:392> $ , and moreover $ <var:Z> <nat:128> $ are known to be even integers . This fact must be true from the fact taking the product between an even number in $ \mathbb{N} $ with an arbitrary integer is guaranteed to be even . See that the product $ <var:x> <nat:118> $ is known to be an even positive integer . This holds from the knowledge the multiplication of an even positive integer and some element of $ \mathbb{Z}^+ $ is even . See the pair $ <nat:456> <var:b> $ is guaranteed to be an even number in $ \mathbb{Z}^+ $ . As a consequence that the addition of an even integer with an even number is guaranteed to be even , $ <nat:128> \times <var:Z> + <var:m> \times <nat:436> + <var:Y> \times <nat:134> + <nat:374> \times <var:o> + <var:b> \times <nat:456> + <var:e> \times <nat:392> + <nat:118> \times <var:x> + <nat:412> \times <var:V> $ must be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:Z> <var:V> <var:m> <var:b> <var:o> <var:x> <var:e> <var:Y> : nat , Nat . even ( <nat:374> * <var:o> + <nat:436> * <var:m> + <nat:392> * <var:e> + <nat:412> * <var:V> + <nat:134> * <var:Y> + <nat:128> * <var:Z> + <nat:456> * <var:b> + <nat:118> * <var:x> + <nat:20> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:436> * <var:m> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:134> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:412> * <var:V> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:374> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:392> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:128> * <var:Z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:118> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:456> * <var:b> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:374> * <var:o> + <nat:436> * <var:m> + <nat:392> * <var:e> + <nat:412> * <var:V> + <nat:134> * <var:Y> + <nat:128> * <var:Z> + <nat:456> * <var:b> + <nat:118> * <var:x> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 