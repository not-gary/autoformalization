\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:H> <nat:266> + <nat:228> <var:r> + <nat:252> <var:g> + <nat:357> + <var:D> <nat:264> + <var:F> <nat:296> + <var:j> <nat:486> + <var:o> <nat:478> $ is guaranteed to be odd using integers $ <var:j> $ , $ <var:F> $ , $ <var:g> $ , $ <var:D> $ , $ <var:r> $ , $ <var:H> $ , and $ <var:o> $ . \end{theorem} \begin{proof} The summing between an even positive integer with an odd natural number will be an odd element of $ \mathbb{N} $ . The expression $ <var:F> \times <nat:296> + <var:o> \times <nat:478> + <nat:266> \times <var:H> + <var:j> \times <nat:486> + <nat:228> \times <var:r> + <var:g> \times <nat:252> + <nat:264> \times <var:D> $ is known as even . The terms $ <var:j> \times <nat:486> $ , $ <var:F> \times <nat:296> $ , and $ <var:D> \times <nat:264> $ are even whole numbers . What's more , recall the pairs $ <nat:228> \times <var:r> $ in addition to $ <var:g> \times <nat:252> $ are known to be even as a consequence that the multiplication of an even positive integer and some number in $ \mathbb{Z}^+ $ must be an even integer . In addition , $ <nat:266> \times <var:H> $ is clearly an even natural . Additionally , the product $ <nat:478> \times <var:o> $ is even since the multiplication between an even number in $ \mathbb{Z}^+ $ and some element of $ \mathbb{Z}^+ $ is an even integer . It can be demonstrated that our proposition is true . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:j> <var:F> <var:g> <var:D> <var:r> <var:H> <var:o> : nat , Nat . odd ( <nat:266> * <var:H> + <nat:228> * <var:r> + <nat:252> * <var:g> + <nat:264> * <var:D> + <nat:296> * <var:F> + <nat:486> * <var:j> + <nat:478> * <var:o> + <nat:357> ) = true . Proof . intros . rewrite Nat . odd_add . assert ( <genH> : Nat . even ( <nat:486> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:296> * <var:F> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:264> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:228> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:252> * <var:g> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:266> * <var:H> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:478> * <var:o> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:266> * <var:H> + <nat:228> * <var:r> + <nat:252> * <var:g> + <nat:264> * <var:D> + <nat:296> * <var:F> + <nat:486> * <var:j> + <nat:478> * <var:o> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 