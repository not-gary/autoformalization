\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:428> * <var:N> + <nat:293> + <nat:320> * <var:u> + <var:M> * <nat:46> + <var:t> * <nat:388> + <var:P> * <nat:472> + <var:q> * <nat:238> + <var:Q> * <nat:188> + <nat:130> * <var:A> + <nat:324> * <var:e> $ is guaranteed to be odd . \end{theorem} \begin{proof} Notice $ <var:u> \cdot <nat:320> + <var:N> \cdot <nat:428> + <nat:188> \cdot <var:Q> + <var:M> \cdot <nat:46> + <var:P> \cdot <nat:472> + <var:e> \cdot <nat:324> + <var:A> \cdot <nat:130> + <var:t> \cdot <nat:388> + <var:q> \cdot <nat:238> $ is known as even , because the adding between even natural numbers together and an even whole number is guaranteed to be an even positive integer . Observe that the product $ <nat:46> \cdot <var:M> $ is known as even since the product between an even element of $ \mathbb{Z}^+ $ and some natural number must be by definition an even element of $ \mathbb{N} $ . On top of this , the terms $ <nat:388> \cdot <var:t> $ , $ <var:N> \cdot <nat:428> $ , $ <var:A> \cdot <nat:130> $ , $ <nat:238> \cdot <var:q> $ , $ <var:u> \cdot <nat:320> $ , additionally $ <var:P> \cdot <nat:472> $ are clearly even from the fact taking the product of an even element of $ \mathbb{Z}^+ $ with some element of $ \mathbb{Z}^+ $ is in itself an even number . In addition , see that the pair $ <nat:324> \cdot <var:e> $ is obviously an even number . This is proven since the multiplication of an even element of $ \mathbb{N} $ with any natural number is by its very nature an even natural number . Furthermore , the pair $ <nat:188> \cdot <var:Q> $ is guaranteed to be an even number because multiplying of an even element of $ \mathbb{N} $ and an arbitrary element of $ \mathbb{N} $ is even . It can be proven that this proposition is proven . See $ <nat:293> $ is guaranteed to be odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:N> <var:u> <var:M> <var:t> <var:P> <var:q> <var:Q> <var:A> <var:e> : nat , Nat . odd ( <nat:428> * <var:N> + <nat:320> * <var:u> + <nat:46> * <var:M> + <nat:388> * <var:t> + <nat:472> * <var:P> + <nat:238> * <var:q> + <nat:188> * <var:Q> + <nat:130> * <var:A> + <nat:324> * <var:e> + <nat:293> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:46> * <var:M> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:388> * <var:t> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:130> * <var:A> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:238> * <var:q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:320> * <var:u> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:472> * <var:P> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:188> * <var:Q> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:428> * <var:N> + <nat:320> * <var:u> + <nat:46> * <var:M> + <nat:388> * <var:t> + <nat:472> * <var:P> + <nat:238> * <var:q> + <nat:188> * <var:Q> + <nat:130> * <var:A> + <nat:324> * <var:e> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . odd <nat:293> = true ) . { auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 