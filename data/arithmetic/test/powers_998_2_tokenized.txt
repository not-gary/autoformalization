\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Allow $ <var:S> \in \mathbb{N} $ satisfying the condition that $ <var:S> \geq <nat:2> $ as well as where the whole number <nat:4> is the exponential of number $ <var:S> \in \mathbb{N} $ repeatedly multiplied to the power <nat:2> . \end{theorem} \begin{proof} Allow $ <var:S> = <nat:2> $ . We demonstrate <nat:4> is the raising of of some natural number $ <var:S> $ exponentiated to the exponential <nat:2> . Note $ <var:S> = <nat:2> $ is larger than or equivalent to <nat:2> . Furthermore note that still $ <nat:4> = <var:S> ^ <nat:2> = <nat:2> ^ <nat:2> $ . This suggests our claim holds . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Theorem <genP:1> : exists <var:S> : nat , ( <var:S> >= <nat:2> ) /\ ( <nat:4> = <var:S> ^ <nat:2> ) . Proof . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 