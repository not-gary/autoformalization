\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:106> \cdot <var:x> + <var:M> \cdot <nat:254> + <nat:14> \cdot <var:p> + <var:z> \cdot <nat:334> + <nat:496> + <nat:86> \cdot <var:h> + <nat:210> \cdot <var:r> + <nat:156> \cdot <var:W> $ is guaranteed to be even with all terms $ <var:W> $ , $ <var:h> $ , $ <var:r> $ , $ <var:p> $ , $ <var:z> $ , $ <var:M> $ , and $ <var:x> $ . \end{theorem} \begin{proof} It can be shown that our proposition is proven . Notice that $ <nat:496> $ is even . Remember the terms $ <var:W> \times <nat:156> $ , $ <nat:254> \times <var:M> $ , and also $ <nat:334> \times <var:z> $ will be even . This claim is proven because the product of an even natural with a element of $ \mathbb{Z}^+ $ must be even . The term $ <nat:106> \times <var:x> $ is known to be even . Recall that the pair $ <var:h> \times <nat:86> $ is obviously even . Observe that the term $ <var:r> \times <nat:210> $ is known to be an even natural number . Additionally , the pair $ <var:p> \times <nat:14> $ is guaranteed to be an even positive integer because the multiplication between an even whole number and a number must be an even element of $ \mathbb{Z}^+ $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:W> <var:h> <var:r> <var:p> <var:z> <var:M> <var:x> : nat , Nat . even ( <nat:106> * <var:x> + <nat:254> * <var:M> + <nat:14> * <var:p> + <nat:334> * <var:z> + <nat:86> * <var:h> + <nat:210> * <var:r> + <nat:156> * <var:W> + <nat:496> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:496> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:156> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:254> * <var:M> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:334> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:106> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:86> * <var:h> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:210> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:14> * <var:p> ) = true ) . { rewrite Nat . even_mul . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 