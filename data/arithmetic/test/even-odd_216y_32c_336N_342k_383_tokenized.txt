\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:k> <nat:342> + <var:y> <nat:216> + <nat:383> + <nat:336> <var:N> + <nat:32> <var:c> $ will be odd . \end{theorem} \begin{proof} We prove the expression $ <var:N> * <nat:336> + <var:c> * <nat:32> + <var:y> * <nat:216> + <nat:383> + <var:k> * <nat:342> $ is known as odd from the fact $ <nat:216> \times <var:y> + <var:c> \times <nat:32> + <var:N> \times <nat:336> + <var:k> \times <nat:342> $ is obviously even . Remember that the pair $ <var:N> * <nat:336> $ is an even integer since the product of an even positive integer with some number in $ \mathbb{N} $ is in itself an even natural number . Additionally , see that the product $ <nat:216> * <var:y> $ is trivially even . This is true since the multiplication of an even number and an arbitrary number in $ \mathbb{N} $ must be even . In addition to this , we know the product $ <nat:342> * <var:k> $ is trivially an even element of $ \mathbb{Z}^+ $ from the knowledge the multiplication between an even element of $ \mathbb{N} $ with a element of $ \mathbb{Z}^+ $ is fundamentally an even whole number . Take note $ <nat:32> * <var:c> $ is even . This claim is proven because taking the product of an even whole number and an arbitrary number in $ \mathbb{N} $ is guaranteed to be by itself an even element of $ \mathbb{Z}^+ $ . As a consequence that the totaling of even whole numbers with even positive integers will be even , and likewise by reason that taking the product of an even natural with some element of $ \mathbb{Z}^+ $ must be by its very nature an even element of $ \mathbb{N} $ . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:k> <var:y> <var:N> <var:c> : nat , Nat . odd ( <nat:342> * <var:k> + <nat:216> * <var:y> + <nat:336> * <var:N> + <nat:32> * <var:c> + <nat:383> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:336> * <var:N> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:216> * <var:y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:k> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:32> * <var:c> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:342> * <var:k> + <nat:216> * <var:y> + <nat:336> * <var:N> + <nat:32> * <var:c> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . auto . Qed . 