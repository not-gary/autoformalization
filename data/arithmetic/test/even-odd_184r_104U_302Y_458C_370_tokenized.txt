\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:458> * <var:C> + <nat:184> * <var:r> + <var:U> * <nat:104> + <nat:370> + <nat:302> * <var:Y> $ must be even . \end{theorem} \begin{proof} $ <var:Y> <nat:302> + <var:U> <nat:104> + <nat:184> <var:r> + <var:C> <nat:458> $ is guaranteed to be even . We know that the products $ <nat:302> \times <var:Y> $ and $ <nat:104> \times <var:U> $ must be even . This claim is true since the product of an even natural and any natural is guaranteed to be an even natural . In addition to this , see that the pair $ <nat:458> \times <var:C> $ is trivially even . In addition , we know the pair $ <nat:184> \times <var:r> $ is clearly even . The formula $ <nat:184> <var:r> + <var:U> <nat:104> + <nat:302> <var:Y> + <nat:458> <var:C> $ will be even by reason that the totaling between even whole numbers and an even integer must be obviously an even number in $ \mathbb{Z}^+ $ additionally from the fact multiplying between an even whole number with some integer is guaranteed to be in itself an even element of $ \mathbb{Z}^+ $ . The summing of even positive integers successively and an even number is guaranteed to be instrinsically an even whole number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:C> <var:r> <var:U> <var:Y> : nat , Nat . even ( <nat:458> * <var:C> + <nat:184> * <var:r> + <nat:104> * <var:U> + <nat:302> * <var:Y> + <nat:370> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:302> * <var:Y> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:104> * <var:U> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:C> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:184> * <var:r> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:458> * <var:C> + <nat:184> * <var:r> + <nat:104> * <var:U> + <nat:302> * <var:Y> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . even_add . rewrite <genH> . auto . Qed . 