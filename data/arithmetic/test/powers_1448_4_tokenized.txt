\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \newtheorem{definition}{Definition} \begin{document} \begin{definition} Given that we get $ <var:L> \geq <nat:2> $ so that using $ <var:V> \in \mathbb{N} $ , $ <var:L> ^ <nat:4> = <var:V> $ using some whole number $ <var:L> $ , then we define that $ <var:V> $ is a <def:tesseract> natural number . \end{definition} \begin{theorem} $ <nat:16> = <var:V> $ is a <def:tesseract> . \end{theorem} \begin{proof} Recollect that <nat:16> is a <def:tesseract> natural number granted that <nat:16> is the exponent of whole number $ <var:L> $ taken to the exponent <nat:4> so that $ <var:L> \geq <nat:2> $ . Allow $ <var:L> = <nat:2> $ . Observe $ <nat:2> $ is larger than or equal to <nat:2> . Similarly observe additionally $ <nat:16> = <var:L> ^ <nat:4> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:tesseract> ( <var:V> : nat ) := exists <var:L> : nat , ( <var:L> >= <nat:2> ) /\ ( <var:L> ^ <nat:4> = <var:V> ) . Theorem <genP:1> : <def:tesseract> <nat:16> . Proof . unfold <def:tesseract> . exists <nat:2> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } split . apply <genH> . auto . Qed . 