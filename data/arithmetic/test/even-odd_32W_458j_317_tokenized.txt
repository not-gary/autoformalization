\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} With some $ <var:j> $ , and $ <var:W> \in \mathbb{N} $ $ <nat:32> \cdot <var:W> + <var:j> \cdot <nat:458> + <nat:317> $ is guaranteed to be odd . \end{theorem} \begin{proof} Take note that $ <nat:317> $ is obviously odd . It can be justified that the sum $ <nat:458> \times <var:j> + <var:W> \times <nat:32> + <nat:317> $ is guaranteed to be odd . It can be demonstrated that that the sum $ <nat:317> + <var:j> * <nat:458> + <nat:32> * <var:W> $ is odd because the sum $ <var:W> <nat:32> + <var:j> <nat:458> $ is guaranteed to be even , since the adding of even whole numbers concurrently and an even integer must be even . The integer $ <nat:458> $ is guaranteed to be even Additionally , the whole number $ <nat:32> $ is trivially even additionally from the fact the product between even integers and an arbitrary integer must be an even element of $ \mathbb{Z}^+ $ $ <nat:32> \cdot <var:W> + <nat:458> \cdot <var:j> $ is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:j> <var:W> : nat , Nat . odd ( <nat:32> * <var:W> + <nat:458> * <var:j> + <nat:317> ) = true . Proof . intros . assert ( <genH> : Nat . odd <nat:317> = true ) . { auto . } assert ( <genH> : Nat . even <nat:458> = true ) . { auto . } assert ( <genH> : Nat . even <nat:32> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:32> * <var:W> + <nat:458> * <var:j> ) = true ) . { repeat rewrite Nat . even_add . repeat rewrite Nat . even_mul . rewrite <genH> . rewrite <genH> . auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 