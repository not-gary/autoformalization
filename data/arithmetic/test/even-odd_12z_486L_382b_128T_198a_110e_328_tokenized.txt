\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <var:z> \cdot <nat:12> + <var:L> \cdot <nat:486> + <nat:328> + <var:b> \cdot <nat:382> + <nat:128> \cdot <var:T> + <nat:198> \cdot <var:a> + <nat:110> \cdot <var:e> $ will be even for integers $ <var:z> $ , $ <var:b> $ , $ <var:e> $ , $ <var:T> $ , $ <var:L> $ , and $ <var:a> $ . \end{theorem} \begin{proof} The products $ <var:T> \times <nat:128> $ , $ <nat:12> \times <var:z> $ , in addition to $ <nat:110> \times <var:e> $ are known to be even naturals . This claim must be true because the multiplication of an even positive integer and any natural must be even . We know $ <var:a> \times <nat:198> $ is known to be even . This must be true from the fact taking the product between an even element of $ \mathbb{Z}^+ $ with an arbitrary natural is by definition an even element of $ \mathbb{Z}^+ $ . The product $ <var:b> \times <nat:382> $ is clearly even from the fact the multiplication between an even integer with an arbitrary number in $ \mathbb{Z}^+ $ is fundamentally an even integer . What's more , recall that the product $ <var:L> \times <nat:486> $ is trivially even from the knowledge multiplying between an even integer and an arbitrary element of $ \mathbb{Z}^+ $ must be itself an even number . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:z> <var:b> <var:e> <var:T> <var:L> <var:a> : nat , Nat . even ( <nat:12> * <var:z> + <nat:486> * <var:L> + <nat:382> * <var:b> + <nat:128> * <var:T> + <nat:198> * <var:a> + <nat:110> * <var:e> + <nat:328> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:128> * <var:T> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:12> * <var:z> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:110> * <var:e> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:198> * <var:a> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:382> * <var:b> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:486> * <var:L> ) = true ) . { rewrite Nat . even_mul . auto . } repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . Qed . 