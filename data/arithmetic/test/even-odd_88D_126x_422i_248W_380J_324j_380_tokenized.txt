\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} $ <nat:380> * <var:J> + <nat:380> + <var:D> * <nat:88> + <nat:324> * <var:j> + <nat:126> * <var:x> + <var:W> * <nat:248> + <var:i> * <nat:422> $ will be even with all unknowns $ <var:D> $ , $ <var:J> $ , $ <var:W> $ , $ <var:j> $ , $ <var:i> $ , and $ <var:x> $ . \end{theorem} \begin{proof} It can be verified that $ <var:i> <nat:422> + <nat:126> <var:x> + <nat:248> <var:W> + <nat:324> <var:j> + <nat:88> <var:D> + <var:J> <nat:380> $ is known to be even . Remember the summing of even naturals concurrently with an even positive integer is even . Observe the term $ <var:i> * <nat:422> $ is clearly an even number . Additionally , the terms $ <var:j> * <nat:324> $ , $ <nat:380> * <var:J> $ , plus $ <nat:88> * <var:D> $ are known to be even by reason that taking the product between an even positive integer and some number is guaranteed to be by itself an even number in $ \mathbb{N} $ . $ <nat:248> * <var:W> $ is even . This fact holds because the product of an even number in $ \mathbb{Z}^+ $ with an arbitrary element of $ \mathbb{N} $ must be even . In addition , the term $ <nat:126> * <var:x> $ is known as even since the product of an even natural number and any number in $ \mathbb{Z}^+ $ must be clearly an even element of $ \mathbb{Z}^+ $ . See that $ <nat:380> $ is trivially even . We show the expression $ <nat:88> \times <var:D> + <var:x> \times <nat:126> + <nat:422> \times <var:i> + <var:W> \times <nat:248> + <nat:380> \times <var:J> + <var:j> \times <nat:324> + <nat:380> $ is even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:D> <var:J> <var:W> <var:j> <var:i> <var:x> : nat , Nat . even ( <nat:380> * <var:J> + <nat:88> * <var:D> + <nat:324> * <var:j> + <nat:126> * <var:x> + <nat:248> * <var:W> + <nat:422> * <var:i> + <nat:380> ) = true . Proof . intros . assert ( <genH> : Nat . even ( <nat:422> * <var:i> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:324> * <var:j> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:380> * <var:J> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:88> * <var:D> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:248> * <var:W> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:126> * <var:x> ) = true ) . { rewrite Nat . even_mul . auto . } assert ( <genH> : Nat . even ( <nat:380> * <var:J> + <nat:88> * <var:D> + <nat:324> * <var:j> + <nat:126> * <var:x> + <nat:248> * <var:W> + <nat:422> * <var:i> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:380> = true ) . { auto . } rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . auto . Qed . 