\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Given any $ <var:v> $ , $ <var:p> $ , $ <var:C> $ , $ <var:D> $ , $ <var:T> $ , and $ <var:m> \in \mathbb{Z}^+ $ the expression $ <var:C> <nat:258> + <nat:10> <var:D> + <nat:352> <var:v> + <nat:444> <var:p> + <nat:312> <var:m> + <nat:382> <var:T> + <nat:400> $ must be even . \end{theorem} \begin{proof} Observe that $ <nat:400> $ is guaranteed to be even . The summing of an even natural with an even element of $ \mathbb{N} $ will be even . Observe that the summation $ <var:D> \times <nat:10> + <var:C> \times <nat:258> + <nat:444> \times <var:p> + <nat:400> + <nat:312> \times <var:m> + <var:T> \times <nat:382> + <nat:352> \times <var:v> $ is even since $ <var:C> <nat:258> + <var:p> <nat:444> + <nat:312> <var:m> + <var:D> <nat:10> + <nat:382> <var:T> + <var:v> <nat:352> $ is clearly even . Notice the numbers $ <nat:444> $ , $ <nat:312> $ , and $ <nat:382> $ are known to be even numbers in $ \mathbb{Z}^+ $ . So , the terms $ <var:p> * <nat:444> $ , $ <var:m> * <nat:312> $ , additionally $ <var:T> * <nat:382> $ will be even at the same time . What's more , the pairs $ <var:C> * <nat:258> $ plus $ <nat:352> * <var:v> $ are known to be even . This is true as a consequence that the numbers $ <nat:258> $ and also $ <nat:352> $ are obviously even integers . The number $ <nat:10> $ is trivially an even natural . In consequence , the term $ <nat:10> * <var:D> $ will be even too . The summation $ <var:p> \times <nat:444> + <var:m> \times <nat:312> + <nat:258> \times <var:C> + <nat:352> \times <var:v> + <var:D> \times <nat:10> + <nat:382> \times <var:T> $ will be fundamentally even from the fact the addition of an even number and even integers will be an even positive integer plus since taking the product between an even integer with an arbitrary number in $ \mathbb{Z}^+ $ must be even . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:v> <var:p> <var:C> <var:D> <var:T> <var:m> : nat , Nat . even ( <nat:258> * <var:C> + <nat:10> * <var:D> + <nat:352> * <var:v> + <nat:444> * <var:p> + <nat:312> * <var:m> + <nat:382> * <var:T> + <nat:400> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:400> = true ) . { auto . } rewrite Nat . even_add . assert ( <genH> : Nat . even <nat:444> = true ) . { auto . } assert ( <genH> : Nat . even <nat:312> = true ) . { auto . } assert ( <genH> : Nat . even <nat:382> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:444> * <var:p> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:312> * <var:m> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:382> * <var:T> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:258> = true ) . { auto . } assert ( <genH> : Nat . even <nat:352> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:258> * <var:C> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:352> * <var:v> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:10> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:10> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:258> * <var:C> + <nat:10> * <var:D> + <nat:352> * <var:v> + <nat:444> * <var:p> + <nat:312> * <var:m> + <nat:382> * <var:T> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } rewrite <genH> . rewrite <genH> . auto . Qed . 