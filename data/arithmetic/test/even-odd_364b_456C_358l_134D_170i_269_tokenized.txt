\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} For all integers $ <var:b> $ , $ <var:i> $ , $ <var:C> $ , $ <var:l> $ , and $ <var:D> $ $ <var:i> \cdot <nat:170> + <var:D> \cdot <nat:134> + <nat:364> \cdot <var:b> + <nat:358> \cdot <var:l> + <nat:269> + <nat:456> \cdot <var:C> $ is odd . \end{theorem} \begin{proof} It can be verified that that the summation $ <var:D> * <nat:134> + <nat:358> * <var:l> + <nat:170> * <var:i> + <var:C> * <nat:456> + <nat:364> * <var:b> $ is even . The terms $ <var:i> * <nat:170> $ and moreover $ <nat:456> * <var:C> $ are obviously even from the fact multiplying of an even element of $ \mathbb{N} $ and some number in $ \mathbb{N} $ must be by its very nature an even number in $ \mathbb{Z}^+ $ , and holds from the knowledge the coefficients $ <nat:170> $ and also $ <nat:456> $ are known to be . In addition to this , take note the positive integer $ <nat:358> $ is known as an even element of $ \mathbb{Z}^+ $ . Consequently , the product $ <nat:358> * <var:l> $ is guaranteed to be even in addition . Building on , take note that the natural number $ <nat:364> $ is known as even . As a consequence , $ <nat:364> * <var:b> $ is even too . What's more , the pair $ <nat:134> * <var:D> $ is guaranteed to be even from the knowledge the multiplication of an even natural with any integer is guaranteed to be an even whole number . This claim is true by reason that the coefficient $ <nat:134> $ is known as even . We show the main result holds . Notice that $ <nat:269> $ is guaranteed to be odd . \end{proof} \end{document} 
~
Require Import Arith . Theorem <genP:1> : forall <var:b> <var:i> <var:C> <var:l> <var:D> : nat , Nat . odd ( <nat:170> * <var:i> + <nat:134> * <var:D> + <nat:364> * <var:b> + <nat:358> * <var:l> + <nat:456> * <var:C> + <nat:269> ) = true . Proof . intros . assert ( <genH> : Nat . even <nat:170> = true ) . { auto . } assert ( <genH> : Nat . even <nat:456> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:170> * <var:i> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:456> * <var:C> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:358> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:358> * <var:l> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:364> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:364> * <var:b> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even <nat:134> = true ) . { auto . } assert ( <genH> : Nat . even ( <nat:134> * <var:D> ) = true ) . { rewrite Nat . even_mul . rewrite <genH> . auto . } assert ( <genH> : Nat . even ( <nat:170> * <var:i> + <nat:134> * <var:D> + <nat:364> * <var:b> + <nat:358> * <var:l> + <nat:456> * <var:C> ) = true ) . { repeat rewrite Nat . even_add . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . rewrite <genH> . auto . } assert ( <genH> : Nat . odd <nat:269> = true ) . { auto . } rewrite Nat . odd_add . rewrite <- Nat . negb_even . rewrite <genH> . rewrite <genH> . auto . Qed . 