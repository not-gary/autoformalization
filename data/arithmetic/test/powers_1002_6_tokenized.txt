\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \begin{document} \begin{theorem} Given that we have so that $ <var:t> > <nat:1> $ so that assuming some $ <var:x> $ , $ <var:t> ^ <nat:6> = <var:x> $ , then take as given that $ <var:x> $ is a <def:hexeract> number assuming some \in \mathbb{N} $ . Consequently the natural number <nat:64> is a <def:hexeract> natural number . \end{theorem} \begin{proof} Remember a <def:hexeract> number is the repeated multiplication of natural number $ <var:t> $ repeatedly multiplied to the exponential <nat:6> so that $ <var:t> > <nat:1> $ . Note $ <nat:2> $ is larger than <nat:1> . Moreover observe that further $ <nat:64> = <nat:2> ^ <nat:6> $ . \end{proof} \end{document} 
~
Require Import Arith . Require Import Lia . Definition <def:hexeract> ( <var:x> : nat ) := exists <var:t> : nat , ( <var:t> > <nat:1> ) /\ ( <var:t> ^ <nat:6> = <var:x> ) . Theorem <genP:1> : <def:hexeract> <nat:64> . Proof . unfold <def:hexeract> . assert ( <genH> : <nat:2> > <nat:1> ) . { lia . } exists <nat:2> . split . apply <genH> . auto . Qed . 