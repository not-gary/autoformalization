\documentclass[12pt]{article} \usepackage{amsthm,amsfonts,amssymb} \newtheorem{theorem}{Theorem} \newtheorem{definition}{Definition} \begin{document} \begin{definition} Iff we have $ <var:g> = <var:e> * <var:q> $ given $ <var:g> \in \mathbb{N} $ plus satisfying the condition that $ <var:q> $ and $ <var:e> \geq <nat:2> $ , then take that $ <var:g> $ is <def:two-composite> with some whole numbers $ <var:e> $ , $ <var:q> $ . \end{definition} \begin{theorem} The number <nat:8> is a <def:two-composite> natural number . \end{theorem} \begin{proof} Remember a <def:two-composite> positive integer is the multiplication between $ <var:q> $ and $ <var:e> $ so that $ <var:e> $ and $ <var:q> \geq <nat:2> $ . We justify <nat:8> is a <def:two-composite> whole number . We prove <nat:8> is a <def:two-composite> natural number because $ <nat:2> $ , and $ <nat:4> \geq <nat:2> $ plus seeing that $ <nat:8> = <nat:2> * <nat:4> $ . \end{proof} \end{document} 
~
Require Import Lia . Definition <def:two-composite> ( <var:g> : nat ) := exists <var:e> <var:q> : nat , ( <var:g> = <var:e> * <var:q> ) /\ ( <var:q> >= <nat:2> ) /\ ( <var:e> >= <nat:2> ) . Theorem <genP:1> : <def:two-composite> <nat:8> . Proof . unfold <def:two-composite> . assert ( <genH> : <nat:2> >= <nat:2> ) . { lia . } assert ( <genH> : <nat:4> >= <nat:2> ) . { lia . } exists <nat:4> . exists <nat:2> . repeat split . all : try apply <genH> . all : try apply <genH> . Qed . 