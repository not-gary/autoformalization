\begin{document} \begin{definition} We define a code <def:poly> in \emph{Imp} as \begin{verbatim} <var:L> := <nat:3> ; <var:L> := <nat:2> + <var:L> * <var:W> ; <var:L> := <nat:1> + <var:L> * <var:W> ; <var:L> := <nat:4> + <var:L> * <var:W> ; <var:L> := <nat:4> + <var:L> * <var:W> ; <var:L> := <nat:3> + <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <nat:2> + <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <nat:2> + <var:L> * <var:W> \end{verbatim} \end{definition} \begin{theorem} Let the following set of instructions <def:poly> . Assuming for some natural numbers $ <var:O> $ it to hold that $ <var:W> = <var:O> $ before evaluating our set of commands , it follows that we see $ <var:L> = <nat:3> \cdot <var:O> ^ <nat:10> + <nat:2> \cdot <var:O> ^ <nat:9> + <nat:1> \cdot <var:O> ^ <nat:8> + <nat:4> \cdot <var:O> ^ <nat:7> + <nat:4> \cdot <var:O> ^ <nat:6> + <nat:3> \cdot <var:O> ^ <nat:5> + <nat:2> \cdot <var:O> ^ <nat:2> + <nat:2> $ once program has executed . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:W> = <var:O> \} \\ <var:L> := <nat:3> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \} \\ <var:L> := <nat:2> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> + <nat:2> \} \\ <var:L> := <nat:1> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:2> + <nat:2> \times <var:O> + <nat:1> \} \\ <var:L> := <nat:4> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:3> + <nat:2> \times <var:O> ^ <nat:2> + <nat:1> \times <var:O> + <nat:4> \} \\ <var:L> := <nat:4> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:4> + <nat:2> \times <var:O> ^ <nat:3> + <nat:1> \times <var:O> ^ <nat:2> + <nat:4> \times <var:O> + <nat:4> \} \\ <var:L> := <nat:3> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:5> + <nat:2> \times <var:O> ^ <nat:4> + <nat:1> \times <var:O> ^ <nat:3> + <nat:4> \times <var:O> ^ <nat:2> + <nat:4> \times <var:O> + <nat:3> \} \\ <var:L> := <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:6> + <nat:2> \times <var:O> ^ <nat:5> + <nat:1> \times <var:O> ^ <nat:4> + <nat:4> \times <var:O> ^ <nat:3> + <nat:4> \times <var:O> ^ <nat:2> + <nat:3> \times <var:O> + \} \\ <var:L> := <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:7> + <nat:2> \times <var:O> ^ <nat:6> + <nat:1> \times <var:O> ^ <nat:5> + <nat:4> \times <var:O> ^ <nat:4> + <nat:4> \times <var:O> ^ <nat:3> + <nat:3> \times <var:O> ^ <nat:2> + \} \\ <var:L> := <nat:2> + <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:8> + <nat:2> \times <var:O> ^ <nat:7> + <nat:1> \times <var:O> ^ <nat:6> + <nat:4> \times <var:O> ^ <nat:5> + <nat:4> \times <var:O> ^ <nat:4> + <nat:3> \times <var:O> ^ <nat:3> + <nat:2> \} \\ <var:L> := <var:L> * <var:W> ; \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:9> + <nat:2> \times <var:O> ^ <nat:8> + <nat:1> \times <var:O> ^ <nat:7> + <nat:4> \times <var:O> ^ <nat:6> + <nat:4> \times <var:O> ^ <nat:5> + <nat:3> \times <var:O> ^ <nat:4> + <nat:2> \times <var:O> + \} \\ <var:L> := <nat:2> + <var:L> * <var:W> \\ \{ <var:W> = <var:O> \land <var:L> = <nat:3> \times <var:O> ^ <nat:10> + <nat:2> \times <var:O> ^ <nat:9> + <nat:1> \times <var:O> ^ <nat:8> + <nat:4> \times <var:O> ^ <nat:7> + <nat:4> \times <var:O> ^ <nat:6> + <nat:3> \times <var:O> ^ <nat:5> + <nat:2> \times <var:O> ^ <nat:2> + <nat:2> \} \end{eqnarray} Applying usual Hoare techniques finalizes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:L> := <nat:3> ; <var:L> := <nat:2> + <var:L> * <var:W> ; <var:L> := <nat:1> + <var:L> * <var:W> ; <var:L> := <nat:4> + <var:L> * <var:W> ; <var:L> := <nat:4> + <var:L> * <var:W> ; <var:L> := <nat:3> + <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <nat:2> + <var:L> * <var:W> ; <var:L> := <var:L> * <var:W> ; <var:L> := <nat:2> + <var:L> * <var:W> . Theorem <genH:poly_code_correct> : forall <var:O> : nat , {{ <var:W> = <var:O> }} <def:poly> {{ <var:L> = <nat:3> * <var:O> ^ <nat:10> + <nat:2> * <var:O> ^ <nat:9> + <nat:1> * <var:O> ^ <nat:8> + <nat:4> * <var:O> ^ <nat:7> + <nat:4> * <var:O> ^ <nat:6> + <nat:3> * <var:O> ^ <nat:5> + <nat:2> * <var:O> ^ <nat:2> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:2> + <nat:2> * <var:O> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:3> + <nat:2> * <var:O> ^ <nat:2> + <nat:1> * <var:O> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:4> + <nat:2> * <var:O> ^ <nat:3> + <nat:1> * <var:O> ^ <nat:2> + <nat:4> * <var:O> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:5> + <nat:2> * <var:O> ^ <nat:4> + <nat:1> * <var:O> ^ <nat:3> + <nat:4> * <var:O> ^ <nat:2> + <nat:4> * <var:O> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:6> + <nat:2> * <var:O> ^ <nat:5> + <nat:1> * <var:O> ^ <nat:4> + <nat:4> * <var:O> ^ <nat:3> + <nat:4> * <var:O> ^ <nat:2> + <nat:3> * <var:O> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:7> + <nat:2> * <var:O> ^ <nat:6> + <nat:1> * <var:O> ^ <nat:5> + <nat:4> * <var:O> ^ <nat:4> + <nat:4> * <var:O> ^ <nat:3> + <nat:3> * <var:O> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:8> + <nat:2> * <var:O> ^ <nat:7> + <nat:1> * <var:O> ^ <nat:6> + <nat:4> * <var:O> ^ <nat:5> + <nat:4> * <var:O> ^ <nat:4> + <nat:3> * <var:O> ^ <nat:3> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:9> + <nat:2> * <var:O> ^ <nat:8> + <nat:1> * <var:O> ^ <nat:7> + <nat:4> * <var:O> ^ <nat:6> + <nat:4> * <var:O> ^ <nat:5> + <nat:3> * <var:O> ^ <nat:4> + <nat:2> * <var:O> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:O> /\ <var:L> = <nat:3> * <var:O> ^ <nat:10> + <nat:2> * <var:O> ^ <nat:9> + <nat:1> * <var:O> ^ <nat:8> + <nat:4> * <var:O> ^ <nat:7> + <nat:4> * <var:O> ^ <nat:6> + <nat:3> * <var:O> ^ <nat:5> + <nat:2> * <var:O> ^ <nat:2> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 