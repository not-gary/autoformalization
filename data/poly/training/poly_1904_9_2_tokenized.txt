\begin{document} \begin{definition} Take the following series of instructions <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:x> := <nat:2> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:3> + <var:x> * <var:s> ; <var:x> := <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:3> + <var:x> * <var:s> \end{verbatim} \end{definition} \begin{theorem} Consider a code <def:poly> . Assume it to be that $ <var:s> = <var:i> $ ahead of evaluating our series of instructions , then $ <var:x> = <nat:2> \times <var:i> ^ <nat:8> + <nat:4> \times <var:i> ^ <nat:7> + <nat:4> \times <var:i> ^ <nat:6> + <nat:4> \times <var:i> ^ <nat:5> + <nat:4> \times <var:i> ^ <nat:4> + <nat:3> \times <var:i> ^ <nat:3> + <nat:4> \times <var:i> + <nat:3> $ . \end{theorem} \begin{proof} Applying typical Hoare logic results in : \begin{eqnarray} \{ <var:s> = <var:i> \} \\ <var:x> := <nat:2> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \} \\ <var:x> := <nat:4> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> + <nat:4> \} \\ <var:x> := <nat:4> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:2> + <nat:4> \cdot <var:i> + <nat:4> \} \\ <var:x> := <nat:4> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:3> + <nat:4> \cdot <var:i> ^ <nat:2> + <nat:4> \cdot <var:i> + <nat:4> \} \\ <var:x> := <nat:4> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:4> + <nat:4> \cdot <var:i> ^ <nat:3> + <nat:4> \cdot <var:i> ^ <nat:2> + <nat:4> \cdot <var:i> + <nat:4> \} \\ <var:x> := <nat:3> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:5> + <nat:4> \cdot <var:i> ^ <nat:4> + <nat:4> \cdot <var:i> ^ <nat:3> + <nat:4> \cdot <var:i> ^ <nat:2> + <nat:4> \cdot <var:i> + <nat:3> \} \\ <var:x> := <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:6> + <nat:4> \cdot <var:i> ^ <nat:5> + <nat:4> \cdot <var:i> ^ <nat:4> + <nat:4> \cdot <var:i> ^ <nat:3> + <nat:4> \cdot <var:i> ^ <nat:2> + <nat:3> \cdot <var:i> + \} \\ <var:x> := <nat:4> + <var:x> * <var:s> ; \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:7> + <nat:4> \cdot <var:i> ^ <nat:6> + <nat:4> \cdot <var:i> ^ <nat:5> + <nat:4> \cdot <var:i> ^ <nat:4> + <nat:4> \cdot <var:i> ^ <nat:3> + <nat:3> \cdot <var:i> ^ <nat:2> + <nat:4> \} \\ <var:x> := <nat:3> + <var:x> * <var:s> \\ \{ <var:s> = <var:i> \land <var:x> = <nat:2> \cdot <var:i> ^ <nat:8> + <nat:4> \cdot <var:i> ^ <nat:7> + <nat:4> \cdot <var:i> ^ <nat:6> + <nat:4> \cdot <var:i> ^ <nat:5> + <nat:4> \cdot <var:i> ^ <nat:4> + <nat:3> \cdot <var:i> ^ <nat:3> + <nat:4> \cdot <var:i> + <nat:3> \} \end{eqnarray} Continuing executing the series of commands produces : \begin{eqnarray} \end{eqnarray} Considering the preceding argument , our list of instructions is demonstrated to be correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:x> := <nat:2> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:3> + <var:x> * <var:s> ; <var:x> := <var:x> * <var:s> ; <var:x> := <nat:4> + <var:x> * <var:s> ; <var:x> := <nat:3> + <var:x> * <var:s> . Theorem <genH:poly_code_correct> : forall <var:i> : nat , {{ <var:s> = <var:i> }} <def:poly> {{ <var:x> = <nat:2> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:3> * <var:i> ^ <nat:3> + <nat:4> * <var:i> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:3> * <var:i> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:i> /\ <var:x> = <nat:2> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:3> * <var:i> ^ <nat:3> + <nat:4> * <var:i> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 