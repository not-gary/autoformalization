\begin{document} \begin{theorem} Define the following code as \begin{verbatim} <var:n> := <nat:2> ; <var:n> := <nat:1> + <var:n> * <var:e> ; <var:n> := <nat:3> + <var:n> * <var:e> ; <var:n> := <nat:4> + <var:n> * <var:e> ; <var:n> := <nat:3> + <var:n> * <var:e> ; <var:n> := <nat:2> + <var:n> * <var:e> ; <var:n> := <nat:2> + <var:n> * <var:e> ; <var:n> := <nat:1> + <var:n> * <var:e> ; <var:n> := <nat:4> + <var:n> * <var:e> \end{verbatim} Allowing that $ <var:e> = <var:D> $ ahead of evaluating this set of commands , it must be that we have $ <var:n> = <nat:2> \cdot <var:D> ^ <nat:8> + <nat:1> \cdot <var:D> ^ <nat:7> + <nat:3> \cdot <var:D> ^ <nat:6> + <nat:4> \cdot <var:D> ^ <nat:5> + <nat:3> \cdot <var:D> ^ <nat:4> + <nat:2> \cdot <var:D> ^ <nat:3> + <nat:2> \cdot <var:D> ^ <nat:2> + <nat:1> \cdot <var:D> + <nat:4> $ . \end{theorem} \begin{proof} The application of typical Hoare reasoning gives : \begin{eqnarray} \{ <var:e> = <var:D> \} \\ <var:n> := <nat:2> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> \} \\ <var:n> := <nat:1> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> + <nat:1> \} \\ <var:n> := <nat:3> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:3> \} \\ <var:n> := <nat:4> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:4> \} \\ <var:n> := <nat:3> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:3> \} \\ <var:n> := <nat:2> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> \} \\ <var:n> := <nat:2> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:2> \} \\ <var:n> := <nat:1> + <var:n> * <var:e> ; \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:4> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:1> \} \\ <var:n> := <nat:4> + <var:n> * <var:e> \\ \{ <var:e> = <var:D> \land <var:n> = <nat:2> * <var:D> ^ <nat:8> + <nat:1> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:6> + <nat:4> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:4> \} \end{eqnarray} Thusly , our set of commands is shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:D> : nat , {{ <var:e> = <var:D> }} <var:n> := <nat:2> ; <var:n> := <nat:1> + <var:n> * <var:e> ; <var:n> := <nat:3> + <var:n> * <var:e> ; <var:n> := <nat:4> + <var:n> * <var:e> ; <var:n> := <nat:3> + <var:n> * <var:e> ; <var:n> := <nat:2> + <var:n> * <var:e> ; <var:n> := <nat:2> + <var:n> * <var:e> ; <var:n> := <nat:1> + <var:n> * <var:e> ; <var:n> := <nat:4> + <var:n> * <var:e> {{ <var:n> = <nat:2> * <var:D> ^ <nat:8> + <nat:1> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:6> + <nat:4> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:4> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:D> /\ <var:n> = <nat:2> * <var:D> ^ <nat:8> + <nat:1> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:6> + <nat:4> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 