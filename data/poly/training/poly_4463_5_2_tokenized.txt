\begin{document} \begin{definition} Define a program <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:D> := <nat:4> ; <var:D> := <nat:3> + <var:D> * <var:w> ; <var:D> := <nat:2> + <var:D> * <var:w> ; <var:D> := <nat:1> + <var:D> * <var:w> ; <var:D> := <nat:4> + <var:D> * <var:w> \end{verbatim} \end{definition} \begin{theorem} Given ( letting positive coefficients $ <var:e> $ ) it to hold that $ <var:w> = <var:e> $ prior to evaluating our code segment it follows that $ <var:D> = <nat:4> \cdot <var:e> ^ <nat:4> + <nat:3> \cdot <var:e> ^ <nat:3> + <nat:2> \cdot <var:e> ^ <nat:2> + <nat:1> \cdot <var:e> + <nat:4> $ when series of instructions finishes . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:w> = <var:e> \} \\ <var:D> := <nat:4> ; \\ \{ <var:w> = <var:e> \land <var:D> = <nat:4> \} \\ <var:D> := <nat:3> + <var:D> * <var:w> ; \\ \{ <var:w> = <var:e> \land <var:D> = <nat:4> * <var:e> + <nat:3> \} \\ <var:D> := <nat:2> + <var:D> * <var:w> ; \\ \{ <var:w> = <var:e> \land <var:D> = <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:2> \} \\ <var:D> := <nat:1> + <var:D> * <var:w> ; \\ \{ <var:w> = <var:e> \land <var:D> = <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:1> \} \\ <var:D> := <nat:4> + <var:D> * <var:w> \\ \{ <var:w> = <var:e> \land <var:D> = <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:4> \} \end{eqnarray} Applying Hoare techniques completes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:D> := <nat:4> ; <var:D> := <nat:3> + <var:D> * <var:w> ; <var:D> := <nat:2> + <var:D> * <var:w> ; <var:D> := <nat:1> + <var:D> * <var:w> ; <var:D> := <nat:4> + <var:D> * <var:w> . Theorem <genH:poly_code_correct> : forall <var:e> : nat , {{ <var:w> = <var:e> }} <def:poly> {{ <var:D> = <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:w> = <var:e> /\ <var:D> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:w> = <var:e> /\ <var:D> = <nat:4> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:w> = <var:e> /\ <var:D> = <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:w> = <var:e> /\ <var:D> = <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:w> = <var:e> /\ <var:D> = <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 