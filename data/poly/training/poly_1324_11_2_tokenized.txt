\begin{document} \begin{definition} Let the following program <def:poly> in \emph{Imp} as \begin{verbatim} <var:q> := <nat:1> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:1> + <var:q> * <var:A> \end{verbatim} \end{definition} \begin{theorem} Take a list of commands <def:poly> in \emph{Imp} . Allow that we have $ <var:A> = <var:R> $ prior to running the segment of code it follows that we have $ <var:q> = <nat:1> \cdot <var:R> ^ <nat:10> + <nat:4> \cdot <var:R> ^ <nat:9> + <nat:4> \cdot <var:R> ^ <nat:8> + <nat:4> \cdot <var:R> ^ <nat:7> + <nat:4> \cdot <var:R> ^ <nat:4> + <nat:4> \cdot <var:R> ^ <nat:3> + <nat:4> \cdot <var:R> + <nat:1> $ . \end{theorem} \begin{proof} Utilizing typical Hoare logic results in : \begin{eqnarray} \{ <var:A> = <var:R> \} \\ <var:q> := <nat:1> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> + <nat:4> \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:2> + <nat:4> \times <var:R> + <nat:4> \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> ^ <nat:2> + <nat:4> \times <var:R> + <nat:4> \} \\ <var:q> := <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> ^ <nat:2> + <nat:4> \times <var:R> + \} \\ <var:q> := <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:5> + <nat:4> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> ^ <nat:2> + \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:6> + <nat:4> \times <var:R> ^ <nat:5> + <nat:4> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:7> + <nat:4> \times <var:R> ^ <nat:6> + <nat:4> \times <var:R> ^ <nat:5> + <nat:4> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> + <nat:4> \} \\ <var:q> := <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:8> + <nat:4> \times <var:R> ^ <nat:7> + <nat:4> \times <var:R> ^ <nat:6> + <nat:4> \times <var:R> ^ <nat:5> + <nat:4> \times <var:R> ^ <nat:2> + <nat:4> \times <var:R> + \} \\ <var:q> := <nat:4> + <var:q> * <var:A> ; \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:9> + <nat:4> \times <var:R> ^ <nat:8> + <nat:4> \times <var:R> ^ <nat:7> + <nat:4> \times <var:R> ^ <nat:6> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> ^ <nat:2> + <nat:4> \} \\ <var:q> := <nat:1> + <var:q> * <var:A> \\ \{ <var:A> = <var:R> \land <var:q> = <nat:1> \times <var:R> ^ <nat:10> + <nat:4> \times <var:R> ^ <nat:9> + <nat:4> \times <var:R> ^ <nat:8> + <nat:4> \times <var:R> ^ <nat:7> + <nat:4> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> + <nat:1> \} \end{eqnarray} Thus , this series of instructions is validated as correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:q> := <nat:1> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <var:q> * <var:A> ; <var:q> := <nat:4> + <var:q> * <var:A> ; <var:q> := <nat:1> + <var:q> * <var:A> . Theorem <genH:poly_code_correct> : forall <var:R> : nat , {{ <var:A> = <var:R> }} <def:poly> {{ <var:q> = <nat:1> * <var:R> ^ <nat:10> + <nat:4> * <var:R> ^ <nat:9> + <nat:4> * <var:R> ^ <nat:8> + <nat:4> * <var:R> ^ <nat:7> + <nat:4> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:2> + <nat:4> * <var:R> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + <nat:4> * <var:R> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + <nat:4> * <var:R> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:6> + <nat:4> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:7> + <nat:4> * <var:R> ^ <nat:6> + <nat:4> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:4> + <nat:4> * <var:R> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:8> + <nat:4> * <var:R> ^ <nat:7> + <nat:4> * <var:R> ^ <nat:6> + <nat:4> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:2> + <nat:4> * <var:R> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:9> + <nat:4> * <var:R> ^ <nat:8> + <nat:4> * <var:R> ^ <nat:7> + <nat:4> * <var:R> ^ <nat:6> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:R> /\ <var:q> = <nat:1> * <var:R> ^ <nat:10> + <nat:4> * <var:R> ^ <nat:9> + <nat:4> * <var:R> ^ <nat:8> + <nat:4> * <var:R> ^ <nat:7> + <nat:4> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 