\begin{document} \begin{theorem} Define a set of commands in \emph{Imp} : \begin{verbatim} <var:Y> := <nat:4> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:2> + <var:Y> * <var:Z> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:1> + <var:Y> * <var:Z> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:2> + <var:Y> * <var:Z> \end{verbatim} Allowing it to hold that $ <var:Z> = <var:C> $ before interpreting this series of instructions it holds that we have $ <var:Y> = <nat:4> * <var:C> ^ <nat:6> + <nat:3> * <var:C> ^ <nat:5> + <nat:2> * <var:C> ^ <nat:4> + <nat:3> * <var:C> ^ <nat:3> + <nat:1> * <var:C> ^ <nat:2> + <nat:3> * <var:C> + <nat:2> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:Z> = <var:C> \} \\ <var:Y> := <nat:4> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \} \\ <var:Y> := <nat:3> + <var:Y> * <var:Z> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> + <nat:3> \} \\ <var:Y> := <nat:2> + <var:Y> * <var:Z> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> ^ <nat:2> + <nat:3> \times <var:C> + <nat:2> \} \\ <var:Y> := <nat:3> + <var:Y> * <var:Z> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> ^ <nat:3> + <nat:3> \times <var:C> ^ <nat:2> + <nat:2> \times <var:C> + <nat:3> \} \\ <var:Y> := <nat:1> + <var:Y> * <var:Z> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> ^ <nat:4> + <nat:3> \times <var:C> ^ <nat:3> + <nat:2> \times <var:C> ^ <nat:2> + <nat:3> \times <var:C> + <nat:1> \} \\ <var:Y> := <nat:3> + <var:Y> * <var:Z> ; \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> ^ <nat:5> + <nat:3> \times <var:C> ^ <nat:4> + <nat:2> \times <var:C> ^ <nat:3> + <nat:3> \times <var:C> ^ <nat:2> + <nat:1> \times <var:C> + <nat:3> \} \\ <var:Y> := <nat:2> + <var:Y> * <var:Z> \\ \{ <var:Z> = <var:C> \land <var:Y> = <nat:4> \times <var:C> ^ <nat:6> + <nat:3> \times <var:C> ^ <nat:5> + <nat:2> \times <var:C> ^ <nat:4> + <nat:3> \times <var:C> ^ <nat:3> + <nat:1> \times <var:C> ^ <nat:2> + <nat:3> \times <var:C> + <nat:2> \} \end{eqnarray} Utilizing typical Hoare logic completes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:C> : nat , {{ <var:Z> = <var:C> }} <var:Y> := <nat:4> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:2> + <var:Y> * <var:Z> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:1> + <var:Y> * <var:Z> ; <var:Y> := <nat:3> + <var:Y> * <var:Z> ; <var:Y> := <nat:2> + <var:Y> * <var:Z> {{ <var:Y> = <nat:4> * <var:C> ^ <nat:6> + <nat:3> * <var:C> ^ <nat:5> + <nat:2> * <var:C> ^ <nat:4> + <nat:3> * <var:C> ^ <nat:3> + <nat:1> * <var:C> ^ <nat:2> + <nat:3> * <var:C> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> ^ <nat:2> + <nat:3> * <var:C> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> ^ <nat:3> + <nat:3> * <var:C> ^ <nat:2> + <nat:2> * <var:C> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> ^ <nat:4> + <nat:3> * <var:C> ^ <nat:3> + <nat:2> * <var:C> ^ <nat:2> + <nat:3> * <var:C> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> ^ <nat:5> + <nat:3> * <var:C> ^ <nat:4> + <nat:2> * <var:C> ^ <nat:3> + <nat:3> * <var:C> ^ <nat:2> + <nat:1> * <var:C> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:C> /\ <var:Y> = <nat:4> * <var:C> ^ <nat:6> + <nat:3> * <var:C> ^ <nat:5> + <nat:2> * <var:C> ^ <nat:4> + <nat:3> * <var:C> ^ <nat:3> + <nat:1> * <var:C> ^ <nat:2> + <nat:3> * <var:C> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 