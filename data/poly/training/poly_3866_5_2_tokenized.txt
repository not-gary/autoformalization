\begin{document} \begin{definition} Define the following code <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:X> := <nat:4> ; <var:X> := <nat:4> + <var:X> * <var:D> ; <var:X> := <nat:3> + <var:X> * <var:D> ; <var:X> := <nat:3> + <var:X> * <var:D> ; <var:X> := <nat:4> + <var:X> * <var:D> \end{verbatim} \end{definition} \begin{theorem} Assuming that $ <var:D> = <var:V> $ ahead of the execution of the set of instructions then $ <var:X> = <nat:4> \times <var:V> ^ <nat:4> + <nat:4> \times <var:V> ^ <nat:3> + <nat:3> \times <var:V> ^ <nat:2> + <nat:3> \times <var:V> + <nat:4> $ when list of instructions executes . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:D> = <var:V> \} \\ <var:X> := <nat:4> ; \\ \{ <var:D> = <var:V> \land <var:X> = <nat:4> \} \\ <var:X> := <nat:4> + <var:X> * <var:D> ; \\ \{ <var:D> = <var:V> \land <var:X> = <nat:4> \times <var:V> + <nat:4> \} \\ <var:X> := <nat:3> + <var:X> * <var:D> ; \\ \{ <var:D> = <var:V> \land <var:X> = <nat:4> \times <var:V> ^ <nat:2> + <nat:4> \times <var:V> + <nat:3> \} \\ <var:X> := <nat:3> + <var:X> * <var:D> ; \\ \{ <var:D> = <var:V> \land <var:X> = <nat:4> \times <var:V> ^ <nat:3> + <nat:4> \times <var:V> ^ <nat:2> + <nat:3> \times <var:V> + <nat:3> \} \\ <var:X> := <nat:4> + <var:X> * <var:D> \\ \{ <var:D> = <var:V> \land <var:X> = <nat:4> \times <var:V> ^ <nat:4> + <nat:4> \times <var:V> ^ <nat:3> + <nat:3> \times <var:V> ^ <nat:2> + <nat:3> \times <var:V> + <nat:4> \} \end{eqnarray} Utilizing Hoare reasoning solves the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:X> := <nat:4> ; <var:X> := <nat:4> + <var:X> * <var:D> ; <var:X> := <nat:3> + <var:X> * <var:D> ; <var:X> := <nat:3> + <var:X> * <var:D> ; <var:X> := <nat:4> + <var:X> * <var:D> . Theorem <genH:poly_code_correct> : forall <var:V> : nat , {{ <var:D> = <var:V> }} <def:poly> {{ <var:X> = <nat:4> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:D> = <var:V> /\ <var:X> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:V> /\ <var:X> = <nat:4> * <var:V> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:V> /\ <var:X> = <nat:4> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:V> /\ <var:X> = <nat:4> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:V> /\ <var:X> = <nat:4> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 