\begin{document} \begin{theorem} Consider the following list of instructions such that \begin{verbatim} <var:v> := <nat:1> ; <var:v> := <nat:3> + <var:v> * <var:Q> ; <var:v> := <nat:4> + <var:v> * <var:Q> ; <var:v> := <nat:3> + <var:v> * <var:Q> ; <var:v> := <nat:2> + <var:v> * <var:Q> ; <var:v> := <var:v> * <var:Q> ; <var:v> := <nat:4> + <var:v> * <var:Q> \end{verbatim} Given $ <var:Q> = <var:S> $ prior to the execution of this program it follows that we have $ <var:v> = <nat:1> \cdot <var:S> ^ <nat:6> + <nat:3> \cdot <var:S> ^ <nat:5> + <nat:4> \cdot <var:S> ^ <nat:4> + <nat:3> \cdot <var:S> ^ <nat:3> + <nat:2> \cdot <var:S> ^ <nat:2> + <nat:4> $ . \end{theorem} \begin{proof} Utilizing standard Hoare logic yields : \begin{eqnarray} \{ <var:Q> = <var:S> \} \\ <var:v> := <nat:1> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \} \\ <var:v> := <nat:3> + <var:v> * <var:Q> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> + <nat:3> \} \\ <var:v> := <nat:4> + <var:v> * <var:Q> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:4> \} \\ <var:v> := <nat:3> + <var:v> * <var:Q> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:4> \times <var:S> + <nat:3> \} \\ <var:v> := <nat:2> + <var:v> * <var:Q> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:2> \} \\ <var:v> := <var:v> * <var:Q> ; \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> ^ <nat:5> + <nat:3> \times <var:S> ^ <nat:4> + <nat:4> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:2> \times <var:S> + \} \\ <var:v> := <nat:4> + <var:v> * <var:Q> \\ \{ <var:Q> = <var:S> \land <var:v> = <nat:1> \times <var:S> ^ <nat:6> + <nat:3> \times <var:S> ^ <nat:5> + <nat:4> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:2> \times <var:S> ^ <nat:2> + <nat:4> \} \end{eqnarray} Hence , this code is shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:S> : nat , {{ <var:Q> = <var:S> }} <var:v> := <nat:1> ; <var:v> := <nat:3> + <var:v> * <var:Q> ; <var:v> := <nat:4> + <var:v> * <var:Q> ; <var:v> := <nat:3> + <var:v> * <var:Q> ; <var:v> := <nat:2> + <var:v> * <var:Q> ; <var:v> := <var:v> * <var:Q> ; <var:v> := <nat:4> + <var:v> * <var:Q> {{ <var:v> = <nat:1> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:2> * <var:S> ^ <nat:2> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> ^ <nat:5> + <nat:3> * <var:S> ^ <nat:4> + <nat:4> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:2> * <var:S> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:S> /\ <var:v> = <nat:1> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:2> * <var:S> ^ <nat:2> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 