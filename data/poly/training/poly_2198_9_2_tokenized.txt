\begin{document} \begin{definition} Take the following code segment <def:poly> in \emph{Imp} as \begin{verbatim} <var:L> := <nat:3> ; <var:L> := <nat:3> + <var:L> * <var:J> ; <var:L> := <nat:4> + <var:L> * <var:J> ; <var:L> := <nat:4> + <var:L> * <var:J> ; <var:L> := <nat:1> + <var:L> * <var:J> ; <var:L> := <var:L> * <var:J> ; <var:L> := <nat:2> + <var:L> * <var:J> ; <var:L> := <nat:3> + <var:L> * <var:J> ; <var:L> := <nat:1> + <var:L> * <var:J> \end{verbatim} \end{definition} \begin{theorem} Allowing it to hold that $ <var:J> = <var:r> $ ahead of evaluating this list of commands , it must be that we see $ <var:L> = <nat:3> \times <var:r> ^ <nat:8> + <nat:3> \times <var:r> ^ <nat:7> + <nat:4> \times <var:r> ^ <nat:6> + <nat:4> \times <var:r> ^ <nat:5> + <nat:1> \times <var:r> ^ <nat:4> + <nat:2> \times <var:r> ^ <nat:2> + <nat:3> \times <var:r> + <nat:1> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:J> = <var:r> \} \\ <var:L> := <nat:3> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \} \\ <var:L> := <nat:3> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> + <nat:3> \} \\ <var:L> := <nat:4> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:2> + <nat:3> \times <var:r> + <nat:4> \} \\ <var:L> := <nat:4> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:3> + <nat:3> \times <var:r> ^ <nat:2> + <nat:4> \times <var:r> + <nat:4> \} \\ <var:L> := <nat:1> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:4> + <nat:3> \times <var:r> ^ <nat:3> + <nat:4> \times <var:r> ^ <nat:2> + <nat:4> \times <var:r> + <nat:1> \} \\ <var:L> := <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:5> + <nat:3> \times <var:r> ^ <nat:4> + <nat:4> \times <var:r> ^ <nat:3> + <nat:4> \times <var:r> ^ <nat:2> + <nat:1> \times <var:r> + \} \\ <var:L> := <nat:2> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:6> + <nat:3> \times <var:r> ^ <nat:5> + <nat:4> \times <var:r> ^ <nat:4> + <nat:4> \times <var:r> ^ <nat:3> + <nat:1> \times <var:r> ^ <nat:2> + <nat:2> \} \\ <var:L> := <nat:3> + <var:L> * <var:J> ; \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:7> + <nat:3> \times <var:r> ^ <nat:6> + <nat:4> \times <var:r> ^ <nat:5> + <nat:4> \times <var:r> ^ <nat:4> + <nat:1> \times <var:r> ^ <nat:3> + <nat:2> \times <var:r> + <nat:3> \} \\ <var:L> := <nat:1> + <var:L> * <var:J> \\ \{ <var:J> = <var:r> \land <var:L> = <nat:3> \times <var:r> ^ <nat:8> + <nat:3> \times <var:r> ^ <nat:7> + <nat:4> \times <var:r> ^ <nat:6> + <nat:4> \times <var:r> ^ <nat:5> + <nat:1> \times <var:r> ^ <nat:4> + <nat:2> \times <var:r> ^ <nat:2> + <nat:3> \times <var:r> + <nat:1> \} \end{eqnarray} Using usual Hoare reasoning accomplishes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:L> := <nat:3> ; <var:L> := <nat:3> + <var:L> * <var:J> ; <var:L> := <nat:4> + <var:L> * <var:J> ; <var:L> := <nat:4> + <var:L> * <var:J> ; <var:L> := <nat:1> + <var:L> * <var:J> ; <var:L> := <var:L> * <var:J> ; <var:L> := <nat:2> + <var:L> * <var:J> ; <var:L> := <nat:3> + <var:L> * <var:J> ; <var:L> := <nat:1> + <var:L> * <var:J> . Theorem <genH:poly_code_correct> : forall <var:r> : nat , {{ <var:J> = <var:r> }} <def:poly> {{ <var:L> = <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:4> * <var:r> ^ <nat:6> + <nat:4> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:4> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:4> * <var:r> ^ <nat:5> + <nat:4> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:J> = <var:r> /\ <var:L> = <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:4> * <var:r> ^ <nat:6> + <nat:4> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 