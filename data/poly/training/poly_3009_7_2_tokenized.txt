\begin{document} \begin{definition} Take the following code <def:poly> in \emph{Imp} : \begin{verbatim} <var:c> := <nat:4> ; <var:c> := <nat:3> + <var:c> * <var:E> ; <var:c> := <nat:4> + <var:c> * <var:E> ; <var:c> := <nat:3> + <var:c> * <var:E> ; <var:c> := <nat:1> + <var:c> * <var:E> ; <var:c> := <nat:2> + <var:c> * <var:E> ; <var:c> := <nat:3> + <var:c> * <var:E> \end{verbatim} \end{definition} \begin{theorem} Assuming that we have $ <var:E> = <var:t> $ ahead of running the code it must be that $ <var:c> = <nat:4> \cdot <var:t> ^ <nat:6> + <nat:3> \cdot <var:t> ^ <nat:5> + <nat:4> \cdot <var:t> ^ <nat:4> + <nat:3> \cdot <var:t> ^ <nat:3> + <nat:1> \cdot <var:t> ^ <nat:2> + <nat:2> \cdot <var:t> + <nat:3> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:E> = <var:t> \} \\ <var:c> := <nat:4> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \} \\ <var:c> := <nat:3> + <var:c> * <var:E> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> + <nat:3> \} \\ <var:c> := <nat:4> + <var:c> * <var:E> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> ^ <nat:2> + <nat:3> \times <var:t> + <nat:4> \} \\ <var:c> := <nat:3> + <var:c> * <var:E> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> ^ <nat:3> + <nat:3> \times <var:t> ^ <nat:2> + <nat:4> \times <var:t> + <nat:3> \} \\ <var:c> := <nat:1> + <var:c> * <var:E> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> ^ <nat:4> + <nat:3> \times <var:t> ^ <nat:3> + <nat:4> \times <var:t> ^ <nat:2> + <nat:3> \times <var:t> + <nat:1> \} \\ <var:c> := <nat:2> + <var:c> * <var:E> ; \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> ^ <nat:5> + <nat:3> \times <var:t> ^ <nat:4> + <nat:4> \times <var:t> ^ <nat:3> + <nat:3> \times <var:t> ^ <nat:2> + <nat:1> \times <var:t> + <nat:2> \} \\ <var:c> := <nat:3> + <var:c> * <var:E> \\ \{ <var:E> = <var:t> \land <var:c> = <nat:4> \times <var:t> ^ <nat:6> + <nat:3> \times <var:t> ^ <nat:5> + <nat:4> \times <var:t> ^ <nat:4> + <nat:3> \times <var:t> ^ <nat:3> + <nat:1> \times <var:t> ^ <nat:2> + <nat:2> \times <var:t> + <nat:3> \} \end{eqnarray} Using typical Hoare logic finishes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:c> := <nat:4> ; <var:c> := <nat:3> + <var:c> * <var:E> ; <var:c> := <nat:4> + <var:c> * <var:E> ; <var:c> := <nat:3> + <var:c> * <var:E> ; <var:c> := <nat:1> + <var:c> * <var:E> ; <var:c> := <nat:2> + <var:c> * <var:E> ; <var:c> := <nat:3> + <var:c> * <var:E> . Theorem <genH:poly_code_correct> : forall <var:t> : nat , {{ <var:E> = <var:t> }} <def:poly> {{ <var:c> = <nat:4> * <var:t> ^ <nat:6> + <nat:3> * <var:t> ^ <nat:5> + <nat:4> * <var:t> ^ <nat:4> + <nat:3> * <var:t> ^ <nat:3> + <nat:1> * <var:t> ^ <nat:2> + <nat:2> * <var:t> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> ^ <nat:2> + <nat:3> * <var:t> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> ^ <nat:3> + <nat:3> * <var:t> ^ <nat:2> + <nat:4> * <var:t> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> ^ <nat:4> + <nat:3> * <var:t> ^ <nat:3> + <nat:4> * <var:t> ^ <nat:2> + <nat:3> * <var:t> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> ^ <nat:5> + <nat:3> * <var:t> ^ <nat:4> + <nat:4> * <var:t> ^ <nat:3> + <nat:3> * <var:t> ^ <nat:2> + <nat:1> * <var:t> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:t> /\ <var:c> = <nat:4> * <var:t> ^ <nat:6> + <nat:3> * <var:t> ^ <nat:5> + <nat:4> * <var:t> ^ <nat:4> + <nat:3> * <var:t> ^ <nat:3> + <nat:1> * <var:t> ^ <nat:2> + <nat:2> * <var:t> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 