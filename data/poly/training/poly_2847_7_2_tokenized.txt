\begin{document} \begin{theorem} Consider the following code : \begin{verbatim} <var:s> := <nat:2> ; <var:s> := <nat:3> + <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> ; <var:s> := <nat:3> + <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> ; <var:s> := <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> \end{verbatim} Given that we have $ <var:O> = <var:S> $ --- with some natural coefficients $ <var:S> $ --- before the execution of this segment of code it must be that $ <var:s> = <nat:2> \times <var:S> ^ <nat:6> + <nat:3> \times <var:S> ^ <nat:5> + <nat:4> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:4> $ . \end{theorem} \begin{proof} The application of typical Hoare reasoning gives us : \begin{eqnarray} \{ <var:O> = <var:S> \} \\ <var:s> := <nat:2> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \} \\ <var:s> := <nat:3> + <var:s> * <var:O> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> + <nat:3> \} \\ <var:s> := <nat:4> + <var:s> * <var:O> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:4> \} \\ <var:s> := <nat:3> + <var:s> * <var:O> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:4> \times <var:S> + <nat:3> \} \\ <var:s> := <nat:4> + <var:s> * <var:O> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:4> \} \\ <var:s> := <var:s> * <var:O> ; \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> ^ <nat:5> + <nat:3> \times <var:S> ^ <nat:4> + <nat:4> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:4> \times <var:S> + \} \\ <var:s> := <nat:4> + <var:s> * <var:O> \\ \{ <var:O> = <var:S> \land <var:s> = <nat:2> \times <var:S> ^ <nat:6> + <nat:3> \times <var:S> ^ <nat:5> + <nat:4> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:4> \} \end{eqnarray} As a result , the set of instructions has been shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:S> : nat , {{ <var:O> = <var:S> }} <var:s> := <nat:2> ; <var:s> := <nat:3> + <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> ; <var:s> := <nat:3> + <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> ; <var:s> := <var:s> * <var:O> ; <var:s> := <nat:4> + <var:s> * <var:O> {{ <var:s> = <nat:2> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> ^ <nat:5> + <nat:3> * <var:S> ^ <nat:4> + <nat:4> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:S> /\ <var:s> = <nat:2> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 