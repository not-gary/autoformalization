\begin{document} \begin{theorem} We define the following set of instructions as follows \begin{verbatim} <var:p> := <nat:1> ; <var:p> := <var:p> * <var:W> ; <var:p> := <var:p> * <var:W> ; <var:p> := <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:2> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> \end{verbatim} Assuming ( given some natural numbers $ <var:x> $ ) it to be that $ <var:W> = <var:x> $ ahead of executing this set of instructions , it follows that we have $ <var:p> = <nat:1> \times <var:x> ^ <nat:12> + <nat:4> \times <var:x> ^ <nat:8> + <nat:3> \times <var:x> ^ <nat:7> + <nat:2> \times <var:x> ^ <nat:6> + <nat:3> \times <var:x> ^ <nat:5> + <nat:3> \times <var:x> ^ <nat:4> + <nat:3> \times <var:x> ^ <nat:3> + <nat:4> \times <var:x> ^ <nat:2> + <nat:4> \times <var:x> + <nat:4> $ . \end{theorem} \begin{proof} Utilizing typical Hoare reasoning gives : \begin{eqnarray} \{ <var:W> = <var:x> \} \\ <var:p> := <nat:1> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> \} \\ <var:p> := <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> + \} \\ <var:p> := <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:2> + \} \\ <var:p> := <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:3> + \} \\ <var:p> := <nat:4> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:4> + <nat:4> \} \\ <var:p> := <nat:3> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:5> + <nat:4> * <var:x> + <nat:3> \} \\ <var:p> := <nat:2> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:6> + <nat:4> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:2> \} \\ <var:p> := <nat:3> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:7> + <nat:4> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:2> * <var:x> + <nat:3> \} \\ <var:p> := <nat:3> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:8> + <nat:4> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:2> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:3> \} \\ <var:p> := <nat:3> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:9> + <nat:4> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:2> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:3> \} \\ <var:p> := <nat:4> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:10> + <nat:4> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:5> + <nat:2> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:4> \} \\ <var:p> := <nat:4> + <var:p> * <var:W> ; \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:11> + <nat:4> * <var:x> ^ <nat:7> + <nat:3> * <var:x> ^ <nat:6> + <nat:2> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + <nat:4> \} \\ <var:p> := <nat:4> + <var:p> * <var:W> \\ \{ <var:W> = <var:x> \land <var:p> = <nat:1> * <var:x> ^ <nat:12> + <nat:4> * <var:x> ^ <nat:8> + <nat:3> * <var:x> ^ <nat:7> + <nat:2> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:4> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + <nat:4> \} \end{eqnarray} Therefore , the set of instructions has been demonstrated to be correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:x> : nat , {{ <var:W> = <var:x> }} <var:p> := <nat:1> ; <var:p> := <var:p> * <var:W> ; <var:p> := <var:p> * <var:W> ; <var:p> := <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:2> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:3> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> ; <var:p> := <nat:4> + <var:p> * <var:W> {{ <var:p> = <nat:1> * <var:x> ^ <nat:12> + <nat:4> * <var:x> ^ <nat:8> + <nat:3> * <var:x> ^ <nat:7> + <nat:2> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:4> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:3> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:4> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:5> + <nat:4> * <var:x> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:6> + <nat:4> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:7> + <nat:4> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:2> * <var:x> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:8> + <nat:4> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:2> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:9> + <nat:4> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:2> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:10> + <nat:4> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:5> + <nat:2> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:11> + <nat:4> * <var:x> ^ <nat:7> + <nat:3> * <var:x> ^ <nat:6> + <nat:2> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:W> = <var:x> /\ <var:p> = <nat:1> * <var:x> ^ <nat:12> + <nat:4> * <var:x> ^ <nat:8> + <nat:3> * <var:x> ^ <nat:7> + <nat:2> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:4> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 