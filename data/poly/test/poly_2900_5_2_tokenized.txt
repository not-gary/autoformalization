\begin{document} \begin{definition} We define a segment of code <def:poly> in \emph{Imp} as \begin{verbatim} <var:g> := <nat:2> ; <var:g> := <nat:3> + <var:g> * <var:x> ; <var:g> := <var:g> * <var:x> ; <var:g> := <var:g> * <var:x> ; <var:g> := <nat:4> + <var:g> * <var:x> \end{verbatim} \end{definition} \begin{theorem} Define the following list of instructions <def:poly> . Given --- for some $ <var:V> \in \mathbb{N} $ --- it to hold that $ <var:x> = <var:V> $ before evaluating our series of commands , then $ <var:g> = <nat:2> \times <var:V> ^ <nat:4> + <nat:3> \times <var:V> ^ <nat:3> + <nat:4> $ when list of instructions is done executing . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:x> = <var:V> \} \\ <var:g> := <nat:2> ; \\ \{ <var:x> = <var:V> \land <var:g> = <nat:2> \} \\ <var:g> := <nat:3> + <var:g> * <var:x> ; \\ \{ <var:x> = <var:V> \land <var:g> = <nat:2> \times <var:V> + <nat:3> \} \\ <var:g> := <var:g> * <var:x> ; \\ \{ <var:x> = <var:V> \land <var:g> = <nat:2> \times <var:V> ^ <nat:2> + <nat:3> \times <var:V> + \} \\ <var:g> := <var:g> * <var:x> ; \\ \{ <var:x> = <var:V> \land <var:g> = <nat:2> \times <var:V> ^ <nat:3> + <nat:3> \times <var:V> ^ <nat:2> + \} \\ <var:g> := <nat:4> + <var:g> * <var:x> \\ \{ <var:x> = <var:V> \land <var:g> = <nat:2> \times <var:V> ^ <nat:4> + <nat:3> \times <var:V> ^ <nat:3> + <nat:4> \} \end{eqnarray} Applying standard Hoare techniques solves the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:g> := <nat:2> ; <var:g> := <nat:3> + <var:g> * <var:x> ; <var:g> := <var:g> * <var:x> ; <var:g> := <var:g> * <var:x> ; <var:g> := <nat:4> + <var:g> * <var:x> . Theorem <genH:poly_code_correct> : forall <var:V> : nat , {{ <var:x> = <var:V> }} <def:poly> {{ <var:g> = <nat:2> * <var:V> ^ <nat:4> + <nat:3> * <var:V> ^ <nat:3> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:x> = <var:V> /\ <var:g> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:V> /\ <var:g> = <nat:2> * <var:V> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:V> /\ <var:g> = <nat:2> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:V> /\ <var:g> = <nat:2> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:V> /\ <var:g> = <nat:2> * <var:V> ^ <nat:4> + <nat:3> * <var:V> ^ <nat:3> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 