\begin{document} \begin{definition} Take the following list of instructions <def:poly> in \emph{Imp} : \begin{verbatim} <var:L> := <nat:2> ; <var:L> := <nat:3> + <var:L> * <var:s> ; <var:L> := <nat:3> + <var:L> * <var:s> ; <var:L> := <nat:1> + <var:L> * <var:s> ; <var:L> := <var:L> * <var:s> ; <var:L> := <nat:4> + <var:L> * <var:s> ; <var:L> := <nat:4> + <var:L> * <var:s> ; <var:L> := <nat:1> + <var:L> * <var:s> ; <var:L> := <nat:2> + <var:L> * <var:s> \end{verbatim} \end{definition} \begin{theorem} Allowing that $ <var:s> = <var:R> $ before the evaluation of the code , it must be that we have $ <var:L> = <nat:2> \cdot <var:R> ^ <nat:8> + <nat:3> \cdot <var:R> ^ <nat:7> + <nat:3> \cdot <var:R> ^ <nat:6> + <nat:1> \cdot <var:R> ^ <nat:5> + <nat:4> \cdot <var:R> ^ <nat:3> + <nat:4> \cdot <var:R> ^ <nat:2> + <nat:1> \cdot <var:R> + <nat:2> $ once segment of code is done executing . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:s> = <var:R> \} \\ <var:L> := <nat:2> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \} \\ <var:L> := <nat:3> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> + <nat:3> \} \\ <var:L> := <nat:3> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:2> + <nat:3> \times <var:R> + <nat:3> \} \\ <var:L> := <nat:1> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:3> + <nat:3> \times <var:R> ^ <nat:2> + <nat:3> \times <var:R> + <nat:1> \} \\ <var:L> := <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:4> + <nat:3> \times <var:R> ^ <nat:3> + <nat:3> \times <var:R> ^ <nat:2> + <nat:1> \times <var:R> + \} \\ <var:L> := <nat:4> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:5> + <nat:3> \times <var:R> ^ <nat:4> + <nat:3> \times <var:R> ^ <nat:3> + <nat:1> \times <var:R> ^ <nat:2> + <nat:4> \} \\ <var:L> := <nat:4> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:6> + <nat:3> \times <var:R> ^ <nat:5> + <nat:3> \times <var:R> ^ <nat:4> + <nat:1> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> + <nat:4> \} \\ <var:L> := <nat:1> + <var:L> * <var:s> ; \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:7> + <nat:3> \times <var:R> ^ <nat:6> + <nat:3> \times <var:R> ^ <nat:5> + <nat:1> \times <var:R> ^ <nat:4> + <nat:4> \times <var:R> ^ <nat:2> + <nat:4> \times <var:R> + <nat:1> \} \\ <var:L> := <nat:2> + <var:L> * <var:s> \\ \{ <var:s> = <var:R> \land <var:L> = <nat:2> \times <var:R> ^ <nat:8> + <nat:3> \times <var:R> ^ <nat:7> + <nat:3> \times <var:R> ^ <nat:6> + <nat:1> \times <var:R> ^ <nat:5> + <nat:4> \times <var:R> ^ <nat:3> + <nat:4> \times <var:R> ^ <nat:2> + <nat:1> \times <var:R> + <nat:2> \} \end{eqnarray} The application of standard Hoare reasoning accomplishes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:L> := <nat:2> ; <var:L> := <nat:3> + <var:L> * <var:s> ; <var:L> := <nat:3> + <var:L> * <var:s> ; <var:L> := <nat:1> + <var:L> * <var:s> ; <var:L> := <var:L> * <var:s> ; <var:L> := <nat:4> + <var:L> * <var:s> ; <var:L> := <nat:4> + <var:L> * <var:s> ; <var:L> := <nat:1> + <var:L> * <var:s> ; <var:L> := <nat:2> + <var:L> * <var:s> . Theorem <genH:poly_code_correct> : forall <var:R> : nat , {{ <var:s> = <var:R> }} <def:poly> {{ <var:L> = <nat:2> * <var:R> ^ <nat:8> + <nat:3> * <var:R> ^ <nat:7> + <nat:3> * <var:R> ^ <nat:6> + <nat:1> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + <nat:1> * <var:R> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:2> + <nat:3> * <var:R> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:3> + <nat:3> * <var:R> ^ <nat:2> + <nat:3> * <var:R> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:4> + <nat:3> * <var:R> ^ <nat:3> + <nat:3> * <var:R> ^ <nat:2> + <nat:1> * <var:R> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:5> + <nat:3> * <var:R> ^ <nat:4> + <nat:3> * <var:R> ^ <nat:3> + <nat:1> * <var:R> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:6> + <nat:3> * <var:R> ^ <nat:5> + <nat:3> * <var:R> ^ <nat:4> + <nat:1> * <var:R> ^ <nat:3> + <nat:4> * <var:R> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:7> + <nat:3> * <var:R> ^ <nat:6> + <nat:3> * <var:R> ^ <nat:5> + <nat:1> * <var:R> ^ <nat:4> + <nat:4> * <var:R> ^ <nat:2> + <nat:4> * <var:R> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:s> = <var:R> /\ <var:L> = <nat:2> * <var:R> ^ <nat:8> + <nat:3> * <var:R> ^ <nat:7> + <nat:3> * <var:R> ^ <nat:6> + <nat:1> * <var:R> ^ <nat:5> + <nat:4> * <var:R> ^ <nat:3> + <nat:4> * <var:R> ^ <nat:2> + <nat:1> * <var:R> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 