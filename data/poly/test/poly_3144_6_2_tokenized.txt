\begin{document} \begin{definition} Consider the following series of commands <def:poly> in \emph{Imp} as \begin{verbatim} <var:y> := <nat:2> ; <var:y> := <nat:2> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:3> + <var:y> * <var:N> \end{verbatim} \end{definition} \begin{theorem} Assuming $ <var:N> = <var:r> $ prior to interpreting this set of commands , it must be that we see $ <var:y> = <nat:2> \times <var:r> ^ <nat:5> + <nat:2> \times <var:r> ^ <nat:4> + <nat:4> \times <var:r> ^ <nat:3> + <nat:4> \times <var:r> ^ <nat:2> + <nat:4> \times <var:r> + <nat:3> $ . \end{theorem} \begin{proof} Applying Hoare techniques results in : \begin{eqnarray} \{ <var:N> = <var:r> \} \\ <var:y> := <nat:2> ; \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> \} \\ <var:y> := <nat:2> + <var:y> * <var:N> ; \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> * <var:r> + <nat:2> \} \\ <var:y> := <nat:4> + <var:y> * <var:N> ; \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:4> \} \\ <var:y> := <nat:4> + <var:y> * <var:N> ; \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:4> \} \\ <var:y> := <nat:4> + <var:y> * <var:N> ; \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:4> \} \\ <var:y> := <nat:3> + <var:y> * <var:N> \\ \{ <var:N> = <var:r> \land <var:y> = <nat:2> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:3> \} \end{eqnarray} Therefore , our set of commands has been proven correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:y> := <nat:2> ; <var:y> := <nat:2> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:4> + <var:y> * <var:N> ; <var:y> := <nat:3> + <var:y> * <var:N> . Theorem <genH:poly_code_correct> : forall <var:r> : nat , {{ <var:N> = <var:r> }} <def:poly> {{ <var:y> = <nat:2> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> * <var:r> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:N> = <var:r> /\ <var:y> = <nat:2> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 