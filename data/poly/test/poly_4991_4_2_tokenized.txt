\begin{document} \begin{theorem} Define the following series of commands as follows \begin{verbatim} <var:z> := <nat:4> ; <var:z> := <nat:3> + <var:z> * <var:M> ; <var:z> := <nat:4> + <var:z> * <var:M> ; <var:z> := <nat:3> + <var:z> * <var:M> \end{verbatim} Assume that $ <var:M> = <var:Y> $ before the execution of this list of instructions , it follows that $ <var:z> = <nat:4> * <var:Y> ^ <nat:3> + <nat:3> * <var:Y> ^ <nat:2> + <nat:4> * <var:Y> + <nat:3> $ . \end{theorem} \begin{proof} The way to prove correctness for the set of instructions is accomplished using the below decorated list of instructions . \begin{eqnarray} \{ <var:M> = <var:Y> \} \\ <var:z> := <nat:4> ; \\ \{ <var:M> = <var:Y> \land <var:z> = <nat:4> \} \\ <var:z> := <nat:3> + <var:z> * <var:M> ; \\ \{ <var:M> = <var:Y> \land <var:z> = <nat:4> \cdot <var:Y> + <nat:3> \} \\ <var:z> := <nat:4> + <var:z> * <var:M> ; \\ \{ <var:M> = <var:Y> \land <var:z> = <nat:4> \cdot <var:Y> ^ <nat:2> + <nat:3> \cdot <var:Y> + <nat:4> \} \\ <var:z> := <nat:3> + <var:z> * <var:M> \\ \{ <var:M> = <var:Y> \land <var:z> = <nat:4> \cdot <var:Y> ^ <nat:3> + <nat:3> \cdot <var:Y> ^ <nat:2> + <nat:4> \cdot <var:Y> + <nat:3> \} \end{eqnarray} The application of Hoare logic finishes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:Y> : nat , {{ <var:M> = <var:Y> }} <var:z> := <nat:4> ; <var:z> := <nat:3> + <var:z> * <var:M> ; <var:z> := <nat:4> + <var:z> * <var:M> ; <var:z> := <nat:3> + <var:z> * <var:M> {{ <var:z> = <nat:4> * <var:Y> ^ <nat:3> + <nat:3> * <var:Y> ^ <nat:2> + <nat:4> * <var:Y> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:M> = <var:Y> /\ <var:z> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:Y> /\ <var:z> = <nat:4> * <var:Y> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:Y> /\ <var:z> = <nat:4> * <var:Y> ^ <nat:2> + <nat:3> * <var:Y> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:Y> /\ <var:z> = <nat:4> * <var:Y> ^ <nat:3> + <nat:3> * <var:Y> ^ <nat:2> + <nat:4> * <var:Y> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 