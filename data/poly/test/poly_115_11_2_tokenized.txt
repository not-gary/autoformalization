\begin{document} \begin{definition} Define the following list of instructions <def:poly> in \emph{Imp} such that \begin{verbatim} <var:b> := <nat:4> ; <var:b> := <nat:4> + <var:b> * <var:c> ; <var:b> := <nat:4> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> ; <var:b> := <var:b> * <var:c> ; <var:b> := <nat:3> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> \end{verbatim} \end{definition} \begin{theorem} Assume $ <var:c> = <var:H> $ before interpreting our list of instructions , it holds that we see $ <var:b> = <nat:4> * <var:H> ^ <nat:10> + <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:2> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:1> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:3> * <var:H> + <nat:1> $ after code is done executing . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:c> = <var:H> \} \\ <var:b> := <nat:4> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> \} \\ <var:b> := <nat:4> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> + <nat:4> \} \\ <var:b> := <nat:4> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:2> + <nat:4> * <var:H> + <nat:4> \} \\ <var:b> := <nat:2> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:3> + <nat:4> * <var:H> ^ <nat:2> + <nat:4> * <var:H> + <nat:2> \} \\ <var:b> := <nat:2> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:4> + <nat:4> * <var:H> ^ <nat:3> + <nat:4> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:2> \} \\ <var:b> := <nat:1> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:5> + <nat:4> * <var:H> ^ <nat:4> + <nat:4> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:1> \} \\ <var:b> := <nat:2> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:6> + <nat:4> * <var:H> ^ <nat:5> + <nat:4> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:1> * <var:H> + <nat:2> \} \\ <var:b> := <nat:1> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:7> + <nat:4> * <var:H> ^ <nat:6> + <nat:4> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:1> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:1> \} \\ <var:b> := <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:8> + <nat:4> * <var:H> ^ <nat:7> + <nat:4> * <var:H> ^ <nat:6> + <nat:2> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:1> * <var:H> + \} \\ <var:b> := <nat:3> + <var:b> * <var:c> ; \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:4> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:2> * <var:H> ^ <nat:5> + <nat:1> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:1> * <var:H> ^ <nat:2> + <nat:3> \} \end{eqnarray} Furthering executing the segment of code returns : \begin{eqnarray} <var:b> := <nat:1> + <var:b> * <var:c> \\ \{ <var:c> = <var:H> \land <var:b> = <nat:4> * <var:H> ^ <nat:10> + <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:2> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:1> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:3> * <var:H> + <nat:1> \} \end{eqnarray} Applying standard Hoare techniques finishes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:b> := <nat:4> ; <var:b> := <nat:4> + <var:b> * <var:c> ; <var:b> := <nat:4> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> ; <var:b> := <nat:2> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> ; <var:b> := <var:b> * <var:c> ; <var:b> := <nat:3> + <var:b> * <var:c> ; <var:b> := <nat:1> + <var:b> * <var:c> . Theorem <genH:poly_code_correct> : forall <var:H> : nat , {{ <var:c> = <var:H> }} <def:poly> {{ <var:b> = <nat:4> * <var:H> ^ <nat:10> + <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:2> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:1> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:3> * <var:H> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:2> + <nat:4> * <var:H> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:3> + <nat:4> * <var:H> ^ <nat:2> + <nat:4> * <var:H> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:4> + <nat:4> * <var:H> ^ <nat:3> + <nat:4> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:5> + <nat:4> * <var:H> ^ <nat:4> + <nat:4> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:6> + <nat:4> * <var:H> ^ <nat:5> + <nat:4> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:1> * <var:H> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:7> + <nat:4> * <var:H> ^ <nat:6> + <nat:4> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:1> * <var:H> ^ <nat:2> + <nat:2> * <var:H> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:8> + <nat:4> * <var:H> ^ <nat:7> + <nat:4> * <var:H> ^ <nat:6> + <nat:2> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:2> * <var:H> ^ <nat:2> + <nat:1> * <var:H> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:4> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:2> * <var:H> ^ <nat:5> + <nat:1> * <var:H> ^ <nat:4> + <nat:2> * <var:H> ^ <nat:3> + <nat:1> * <var:H> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:c> = <var:H> /\ <var:b> = <nat:4> * <var:H> ^ <nat:10> + <nat:4> * <var:H> ^ <nat:9> + <nat:4> * <var:H> ^ <nat:8> + <nat:2> * <var:H> ^ <nat:7> + <nat:2> * <var:H> ^ <nat:6> + <nat:1> * <var:H> ^ <nat:5> + <nat:2> * <var:H> ^ <nat:4> + <nat:1> * <var:H> ^ <nat:3> + <nat:3> * <var:H> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 