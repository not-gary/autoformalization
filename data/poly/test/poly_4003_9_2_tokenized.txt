\begin{document} \begin{definition} Take a set of instructions <def:poly> in \emph{Imp} : \begin{verbatim} <var:b> := <nat:2> ; <var:b> := <nat:4> + <var:b> * <var:A> ; <var:b> := <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <nat:2> + <var:b> * <var:A> ; <var:b> := <nat:2> + <var:b> * <var:A> \end{verbatim} \end{definition} \begin{theorem} Given that we have $ <var:A> = <var:x> $ prior to the execution of this series of commands it follows that $ <var:b> = <nat:2> * <var:x> ^ <nat:8> + <nat:4> * <var:x> ^ <nat:7> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:2> + <nat:2> * <var:x> + <nat:2> $ . \end{theorem} \begin{proof} Using Hoare logic yields : \begin{eqnarray} \{ <var:A> = <var:x> \} \\ <var:b> := <nat:2> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \} \\ <var:b> := <nat:4> + <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> + <nat:4> \} \\ <var:b> := <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:2> + <nat:4> \times <var:x> + \} \\ <var:b> := <nat:3> + <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:3> + <nat:4> \times <var:x> ^ <nat:2> + <nat:3> \} \\ <var:b> := <nat:3> + <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:4> + <nat:4> \times <var:x> ^ <nat:3> + <nat:3> \times <var:x> + <nat:3> \} \\ <var:b> := <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:5> + <nat:4> \times <var:x> ^ <nat:4> + <nat:3> \times <var:x> ^ <nat:2> + <nat:3> \times <var:x> + \} \\ <var:b> := <nat:3> + <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:6> + <nat:4> \times <var:x> ^ <nat:5> + <nat:3> \times <var:x> ^ <nat:3> + <nat:3> \times <var:x> ^ <nat:2> + <nat:3> \} \\ <var:b> := <nat:2> + <var:b> * <var:A> ; \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:7> + <nat:4> \times <var:x> ^ <nat:6> + <nat:3> \times <var:x> ^ <nat:4> + <nat:3> \times <var:x> ^ <nat:3> + <nat:3> \times <var:x> + <nat:2> \} \\ <var:b> := <nat:2> + <var:b> * <var:A> \\ \{ <var:A> = <var:x> \land <var:b> = <nat:2> \times <var:x> ^ <nat:8> + <nat:4> \times <var:x> ^ <nat:7> + <nat:3> \times <var:x> ^ <nat:5> + <nat:3> \times <var:x> ^ <nat:4> + <nat:3> \times <var:x> ^ <nat:2> + <nat:2> \times <var:x> + <nat:2> \} \end{eqnarray} Thus , our list of commands has been proven correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:b> := <nat:2> ; <var:b> := <nat:4> + <var:b> * <var:A> ; <var:b> := <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <var:b> * <var:A> ; <var:b> := <nat:3> + <var:b> * <var:A> ; <var:b> := <nat:2> + <var:b> * <var:A> ; <var:b> := <nat:2> + <var:b> * <var:A> . Theorem <genH:poly_code_correct> : forall <var:x> : nat , {{ <var:A> = <var:x> }} <def:poly> {{ <var:b> = <nat:2> * <var:x> ^ <nat:8> + <nat:4> * <var:x> ^ <nat:7> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:2> + <nat:2> * <var:x> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:2> + <nat:4> * <var:x> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:3> + <nat:4> * <var:x> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:4> + <nat:4> * <var:x> ^ <nat:3> + <nat:3> * <var:x> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:5> + <nat:4> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> * <var:x> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:6> + <nat:4> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:7> + <nat:4> * <var:x> ^ <nat:6> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:3> + <nat:3> * <var:x> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:A> = <var:x> /\ <var:b> = <nat:2> * <var:x> ^ <nat:8> + <nat:4> * <var:x> ^ <nat:7> + <nat:3> * <var:x> ^ <nat:5> + <nat:3> * <var:x> ^ <nat:4> + <nat:3> * <var:x> ^ <nat:2> + <nat:2> * <var:x> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 