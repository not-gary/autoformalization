\begin{document} \begin{definition} Define a set of commands <def:poly> in \emph{Imp} as \begin{verbatim} <var:R> := <nat:2> ; <var:R> := <nat:4> + <var:R> * <var:V> ; <var:R> := <nat:4> + <var:R> * <var:V> ; <var:R> := <nat:3> + <var:R> * <var:V> ; <var:R> := <nat:2> + <var:R> * <var:V> \end{verbatim} \end{definition} \begin{theorem} Consider a series of commands <def:poly> . Given that we have $ <var:V> = <var:D> $ before interpreting the program it holds that we see $ <var:R> = <nat:2> \times <var:D> ^ <nat:4> + <nat:4> \times <var:D> ^ <nat:3> + <nat:4> \times <var:D> ^ <nat:2> + <nat:3> \times <var:D> + <nat:2> $ . \end{theorem} \begin{proof} See that the below decorated series of commands . \begin{eqnarray} \{ <var:V> = <var:D> \} \\ <var:R> := <nat:2> ; \\ \{ <var:V> = <var:D> \land <var:R> = <nat:2> \} \end{eqnarray} Allowing evaluating the list of commands returns : \begin{eqnarray} <var:R> := <nat:4> + <var:R> * <var:V> ; \\ \{ <var:V> = <var:D> \land <var:R> = <nat:2> * <var:D> + <nat:4> \} \\ <var:R> := <nat:4> + <var:R> * <var:V> ; \\ \{ <var:V> = <var:D> \land <var:R> = <nat:2> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:4> \} \end{eqnarray} Allowing interpreting this program gives : \begin{eqnarray} <var:R> := <nat:3> + <var:R> * <var:V> ; \\ \{ <var:V> = <var:D> \land <var:R> = <nat:2> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:3> \} \\ <var:R> := <nat:2> + <var:R> * <var:V> \\ \{ <var:V> = <var:D> \land <var:R> = <nat:2> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> \} \end{eqnarray} The application of typical Hoare techniques completes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:R> := <nat:2> ; <var:R> := <nat:4> + <var:R> * <var:V> ; <var:R> := <nat:4> + <var:R> * <var:V> ; <var:R> := <nat:3> + <var:R> * <var:V> ; <var:R> := <nat:2> + <var:R> * <var:V> . Theorem <genH:poly_code_correct> : forall <var:D> : nat , {{ <var:V> = <var:D> }} <def:poly> {{ <var:R> = <nat:2> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:V> = <var:D> /\ <var:R> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:D> /\ <var:R> = <nat:2> * <var:D> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:D> /\ <var:R> = <nat:2> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:D> /\ <var:R> = <nat:2> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:D> /\ <var:R> = <nat:2> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 