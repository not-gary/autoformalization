\begin{document} \begin{theorem} We define the following set of commands : \begin{verbatim} <var:z> := <nat:2> ; <var:z> := <nat:3> + <var:z> * <var:y> ; <var:z> := <var:z> * <var:y> ; <var:z> := <nat:1> + <var:z> * <var:y> ; <var:z> := <nat:1> + <var:z> * <var:y> ; <var:z> := <nat:4> + <var:z> * <var:y> ; <var:z> := <nat:4> + <var:z> * <var:y> ; <var:z> := <nat:2> + <var:z> * <var:y> ; <var:z> := <nat:2> + <var:z> * <var:y> \end{verbatim} Given given some whole numbers $ <var:A> $ it to be that $ <var:y> = <var:A> $ prior to the execution of this list of commands , then we see $ <var:z> = <nat:2> \cdot <var:A> ^ <nat:8> + <nat:3> \cdot <var:A> ^ <nat:7> + <nat:1> \cdot <var:A> ^ <nat:5> + <nat:1> \cdot <var:A> ^ <nat:4> + <nat:4> \cdot <var:A> ^ <nat:3> + <nat:4> \cdot <var:A> ^ <nat:2> + <nat:2> \cdot <var:A> + <nat:2> $ . \end{theorem} \begin{proof} Utilizing Hoare reasoning returns : \begin{eqnarray} \{ <var:y> = <var:A> \} \\ <var:z> := <nat:2> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \} \\ <var:z> := <nat:3> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> + <nat:3> \} \\ <var:z> := <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:2> + <nat:3> \times <var:A> + \} \\ <var:z> := <nat:1> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:3> + <nat:3> \times <var:A> ^ <nat:2> + <nat:1> \} \\ <var:z> := <nat:1> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:4> + <nat:3> \times <var:A> ^ <nat:3> + <nat:1> \times <var:A> + <nat:1> \} \\ <var:z> := <nat:4> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:5> + <nat:3> \times <var:A> ^ <nat:4> + <nat:1> \times <var:A> ^ <nat:2> + <nat:1> \times <var:A> + <nat:4> \} \\ <var:z> := <nat:4> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:6> + <nat:3> \times <var:A> ^ <nat:5> + <nat:1> \times <var:A> ^ <nat:3> + <nat:1> \times <var:A> ^ <nat:2> + <nat:4> \times <var:A> + <nat:4> \} \\ <var:z> := <nat:2> + <var:z> * <var:y> ; \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:7> + <nat:3> \times <var:A> ^ <nat:6> + <nat:1> \times <var:A> ^ <nat:4> + <nat:1> \times <var:A> ^ <nat:3> + <nat:4> \times <var:A> ^ <nat:2> + <nat:4> \times <var:A> + <nat:2> \} \\ <var:z> := <nat:2> + <var:z> * <var:y> \\ \{ <var:y> = <var:A> \land <var:z> = <nat:2> \times <var:A> ^ <nat:8> + <nat:3> \times <var:A> ^ <nat:7> + <nat:1> \times <var:A> ^ <nat:5> + <nat:1> \times <var:A> ^ <nat:4> + <nat:4> \times <var:A> ^ <nat:3> + <nat:4> \times <var:A> ^ <nat:2> + <nat:2> \times <var:A> + <nat:2> \} \end{eqnarray} Hence , the code segment is shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:A> : nat , {{ <var:y> = <var:A> }} <var:z> := <nat:2> ; <var:z> := <nat:3> + <var:z> * <var:y> ; <var:z> := <var:z> * <var:y> ; <var:z> := <nat:1> + <var:z> * <var:y> ; <var:z> := <nat:1> + <var:z> * <var:y> ; <var:z> := <nat:4> + <var:z> * <var:y> ; <var:z> := <nat:4> + <var:z> * <var:y> ; <var:z> := <nat:2> + <var:z> * <var:y> ; <var:z> := <nat:2> + <var:z> * <var:y> {{ <var:z> = <nat:2> * <var:A> ^ <nat:8> + <nat:3> * <var:A> ^ <nat:7> + <nat:1> * <var:A> ^ <nat:5> + <nat:1> * <var:A> ^ <nat:4> + <nat:4> * <var:A> ^ <nat:3> + <nat:4> * <var:A> ^ <nat:2> + <nat:2> * <var:A> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:2> + <nat:3> * <var:A> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:3> + <nat:3> * <var:A> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:4> + <nat:3> * <var:A> ^ <nat:3> + <nat:1> * <var:A> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:5> + <nat:3> * <var:A> ^ <nat:4> + <nat:1> * <var:A> ^ <nat:2> + <nat:1> * <var:A> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:6> + <nat:3> * <var:A> ^ <nat:5> + <nat:1> * <var:A> ^ <nat:3> + <nat:1> * <var:A> ^ <nat:2> + <nat:4> * <var:A> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:7> + <nat:3> * <var:A> ^ <nat:6> + <nat:1> * <var:A> ^ <nat:4> + <nat:1> * <var:A> ^ <nat:3> + <nat:4> * <var:A> ^ <nat:2> + <nat:4> * <var:A> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:A> /\ <var:z> = <nat:2> * <var:A> ^ <nat:8> + <nat:3> * <var:A> ^ <nat:7> + <nat:1> * <var:A> ^ <nat:5> + <nat:1> * <var:A> ^ <nat:4> + <nat:4> * <var:A> ^ <nat:3> + <nat:4> * <var:A> ^ <nat:2> + <nat:2> * <var:A> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 