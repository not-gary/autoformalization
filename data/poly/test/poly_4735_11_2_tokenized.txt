\begin{document} \begin{theorem} Define the following code segment as \begin{verbatim} <var:o> := <nat:3> ; <var:o> := <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:1> + <var:o> * <var:m> ; <var:o> := <nat:1> + <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:4> + <var:o> * <var:m> \end{verbatim} Given that we have $ <var:m> = <var:D> $ before evaluating this code it must be that $ <var:o> = <nat:3> * <var:D> ^ <nat:10> + <nat:2> * <var:D> ^ <nat:8> + <nat:3> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:4> $ once list of commands executes . \end{theorem} \begin{proof} Utilizing Hoare techniques gives : \begin{eqnarray} \{ <var:m> = <var:D> \} \\ <var:o> := <nat:3> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \} \\ <var:o> := <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> + \} \\ <var:o> := <nat:2> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:2> + <nat:2> \} \\ <var:o> := <nat:3> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:3> + <nat:2> \times <var:D> + <nat:3> \} \\ <var:o> := <nat:1> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:4> + <nat:2> \times <var:D> ^ <nat:2> + <nat:3> \times <var:D> + <nat:1> \} \\ <var:o> := <nat:1> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:5> + <nat:2> \times <var:D> ^ <nat:3> + <nat:3> \times <var:D> ^ <nat:2> + <nat:1> \times <var:D> + <nat:1> \} \\ <var:o> := <nat:2> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:6> + <nat:2> \times <var:D> ^ <nat:4> + <nat:3> \times <var:D> ^ <nat:3> + <nat:1> \times <var:D> ^ <nat:2> + <nat:1> \times <var:D> + <nat:2> \} \\ <var:o> := <nat:3> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:7> + <nat:2> \times <var:D> ^ <nat:5> + <nat:3> \times <var:D> ^ <nat:4> + <nat:1> \times <var:D> ^ <nat:3> + <nat:1> \times <var:D> ^ <nat:2> + <nat:2> \times <var:D> + <nat:3> \} \\ <var:o> := <nat:3> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:8> + <nat:2> \times <var:D> ^ <nat:6> + <nat:3> \times <var:D> ^ <nat:5> + <nat:1> \times <var:D> ^ <nat:4> + <nat:1> \times <var:D> ^ <nat:3> + <nat:2> \times <var:D> ^ <nat:2> + <nat:3> \times <var:D> + <nat:3> \} \\ <var:o> := <nat:2> + <var:o> * <var:m> ; \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:9> + <nat:2> \times <var:D> ^ <nat:7> + <nat:3> \times <var:D> ^ <nat:6> + <nat:1> \times <var:D> ^ <nat:5> + <nat:1> \times <var:D> ^ <nat:4> + <nat:2> \times <var:D> ^ <nat:3> + <nat:3> \times <var:D> ^ <nat:2> + <nat:3> \times <var:D> + <nat:2> \} \\ <var:o> := <nat:4> + <var:o> * <var:m> \\ \{ <var:m> = <var:D> \land <var:o> = <nat:3> \times <var:D> ^ <nat:10> + <nat:2> \times <var:D> ^ <nat:8> + <nat:3> \times <var:D> ^ <nat:7> + <nat:1> \times <var:D> ^ <nat:6> + <nat:1> \times <var:D> ^ <nat:5> + <nat:2> \times <var:D> ^ <nat:4> + <nat:3> \times <var:D> ^ <nat:3> + <nat:3> \times <var:D> ^ <nat:2> + <nat:2> \times <var:D> + <nat:4> \} \end{eqnarray} As a consequence , the code is proven correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:D> : nat , {{ <var:m> = <var:D> }} <var:o> := <nat:3> ; <var:o> := <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:1> + <var:o> * <var:m> ; <var:o> := <nat:1> + <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:3> + <var:o> * <var:m> ; <var:o> := <nat:2> + <var:o> * <var:m> ; <var:o> := <nat:4> + <var:o> * <var:m> {{ <var:o> = <nat:3> * <var:D> ^ <nat:10> + <nat:2> * <var:D> ^ <nat:8> + <nat:3> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:3> + <nat:2> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:6> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:7> + <nat:2> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:8> + <nat:2> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:9> + <nat:2> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:m> = <var:D> /\ <var:o> = <nat:3> * <var:D> ^ <nat:10> + <nat:2> * <var:D> ^ <nat:8> + <nat:3> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 