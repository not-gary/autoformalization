\begin{document} \begin{theorem} Define the following program such that \begin{verbatim} <var:h> := <nat:1> ; <var:h> := <nat:2> + <var:h> * <var:n> ; <var:h> := <nat:1> + <var:h> * <var:n> ; <var:h> := <nat:1> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> \end{verbatim} Assuming it to hold that $ <var:n> = <var:e> $ before interpreting the code , then we see $ <var:h> = <nat:1> \cdot <var:e> ^ <nat:13> + <nat:2> \cdot <var:e> ^ <nat:12> + <nat:1> \cdot <var:e> ^ <nat:11> + <nat:1> \cdot <var:e> ^ <nat:10> + <nat:3> \cdot <var:e> ^ <nat:9> + <nat:4> \cdot <var:e> ^ <nat:8> + <nat:3> \cdot <var:e> ^ <nat:7> + <nat:3> \cdot <var:e> ^ <nat:6> + <nat:3> \cdot <var:e> ^ <nat:5> + <nat:4> \cdot <var:e> ^ <nat:4> + <nat:3> \cdot <var:e> ^ <nat:3> + <nat:4> \cdot <var:e> ^ <nat:2> + <nat:3> $ . \end{theorem} \begin{proof} See that this decorated program . \begin{eqnarray} \{ <var:n> = <var:e> \} \\ <var:h> := <nat:1> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> \} \\ <var:h> := <nat:2> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> + <nat:2> \} \\ <var:h> := <nat:1> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:1> \} \\ <var:h> := <nat:1> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:1> \} \\ <var:h> := <nat:3> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:4> + <nat:2> * <var:e> ^ <nat:3> + <nat:1> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:3> \} \\ <var:h> := <nat:4> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:1> * <var:e> ^ <nat:3> + <nat:1> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> \} \\ <var:h> := <nat:3> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:6> + <nat:2> * <var:e> ^ <nat:5> + <nat:1> * <var:e> ^ <nat:4> + <nat:1> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + <nat:3> \} \\ <var:h> := <nat:3> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:7> + <nat:2> * <var:e> ^ <nat:6> + <nat:1> * <var:e> ^ <nat:5> + <nat:1> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:3> \} \\ <var:h> := <nat:3> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:8> + <nat:2> * <var:e> ^ <nat:7> + <nat:1> * <var:e> ^ <nat:6> + <nat:1> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:3> \} \\ <var:h> := <nat:4> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:9> + <nat:2> * <var:e> ^ <nat:8> + <nat:1> * <var:e> ^ <nat:7> + <nat:1> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> \} \\ <var:h> := <nat:3> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:10> + <nat:2> * <var:e> ^ <nat:9> + <nat:1> * <var:e> ^ <nat:8> + <nat:1> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:4> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + <nat:3> \} \\ <var:h> := <nat:4> + <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:11> + <nat:2> * <var:e> ^ <nat:10> + <nat:1> * <var:e> ^ <nat:9> + <nat:1> * <var:e> ^ <nat:8> + <nat:3> * <var:e> ^ <nat:7> + <nat:4> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> \} \\ <var:h> := <var:h> * <var:n> ; \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:12> + <nat:2> * <var:e> ^ <nat:11> + <nat:1> * <var:e> ^ <nat:10> + <nat:1> * <var:e> ^ <nat:9> + <nat:3> * <var:e> ^ <nat:8> + <nat:4> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + \} \\ <var:h> := <nat:3> + <var:h> * <var:n> \\ \{ <var:n> = <var:e> \land <var:h> = <nat:1> * <var:e> ^ <nat:13> + <nat:2> * <var:e> ^ <nat:12> + <nat:1> * <var:e> ^ <nat:11> + <nat:1> * <var:e> ^ <nat:10> + <nat:3> * <var:e> ^ <nat:9> + <nat:4> * <var:e> ^ <nat:8> + <nat:3> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> \} \end{eqnarray} The application of standard Hoare logic finishes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:e> : nat , {{ <var:n> = <var:e> }} <var:h> := <nat:1> ; <var:h> := <nat:2> + <var:h> * <var:n> ; <var:h> := <nat:1> + <var:h> * <var:n> ; <var:h> := <nat:1> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> ; <var:h> := <nat:4> + <var:h> * <var:n> ; <var:h> := <var:h> * <var:n> ; <var:h> := <nat:3> + <var:h> * <var:n> {{ <var:h> = <nat:1> * <var:e> ^ <nat:13> + <nat:2> * <var:e> ^ <nat:12> + <nat:1> * <var:e> ^ <nat:11> + <nat:1> * <var:e> ^ <nat:10> + <nat:3> * <var:e> ^ <nat:9> + <nat:4> * <var:e> ^ <nat:8> + <nat:3> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:4> + <nat:2> * <var:e> ^ <nat:3> + <nat:1> * <var:e> ^ <nat:2> + <nat:1> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:1> * <var:e> ^ <nat:3> + <nat:1> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:6> + <nat:2> * <var:e> ^ <nat:5> + <nat:1> * <var:e> ^ <nat:4> + <nat:1> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:7> + <nat:2> * <var:e> ^ <nat:6> + <nat:1> * <var:e> ^ <nat:5> + <nat:1> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:8> + <nat:2> * <var:e> ^ <nat:7> + <nat:1> * <var:e> ^ <nat:6> + <nat:1> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:9> + <nat:2> * <var:e> ^ <nat:8> + <nat:1> * <var:e> ^ <nat:7> + <nat:1> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:10> + <nat:2> * <var:e> ^ <nat:9> + <nat:1> * <var:e> ^ <nat:8> + <nat:1> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:4> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:11> + <nat:2> * <var:e> ^ <nat:10> + <nat:1> * <var:e> ^ <nat:9> + <nat:1> * <var:e> ^ <nat:8> + <nat:3> * <var:e> ^ <nat:7> + <nat:4> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> * <var:e> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:12> + <nat:2> * <var:e> ^ <nat:11> + <nat:1> * <var:e> ^ <nat:10> + <nat:1> * <var:e> ^ <nat:9> + <nat:3> * <var:e> ^ <nat:8> + <nat:4> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:3> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:e> /\ <var:h> = <nat:1> * <var:e> ^ <nat:13> + <nat:2> * <var:e> ^ <nat:12> + <nat:1> * <var:e> ^ <nat:11> + <nat:1> * <var:e> ^ <nat:10> + <nat:3> * <var:e> ^ <nat:9> + <nat:4> * <var:e> ^ <nat:8> + <nat:3> * <var:e> ^ <nat:7> + <nat:3> * <var:e> ^ <nat:6> + <nat:3> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:3> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 