\begin{document} \begin{definition} Let the following series of instructions <def:poly> in \emph{Imp} such that \begin{verbatim} <var:S> := <nat:1> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> ; <var:S> := <var:S> * <var:j> ; <var:S> := <nat:3> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> \end{verbatim} \end{definition} \begin{theorem} Assume that $ <var:j> = <var:e> $ before the evaluation of our list of commands then we have $ <var:S> = <nat:1> \cdot <var:e> ^ <nat:8> + <nat:2> \cdot <var:e> ^ <nat:7> + <nat:2> \cdot <var:e> ^ <nat:6> + <nat:4> \cdot <var:e> ^ <nat:5> + <nat:2> \cdot <var:e> ^ <nat:4> + <nat:4> \cdot <var:e> ^ <nat:3> + <nat:3> \cdot <var:e> + <nat:4> $ when segment of code finishes . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:j> = <var:e> \} \\ <var:S> := <nat:1> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \} \\ <var:S> := <nat:2> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> + <nat:2> \} \\ <var:S> := <nat:2> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:2> + <nat:2> \cdot <var:e> + <nat:2> \} \\ <var:S> := <nat:4> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:3> + <nat:2> \cdot <var:e> ^ <nat:2> + <nat:2> \cdot <var:e> + <nat:4> \} \\ <var:S> := <nat:2> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:4> + <nat:2> \cdot <var:e> ^ <nat:3> + <nat:2> \cdot <var:e> ^ <nat:2> + <nat:4> \cdot <var:e> + <nat:2> \} \\ <var:S> := <nat:4> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:5> + <nat:2> \cdot <var:e> ^ <nat:4> + <nat:2> \cdot <var:e> ^ <nat:3> + <nat:4> \cdot <var:e> ^ <nat:2> + <nat:2> \cdot <var:e> + <nat:4> \} \\ <var:S> := <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:6> + <nat:2> \cdot <var:e> ^ <nat:5> + <nat:2> \cdot <var:e> ^ <nat:4> + <nat:4> \cdot <var:e> ^ <nat:3> + <nat:2> \cdot <var:e> ^ <nat:2> + <nat:4> \cdot <var:e> + \} \\ <var:S> := <nat:3> + <var:S> * <var:j> ; \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:7> + <nat:2> \cdot <var:e> ^ <nat:6> + <nat:2> \cdot <var:e> ^ <nat:5> + <nat:4> \cdot <var:e> ^ <nat:4> + <nat:2> \cdot <var:e> ^ <nat:3> + <nat:4> \cdot <var:e> ^ <nat:2> + <nat:3> \} \\ <var:S> := <nat:4> + <var:S> * <var:j> \\ \{ <var:j> = <var:e> \land <var:S> = <nat:1> \cdot <var:e> ^ <nat:8> + <nat:2> \cdot <var:e> ^ <nat:7> + <nat:2> \cdot <var:e> ^ <nat:6> + <nat:4> \cdot <var:e> ^ <nat:5> + <nat:2> \cdot <var:e> ^ <nat:4> + <nat:4> \cdot <var:e> ^ <nat:3> + <nat:3> \cdot <var:e> + <nat:4> \} \end{eqnarray} Applying usual Hoare techniques solves the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:S> := <nat:1> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> ; <var:S> := <nat:2> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> ; <var:S> := <var:S> * <var:j> ; <var:S> := <nat:3> + <var:S> * <var:j> ; <var:S> := <nat:4> + <var:S> * <var:j> . Theorem <genH:poly_code_correct> : forall <var:e> : nat , {{ <var:j> = <var:e> }} <def:poly> {{ <var:S> = <nat:1> * <var:e> ^ <nat:8> + <nat:2> * <var:e> ^ <nat:7> + <nat:2> * <var:e> ^ <nat:6> + <nat:4> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:4> + <nat:2> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:2> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:2> * <var:e> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:6> + <nat:2> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:2> * <var:e> ^ <nat:2> + <nat:4> * <var:e> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:7> + <nat:2> * <var:e> ^ <nat:6> + <nat:2> * <var:e> ^ <nat:5> + <nat:4> * <var:e> ^ <nat:4> + <nat:2> * <var:e> ^ <nat:3> + <nat:4> * <var:e> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:e> /\ <var:S> = <nat:1> * <var:e> ^ <nat:8> + <nat:2> * <var:e> ^ <nat:7> + <nat:2> * <var:e> ^ <nat:6> + <nat:4> * <var:e> ^ <nat:5> + <nat:2> * <var:e> ^ <nat:4> + <nat:4> * <var:e> ^ <nat:3> + <nat:3> * <var:e> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 