\begin{document} \begin{theorem} Define the following list of instructions as follows \begin{verbatim} <var:w> := <nat:4> ; <var:w> := <nat:3> + <var:w> * <var:D> ; <var:w> := <nat:2> + <var:w> * <var:D> ; <var:w> := <var:w> * <var:D> ; <var:w> := <nat:2> + <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> ; <var:w> := <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> \end{verbatim} Assuming $ <var:D> = <var:W> $ ahead of interpreting our segment of code then $ <var:w> = <nat:4> \times <var:W> ^ <nat:11> + <nat:3> \times <var:W> ^ <nat:10> + <nat:2> \times <var:W> ^ <nat:9> + <nat:2> \times <var:W> ^ <nat:7> + <nat:1> \times <var:W> ^ <nat:6> + <nat:4> \times <var:W> ^ <nat:5> + <nat:4> \times <var:W> ^ <nat:4> + <nat:1> \times <var:W> ^ <nat:2> + <nat:1> \times <var:W> + <nat:4> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:D> = <var:W> \} \\ <var:w> := <nat:4> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \} \\ <var:w> := <nat:3> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> + <nat:3> \} \\ <var:w> := <nat:2> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:2> + <nat:3> \times <var:W> + <nat:2> \} \\ <var:w> := <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:3> + <nat:3> \times <var:W> ^ <nat:2> + <nat:2> \times <var:W> + \} \\ <var:w> := <nat:2> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:4> + <nat:3> \times <var:W> ^ <nat:3> + <nat:2> \times <var:W> ^ <nat:2> + <nat:2> \} \\ <var:w> := <nat:1> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:5> + <nat:3> \times <var:W> ^ <nat:4> + <nat:2> \times <var:W> ^ <nat:3> + <nat:2> \times <var:W> + <nat:1> \} \\ <var:w> := <nat:4> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:6> + <nat:3> \times <var:W> ^ <nat:5> + <nat:2> \times <var:W> ^ <nat:4> + <nat:2> \times <var:W> ^ <nat:2> + <nat:1> \times <var:W> + <nat:4> \} \\ <var:w> := <nat:4> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:7> + <nat:3> \times <var:W> ^ <nat:6> + <nat:2> \times <var:W> ^ <nat:5> + <nat:2> \times <var:W> ^ <nat:3> + <nat:1> \times <var:W> ^ <nat:2> + <nat:4> \times <var:W> + <nat:4> \} \\ <var:w> := <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:8> + <nat:3> \times <var:W> ^ <nat:7> + <nat:2> \times <var:W> ^ <nat:6> + <nat:2> \times <var:W> ^ <nat:4> + <nat:1> \times <var:W> ^ <nat:3> + <nat:4> \times <var:W> ^ <nat:2> + <nat:4> \times <var:W> + \} \\ <var:w> := <nat:1> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:9> + <nat:3> \times <var:W> ^ <nat:8> + <nat:2> \times <var:W> ^ <nat:7> + <nat:2> \times <var:W> ^ <nat:5> + <nat:1> \times <var:W> ^ <nat:4> + <nat:4> \times <var:W> ^ <nat:3> + <nat:4> \times <var:W> ^ <nat:2> + <nat:1> \} \\ <var:w> := <nat:1> + <var:w> * <var:D> ; \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:10> + <nat:3> \times <var:W> ^ <nat:9> + <nat:2> \times <var:W> ^ <nat:8> + <nat:2> \times <var:W> ^ <nat:6> + <nat:1> \times <var:W> ^ <nat:5> + <nat:4> \times <var:W> ^ <nat:4> + <nat:4> \times <var:W> ^ <nat:3> + <nat:1> \times <var:W> + <nat:1> \} \\ <var:w> := <nat:4> + <var:w> * <var:D> \\ \{ <var:D> = <var:W> \land <var:w> = <nat:4> \times <var:W> ^ <nat:11> + <nat:3> \times <var:W> ^ <nat:10> + <nat:2> \times <var:W> ^ <nat:9> + <nat:2> \times <var:W> ^ <nat:7> + <nat:1> \times <var:W> ^ <nat:6> + <nat:4> \times <var:W> ^ <nat:5> + <nat:4> \times <var:W> ^ <nat:4> + <nat:1> \times <var:W> ^ <nat:2> + <nat:1> \times <var:W> + <nat:4> \} \end{eqnarray} Utilizing typical Hoare logic accomplishes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:W> : nat , {{ <var:D> = <var:W> }} <var:w> := <nat:4> ; <var:w> := <nat:3> + <var:w> * <var:D> ; <var:w> := <nat:2> + <var:w> * <var:D> ; <var:w> := <var:w> * <var:D> ; <var:w> := <nat:2> + <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> ; <var:w> := <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:1> + <var:w> * <var:D> ; <var:w> := <nat:4> + <var:w> * <var:D> {{ <var:w> = <nat:4> * <var:W> ^ <nat:11> + <nat:3> * <var:W> ^ <nat:10> + <nat:2> * <var:W> ^ <nat:9> + <nat:2> * <var:W> ^ <nat:7> + <nat:1> * <var:W> ^ <nat:6> + <nat:4> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:1> * <var:W> ^ <nat:2> + <nat:1> * <var:W> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:2> + <nat:3> * <var:W> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:3> + <nat:3> * <var:W> ^ <nat:2> + <nat:2> * <var:W> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:4> + <nat:3> * <var:W> ^ <nat:3> + <nat:2> * <var:W> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:5> + <nat:3> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:3> + <nat:2> * <var:W> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:2> + <nat:1> * <var:W> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:7> + <nat:3> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:3> + <nat:1> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:8> + <nat:3> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:4> + <nat:1> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:9> + <nat:3> * <var:W> ^ <nat:8> + <nat:2> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:5> + <nat:1> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:10> + <nat:3> * <var:W> ^ <nat:9> + <nat:2> * <var:W> ^ <nat:8> + <nat:2> * <var:W> ^ <nat:6> + <nat:1> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:1> * <var:W> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:D> = <var:W> /\ <var:w> = <nat:4> * <var:W> ^ <nat:11> + <nat:3> * <var:W> ^ <nat:10> + <nat:2> * <var:W> ^ <nat:9> + <nat:2> * <var:W> ^ <nat:7> + <nat:1> * <var:W> ^ <nat:6> + <nat:4> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:1> * <var:W> ^ <nat:2> + <nat:1> * <var:W> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 