\begin{document} \begin{definition} We define the following set of instructions <def:poly> in \emph{Imp} as \begin{verbatim} <var:s> := <nat:1> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> \end{verbatim} \end{definition} \begin{theorem} Let the following list of commands <def:poly> . Assume given some positive integers $ <var:N> $ that $ <var:r> = <var:N> $ before evaluating the list of instructions , it must be that $ <var:s> = <nat:1> \times <var:N> ^ <nat:13> + <nat:4> \times <var:N> ^ <nat:12> + <nat:3> \times <var:N> ^ <nat:11> + <nat:2> \times <var:N> ^ <nat:10> + <nat:4> \times <var:N> ^ <nat:9> + <nat:2> \times <var:N> ^ <nat:8> + <nat:3> \times <var:N> ^ <nat:7> + <nat:2> \times <var:N> ^ <nat:6> + <nat:4> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:3> + <nat:3> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:3> $ once series of instructions has finished executing . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:r> = <var:N> \} \\ <var:s> := <nat:1> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \} \\ <var:s> := <nat:4> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> + <nat:4> \} \\ <var:s> := <nat:3> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:2> + <nat:4> \times <var:N> + <nat:3> \} \end{eqnarray} Furthering the evaluation of this set of instructions produces : \begin{eqnarray} <var:s> := <nat:2> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:2> \} \\ <var:s> := <nat:4> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:3> + <nat:3> \times <var:N> ^ <nat:2> + <nat:2> \times <var:N> + <nat:4> \} \\ <var:s> := <nat:2> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:5> + <nat:4> \times <var:N> ^ <nat:4> + <nat:3> \times <var:N> ^ <nat:3> + <nat:2> \times <var:N> ^ <nat:2> + <nat:4> \times <var:N> + <nat:2> \} \\ <var:s> := <nat:3> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:6> + <nat:4> \times <var:N> ^ <nat:5> + <nat:3> \times <var:N> ^ <nat:4> + <nat:2> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> ^ <nat:2> + <nat:2> \times <var:N> + <nat:3> \} \\ <var:s> := <nat:2> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:7> + <nat:4> \times <var:N> ^ <nat:6> + <nat:3> \times <var:N> ^ <nat:5> + <nat:2> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:3> + <nat:2> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:2> \} \\ <var:s> := <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:8> + <nat:4> \times <var:N> ^ <nat:7> + <nat:3> \times <var:N> ^ <nat:6> + <nat:2> \times <var:N> ^ <nat:5> + <nat:4> \times <var:N> ^ <nat:4> + <nat:2> \times <var:N> ^ <nat:3> + <nat:3> \times <var:N> ^ <nat:2> + <nat:2> \times <var:N> + \} \\ <var:s> := <nat:4> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:9> + <nat:4> \times <var:N> ^ <nat:8> + <nat:3> \times <var:N> ^ <nat:7> + <nat:2> \times <var:N> ^ <nat:6> + <nat:4> \times <var:N> ^ <nat:5> + <nat:2> \times <var:N> ^ <nat:4> + <nat:3> \times <var:N> ^ <nat:3> + <nat:2> \times <var:N> ^ <nat:2> + <nat:4> \} \\ <var:s> := <nat:4> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:10> + <nat:4> \times <var:N> ^ <nat:9> + <nat:3> \times <var:N> ^ <nat:8> + <nat:2> \times <var:N> ^ <nat:7> + <nat:4> \times <var:N> ^ <nat:6> + <nat:2> \times <var:N> ^ <nat:5> + <nat:3> \times <var:N> ^ <nat:4> + <nat:2> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> + <nat:4> \} \\ <var:s> := <nat:3> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:11> + <nat:4> \times <var:N> ^ <nat:10> + <nat:3> \times <var:N> ^ <nat:9> + <nat:2> \times <var:N> ^ <nat:8> + <nat:4> \times <var:N> ^ <nat:7> + <nat:2> \times <var:N> ^ <nat:6> + <nat:3> \times <var:N> ^ <nat:5> + <nat:2> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:2> + <nat:4> \times <var:N> + <nat:3> \} \\ <var:s> := <nat:3> + <var:s> * <var:r> ; \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:12> + <nat:4> \times <var:N> ^ <nat:11> + <nat:3> \times <var:N> ^ <nat:10> + <nat:2> \times <var:N> ^ <nat:9> + <nat:4> \times <var:N> ^ <nat:8> + <nat:2> \times <var:N> ^ <nat:7> + <nat:3> \times <var:N> ^ <nat:6> + <nat:2> \times <var:N> ^ <nat:5> + <nat:4> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:3> \} \\ <var:s> := <nat:3> + <var:s> * <var:r> \\ \{ <var:r> = <var:N> \land <var:s> = <nat:1> \times <var:N> ^ <nat:13> + <nat:4> \times <var:N> ^ <nat:12> + <nat:3> \times <var:N> ^ <nat:11> + <nat:2> \times <var:N> ^ <nat:10> + <nat:4> \times <var:N> ^ <nat:9> + <nat:2> \times <var:N> ^ <nat:8> + <nat:3> \times <var:N> ^ <nat:7> + <nat:2> \times <var:N> ^ <nat:6> + <nat:4> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:3> + <nat:3> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:3> \} \end{eqnarray} Utilizing typical Hoare reasoning finishes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:s> := <nat:1> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:2> + <var:s> * <var:r> ; <var:s> := <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:4> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> ; <var:s> := <nat:3> + <var:s> * <var:r> . Theorem <genH:poly_code_correct> : forall <var:N> : nat , {{ <var:r> = <var:N> }} <def:poly> {{ <var:s> = <nat:1> * <var:N> ^ <nat:13> + <nat:4> * <var:N> ^ <nat:12> + <nat:3> * <var:N> ^ <nat:11> + <nat:2> * <var:N> ^ <nat:10> + <nat:4> * <var:N> ^ <nat:9> + <nat:2> * <var:N> ^ <nat:8> + <nat:3> * <var:N> ^ <nat:7> + <nat:2> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:3> + <nat:3> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:2> + <nat:4> * <var:N> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:3> + <nat:4> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:3> + <nat:3> * <var:N> ^ <nat:2> + <nat:2> * <var:N> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:5> + <nat:4> * <var:N> ^ <nat:4> + <nat:3> * <var:N> ^ <nat:3> + <nat:2> * <var:N> ^ <nat:2> + <nat:4> * <var:N> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:5> + <nat:3> * <var:N> ^ <nat:4> + <nat:2> * <var:N> ^ <nat:3> + <nat:4> * <var:N> ^ <nat:2> + <nat:2> * <var:N> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:7> + <nat:4> * <var:N> ^ <nat:6> + <nat:3> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:3> + <nat:2> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:8> + <nat:4> * <var:N> ^ <nat:7> + <nat:3> * <var:N> ^ <nat:6> + <nat:2> * <var:N> ^ <nat:5> + <nat:4> * <var:N> ^ <nat:4> + <nat:2> * <var:N> ^ <nat:3> + <nat:3> * <var:N> ^ <nat:2> + <nat:2> * <var:N> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:9> + <nat:4> * <var:N> ^ <nat:8> + <nat:3> * <var:N> ^ <nat:7> + <nat:2> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:4> + <nat:3> * <var:N> ^ <nat:3> + <nat:2> * <var:N> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:10> + <nat:4> * <var:N> ^ <nat:9> + <nat:3> * <var:N> ^ <nat:8> + <nat:2> * <var:N> ^ <nat:7> + <nat:4> * <var:N> ^ <nat:6> + <nat:2> * <var:N> ^ <nat:5> + <nat:3> * <var:N> ^ <nat:4> + <nat:2> * <var:N> ^ <nat:3> + <nat:4> * <var:N> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:11> + <nat:4> * <var:N> ^ <nat:10> + <nat:3> * <var:N> ^ <nat:9> + <nat:2> * <var:N> ^ <nat:8> + <nat:4> * <var:N> ^ <nat:7> + <nat:2> * <var:N> ^ <nat:6> + <nat:3> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:2> + <nat:4> * <var:N> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:12> + <nat:4> * <var:N> ^ <nat:11> + <nat:3> * <var:N> ^ <nat:10> + <nat:2> * <var:N> ^ <nat:9> + <nat:4> * <var:N> ^ <nat:8> + <nat:2> * <var:N> ^ <nat:7> + <nat:3> * <var:N> ^ <nat:6> + <nat:2> * <var:N> ^ <nat:5> + <nat:4> * <var:N> ^ <nat:3> + <nat:4> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:r> = <var:N> /\ <var:s> = <nat:1> * <var:N> ^ <nat:13> + <nat:4> * <var:N> ^ <nat:12> + <nat:3> * <var:N> ^ <nat:11> + <nat:2> * <var:N> ^ <nat:10> + <nat:4> * <var:N> ^ <nat:9> + <nat:2> * <var:N> ^ <nat:8> + <nat:3> * <var:N> ^ <nat:7> + <nat:2> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:3> + <nat:3> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 