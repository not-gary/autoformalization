\begin{document} \begin{definition} We define the following code <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:N> := <nat:4> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> ; <var:N> := <nat:3> + <var:N> * <var:z> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:3> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> ; <var:N> := <var:N> * <var:z> ; <var:N> := <var:N> * <var:z> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> \end{verbatim} \end{definition} \begin{theorem} We define the following code <def:poly> . Assume that $ <var:z> = <var:W> $ --- given any positive numbers $ <var:W> $ --- before the execution of this series of commands then we see $ <var:N> = <nat:4> \times <var:W> ^ <nat:10> + <nat:2> \times <var:W> ^ <nat:9> + <nat:4> \times <var:W> ^ <nat:8> + <nat:3> \times <var:W> ^ <nat:7> + <nat:2> \times <var:W> ^ <nat:6> + <nat:3> \times <var:W> ^ <nat:5> + <nat:4> \times <var:W> ^ <nat:4> + <nat:2> \times <var:W> + <nat:4> $ once set of commands has executed . \end{theorem} \begin{proof} Let the following decorated code segment . \begin{eqnarray} \{ <var:z> = <var:W> \} \\ <var:N> := <nat:4> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> \} \\ <var:N> := <nat:2> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> + <nat:2> \} \\ <var:N> := <nat:4> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:2> + <nat:2> * <var:W> + <nat:4> \} \\ <var:N> := <nat:3> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:3> + <nat:2> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + <nat:3> \} \\ <var:N> := <nat:2> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + <nat:3> * <var:W> + <nat:2> \} \\ <var:N> := <nat:3> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:3> * <var:W> ^ <nat:2> + <nat:2> * <var:W> + <nat:3> \} \\ <var:N> := <nat:4> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:3> * <var:W> ^ <nat:3> + <nat:2> * <var:W> ^ <nat:2> + <nat:3> * <var:W> + <nat:4> \} \\ <var:N> := <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:4> * <var:W> ^ <nat:5> + <nat:3> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:3> + <nat:3> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + \} \\ <var:N> := <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:8> + <nat:2> * <var:W> ^ <nat:7> + <nat:4> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:4> + <nat:3> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + \} \\ <var:N> := <nat:2> + <var:N> * <var:z> ; \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:9> + <nat:2> * <var:W> ^ <nat:8> + <nat:4> * <var:W> ^ <nat:7> + <nat:3> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:5> + <nat:3> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:2> \} \\ <var:N> := <nat:4> + <var:N> * <var:z> \\ \{ <var:z> = <var:W> \land <var:N> = <nat:4> * <var:W> ^ <nat:10> + <nat:2> * <var:W> ^ <nat:9> + <nat:4> * <var:W> ^ <nat:8> + <nat:3> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:2> * <var:W> + <nat:4> \} \end{eqnarray} Utilizing typical Hoare techniques completes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:N> := <nat:4> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> ; <var:N> := <nat:3> + <var:N> * <var:z> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:3> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> ; <var:N> := <var:N> * <var:z> ; <var:N> := <var:N> * <var:z> ; <var:N> := <nat:2> + <var:N> * <var:z> ; <var:N> := <nat:4> + <var:N> * <var:z> . Theorem <genH:poly_code_correct> : forall <var:W> : nat , {{ <var:z> = <var:W> }} <def:poly> {{ <var:N> = <nat:4> * <var:W> ^ <nat:10> + <nat:2> * <var:W> ^ <nat:9> + <nat:4> * <var:W> ^ <nat:8> + <nat:3> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:2> * <var:W> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:2> + <nat:2> * <var:W> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:3> + <nat:2> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + <nat:3> * <var:W> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:3> * <var:W> ^ <nat:2> + <nat:2> * <var:W> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:3> * <var:W> ^ <nat:3> + <nat:2> * <var:W> ^ <nat:2> + <nat:3> * <var:W> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:4> * <var:W> ^ <nat:5> + <nat:3> * <var:W> ^ <nat:4> + <nat:2> * <var:W> ^ <nat:3> + <nat:3> * <var:W> ^ <nat:2> + <nat:4> * <var:W> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:8> + <nat:2> * <var:W> ^ <nat:7> + <nat:4> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:2> * <var:W> ^ <nat:4> + <nat:3> * <var:W> ^ <nat:3> + <nat:4> * <var:W> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:9> + <nat:2> * <var:W> ^ <nat:8> + <nat:4> * <var:W> ^ <nat:7> + <nat:3> * <var:W> ^ <nat:6> + <nat:2> * <var:W> ^ <nat:5> + <nat:3> * <var:W> ^ <nat:4> + <nat:4> * <var:W> ^ <nat:3> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:W> /\ <var:N> = <nat:4> * <var:W> ^ <nat:10> + <nat:2> * <var:W> ^ <nat:9> + <nat:4> * <var:W> ^ <nat:8> + <nat:3> * <var:W> ^ <nat:7> + <nat:2> * <var:W> ^ <nat:6> + <nat:3> * <var:W> ^ <nat:5> + <nat:4> * <var:W> ^ <nat:4> + <nat:2> * <var:W> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 