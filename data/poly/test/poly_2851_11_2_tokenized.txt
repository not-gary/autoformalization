\begin{document} \begin{theorem} We define the following list of commands in \emph{Imp} such that \begin{verbatim} <var:V> := <nat:4> ; <var:V> := <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> ; <var:V> := <nat:1> + <var:V> * <var:Z> ; <var:V> := <nat:3> + <var:V> * <var:Z> ; <var:V> := <nat:4> + <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> ; <var:V> := <var:V> * <var:Z> ; <var:V> := <nat:4> + <var:V> * <var:Z> ; <var:V> := <nat:1> + <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> \end{verbatim} Allowing it to be that $ <var:Z> = <var:N> $ ( for any positive integers $ <var:N> $ ) ahead of interpreting our segment of code it must be that $ <var:V> = <nat:4> \cdot <var:N> ^ <nat:10> + <nat:2> \cdot <var:N> ^ <nat:8> + <nat:1> \cdot <var:N> ^ <nat:7> + <nat:3> \cdot <var:N> ^ <nat:6> + <nat:4> \cdot <var:N> ^ <nat:5> + <nat:2> \cdot <var:N> ^ <nat:4> + <nat:4> \cdot <var:N> ^ <nat:2> + <nat:1> \cdot <var:N> + <nat:2> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:Z> = <var:N> \} \\ <var:V> := <nat:4> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \} \\ <var:V> := <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> + \} \\ <var:V> := <nat:2> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:2> + <nat:2> \} \\ <var:V> := <nat:1> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:3> + <nat:2> \times <var:N> + <nat:1> \} \\ <var:V> := <nat:3> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:4> + <nat:2> \times <var:N> ^ <nat:2> + <nat:1> \times <var:N> + <nat:3> \} \\ <var:V> := <nat:4> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:5> + <nat:2> \times <var:N> ^ <nat:3> + <nat:1> \times <var:N> ^ <nat:2> + <nat:3> \times <var:N> + <nat:4> \} \\ <var:V> := <nat:2> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:6> + <nat:2> \times <var:N> ^ <nat:4> + <nat:1> \times <var:N> ^ <nat:3> + <nat:3> \times <var:N> ^ <nat:2> + <nat:4> \times <var:N> + <nat:2> \} \\ <var:V> := <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:7> + <nat:2> \times <var:N> ^ <nat:5> + <nat:1> \times <var:N> ^ <nat:4> + <nat:3> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> ^ <nat:2> + <nat:2> \times <var:N> + \} \\ <var:V> := <nat:4> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:8> + <nat:2> \times <var:N> ^ <nat:6> + <nat:1> \times <var:N> ^ <nat:5> + <nat:3> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:3> + <nat:2> \times <var:N> ^ <nat:2> + <nat:4> \} \\ <var:V> := <nat:1> + <var:V> * <var:Z> ; \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:9> + <nat:2> \times <var:N> ^ <nat:7> + <nat:1> \times <var:N> ^ <nat:6> + <nat:3> \times <var:N> ^ <nat:5> + <nat:4> \times <var:N> ^ <nat:4> + <nat:2> \times <var:N> ^ <nat:3> + <nat:4> \times <var:N> + <nat:1> \} \\ <var:V> := <nat:2> + <var:V> * <var:Z> \\ \{ <var:Z> = <var:N> \land <var:V> = <nat:4> \times <var:N> ^ <nat:10> + <nat:2> \times <var:N> ^ <nat:8> + <nat:1> \times <var:N> ^ <nat:7> + <nat:3> \times <var:N> ^ <nat:6> + <nat:4> \times <var:N> ^ <nat:5> + <nat:2> \times <var:N> ^ <nat:4> + <nat:4> \times <var:N> ^ <nat:2> + <nat:1> \times <var:N> + <nat:2> \} \end{eqnarray} The application of standard Hoare logic resolves the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:N> : nat , {{ <var:Z> = <var:N> }} <var:V> := <nat:4> ; <var:V> := <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> ; <var:V> := <nat:1> + <var:V> * <var:Z> ; <var:V> := <nat:3> + <var:V> * <var:Z> ; <var:V> := <nat:4> + <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> ; <var:V> := <var:V> * <var:Z> ; <var:V> := <nat:4> + <var:V> * <var:Z> ; <var:V> := <nat:1> + <var:V> * <var:Z> ; <var:V> := <nat:2> + <var:V> * <var:Z> {{ <var:V> = <nat:4> * <var:N> ^ <nat:10> + <nat:2> * <var:N> ^ <nat:8> + <nat:1> * <var:N> ^ <nat:7> + <nat:3> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:2> + <nat:1> * <var:N> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:3> + <nat:2> * <var:N> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:4> + <nat:2> * <var:N> ^ <nat:2> + <nat:1> * <var:N> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:3> + <nat:1> * <var:N> ^ <nat:2> + <nat:3> * <var:N> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:6> + <nat:2> * <var:N> ^ <nat:4> + <nat:1> * <var:N> ^ <nat:3> + <nat:3> * <var:N> ^ <nat:2> + <nat:4> * <var:N> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:7> + <nat:2> * <var:N> ^ <nat:5> + <nat:1> * <var:N> ^ <nat:4> + <nat:3> * <var:N> ^ <nat:3> + <nat:4> * <var:N> ^ <nat:2> + <nat:2> * <var:N> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:8> + <nat:2> * <var:N> ^ <nat:6> + <nat:1> * <var:N> ^ <nat:5> + <nat:3> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:3> + <nat:2> * <var:N> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:9> + <nat:2> * <var:N> ^ <nat:7> + <nat:1> * <var:N> ^ <nat:6> + <nat:3> * <var:N> ^ <nat:5> + <nat:4> * <var:N> ^ <nat:4> + <nat:2> * <var:N> ^ <nat:3> + <nat:4> * <var:N> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:N> /\ <var:V> = <nat:4> * <var:N> ^ <nat:10> + <nat:2> * <var:N> ^ <nat:8> + <nat:1> * <var:N> ^ <nat:7> + <nat:3> * <var:N> ^ <nat:6> + <nat:4> * <var:N> ^ <nat:5> + <nat:2> * <var:N> ^ <nat:4> + <nat:4> * <var:N> ^ <nat:2> + <nat:1> * <var:N> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 