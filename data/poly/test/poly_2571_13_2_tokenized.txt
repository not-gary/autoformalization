\begin{document} \begin{definition} We define a program <def:poly> in \emph{Imp} : \begin{verbatim} <var:h> := <nat:4> ; <var:h> := <nat:1> + <var:h> * <var:X> ; <var:h> := <nat:2> + <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <nat:4> + <var:h> * <var:X> ; <var:h> := <nat:2> + <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <nat:4> + <var:h> * <var:X> \end{verbatim} \end{definition} \begin{theorem} Given for all $ <var:D> $ that we have $ <var:X> = <var:D> $ before evaluating this set of instructions it follows that we see $ <var:h> = <nat:4> \cdot <var:D> ^ <nat:12> + <nat:1> \cdot <var:D> ^ <nat:11> + <nat:2> \cdot <var:D> ^ <nat:10> + <nat:3> \cdot <var:D> ^ <nat:6> + <nat:3> \cdot <var:D> ^ <nat:5> + <nat:4> \cdot <var:D> ^ <nat:4> + <nat:2> \cdot <var:D> ^ <nat:3> + <nat:3> \cdot <var:D> ^ <nat:2> + <nat:4> $ when list of instructions executes . \end{theorem} \begin{proof} The way to prove correctness for our code can be done using this decorated code segment . \begin{eqnarray} \{ <var:X> = <var:D> \} \\ <var:h> := <nat:4> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> \} \\ <var:h> := <nat:1> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> + <nat:1> \} \\ <var:h> := <nat:2> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:2> \} \\ <var:h> := <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + \} \\ <var:h> := <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + \} \\ <var:h> := <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + \} \\ <var:h> := <nat:3> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> \} \\ <var:h> := <nat:3> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:2> * <var:D> ^ <nat:5> + <nat:3> * <var:D> + <nat:3> \} \\ <var:h> := <nat:4> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:8> + <nat:1> * <var:D> ^ <nat:7> + <nat:2> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:4> \} \\ <var:h> := <nat:2> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:9> + <nat:1> * <var:D> ^ <nat:8> + <nat:2> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:2> \} \\ <var:h> := <nat:3> + <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:10> + <nat:1> * <var:D> ^ <nat:9> + <nat:2> * <var:D> ^ <nat:8> + <nat:3> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:3> \} \\ <var:h> := <var:h> * <var:X> ; \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:11> + <nat:1> * <var:D> ^ <nat:10> + <nat:2> * <var:D> ^ <nat:9> + <nat:3> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + \} \\ <var:h> := <nat:4> + <var:h> * <var:X> \\ \{ <var:X> = <var:D> \land <var:h> = <nat:4> * <var:D> ^ <nat:12> + <nat:1> * <var:D> ^ <nat:11> + <nat:2> * <var:D> ^ <nat:10> + <nat:3> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:4> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> \} \end{eqnarray} The application of Hoare logic completes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:h> := <nat:4> ; <var:h> := <nat:1> + <var:h> * <var:X> ; <var:h> := <nat:2> + <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <nat:4> + <var:h> * <var:X> ; <var:h> := <nat:2> + <var:h> * <var:X> ; <var:h> := <nat:3> + <var:h> * <var:X> ; <var:h> := <var:h> * <var:X> ; <var:h> := <nat:4> + <var:h> * <var:X> . Theorem <genH:poly_code_correct> : forall <var:D> : nat , {{ <var:X> = <var:D> }} <def:poly> {{ <var:h> = <nat:4> * <var:D> ^ <nat:12> + <nat:1> * <var:D> ^ <nat:11> + <nat:2> * <var:D> ^ <nat:10> + <nat:3> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:4> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:2> + <nat:1> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:3> + <nat:1> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:4> + <nat:1> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:5> + <nat:1> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:6> + <nat:1> * <var:D> ^ <nat:5> + <nat:2> * <var:D> ^ <nat:4> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:7> + <nat:1> * <var:D> ^ <nat:6> + <nat:2> * <var:D> ^ <nat:5> + <nat:3> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:8> + <nat:1> * <var:D> ^ <nat:7> + <nat:2> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:9> + <nat:1> * <var:D> ^ <nat:8> + <nat:2> * <var:D> ^ <nat:7> + <nat:3> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> * <var:D> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:10> + <nat:1> * <var:D> ^ <nat:9> + <nat:2> * <var:D> ^ <nat:8> + <nat:3> * <var:D> ^ <nat:4> + <nat:3> * <var:D> ^ <nat:3> + <nat:4> * <var:D> ^ <nat:2> + <nat:2> * <var:D> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:11> + <nat:1> * <var:D> ^ <nat:10> + <nat:2> * <var:D> ^ <nat:9> + <nat:3> * <var:D> ^ <nat:5> + <nat:3> * <var:D> ^ <nat:4> + <nat:4> * <var:D> ^ <nat:3> + <nat:2> * <var:D> ^ <nat:2> + <nat:3> * <var:D> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:X> = <var:D> /\ <var:h> = <nat:4> * <var:D> ^ <nat:12> + <nat:1> * <var:D> ^ <nat:11> + <nat:2> * <var:D> ^ <nat:10> + <nat:3> * <var:D> ^ <nat:6> + <nat:3> * <var:D> ^ <nat:5> + <nat:4> * <var:D> ^ <nat:4> + <nat:2> * <var:D> ^ <nat:3> + <nat:3> * <var:D> ^ <nat:2> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 