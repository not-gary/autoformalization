\begin{document} \begin{theorem} Let the following program : \begin{verbatim} <var:G> := <nat:3> ; <var:G> := <nat:2> + <var:G> * <var:j> ; <var:G> := <var:G> * <var:j> ; <var:G> := <nat:4> + <var:G> * <var:j> ; <var:G> := <nat:4> + <var:G> * <var:j> ; <var:G> := <nat:1> + <var:G> * <var:j> ; <var:G> := <nat:3> + <var:G> * <var:j> ; <var:G> := <var:G> * <var:j> ; <var:G> := <nat:2> + <var:G> * <var:j> \end{verbatim} Assuming it to be that $ <var:j> = <var:V> $ before evaluating our list of instructions then we see $ <var:G> = <nat:3> \times <var:V> ^ <nat:8> + <nat:2> \times <var:V> ^ <nat:7> + <nat:4> \times <var:V> ^ <nat:5> + <nat:4> \times <var:V> ^ <nat:4> + <nat:1> \times <var:V> ^ <nat:3> + <nat:3> \times <var:V> ^ <nat:2> + <nat:2> $ when series of instructions executes . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:j> = <var:V> \} \\ <var:G> := <nat:3> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> \} \\ <var:G> := <nat:2> + <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> + <nat:2> \} \\ <var:G> := <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:2> + <nat:2> * <var:V> + \} \\ <var:G> := <nat:4> + <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:3> + <nat:2> * <var:V> ^ <nat:2> + <nat:4> \} \\ <var:G> := <nat:4> + <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:4> + <nat:2> * <var:V> ^ <nat:3> + <nat:4> * <var:V> + <nat:4> \} \\ <var:G> := <nat:1> + <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:5> + <nat:2> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:1> \} \\ <var:G> := <nat:3> + <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:6> + <nat:2> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + <nat:3> \} \\ <var:G> := <var:G> * <var:j> ; \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:7> + <nat:2> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + \} \\ <var:G> := <nat:2> + <var:G> * <var:j> \\ \{ <var:j> = <var:V> \land <var:G> = <nat:3> * <var:V> ^ <nat:8> + <nat:2> * <var:V> ^ <nat:7> + <nat:4> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + <nat:2> \} \end{eqnarray} The application of standard Hoare reasoning accomplishes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:V> : nat , {{ <var:j> = <var:V> }} <var:G> := <nat:3> ; <var:G> := <nat:2> + <var:G> * <var:j> ; <var:G> := <var:G> * <var:j> ; <var:G> := <nat:4> + <var:G> * <var:j> ; <var:G> := <nat:4> + <var:G> * <var:j> ; <var:G> := <nat:1> + <var:G> * <var:j> ; <var:G> := <nat:3> + <var:G> * <var:j> ; <var:G> := <var:G> * <var:j> ; <var:G> := <nat:2> + <var:G> * <var:j> {{ <var:G> = <nat:3> * <var:V> ^ <nat:8> + <nat:2> * <var:V> ^ <nat:7> + <nat:4> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:2> + <nat:2> * <var:V> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:3> + <nat:2> * <var:V> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:4> + <nat:2> * <var:V> ^ <nat:3> + <nat:4> * <var:V> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:5> + <nat:2> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:6> + <nat:2> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:7> + <nat:2> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:3> * <var:V> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:j> = <var:V> /\ <var:G> = <nat:3> * <var:V> ^ <nat:8> + <nat:2> * <var:V> ^ <nat:7> + <nat:4> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:3> * <var:V> ^ <nat:2> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 