\begin{document} \begin{theorem} Define the following series of commands in \emph{Imp} as \begin{verbatim} <var:o> := <nat:4> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <nat:3> + <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> ; <var:o> := <nat:3> + <var:o> * <var:y> ; <var:o> := <nat:1> + <var:o> * <var:y> ; <var:o> := <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <var:o> * <var:y> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> \end{verbatim} Given it to be that $ <var:y> = <var:E> $ prior to the execution of our code segment , it must be that $ <var:o> = <nat:4> \cdot <var:E> ^ <nat:11> + <nat:2> \cdot <var:E> ^ <nat:10> + <nat:3> \cdot <var:E> ^ <nat:9> + <nat:4> \cdot <var:E> ^ <nat:8> + <nat:3> \cdot <var:E> ^ <nat:7> + <nat:1> \cdot <var:E> ^ <nat:6> + <nat:4> \cdot <var:E> ^ <nat:4> + <nat:2> \cdot <var:E> ^ <nat:3> + <nat:2> \cdot <var:E> + <nat:4> $ . \end{theorem} \begin{proof} Applying standard Hoare techniques produces : \begin{eqnarray} \{ <var:y> = <var:E> \} \\ <var:o> := <nat:4> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \} \\ <var:o> := <nat:2> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> + <nat:2> \} \\ <var:o> := <nat:3> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:2> + <nat:2> \times <var:E> + <nat:3> \} \\ <var:o> := <nat:4> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:3> + <nat:2> \times <var:E> ^ <nat:2> + <nat:3> \times <var:E> + <nat:4> \} \\ <var:o> := <nat:3> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:4> + <nat:2> \times <var:E> ^ <nat:3> + <nat:3> \times <var:E> ^ <nat:2> + <nat:4> \times <var:E> + <nat:3> \} \\ <var:o> := <nat:1> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:5> + <nat:2> \times <var:E> ^ <nat:4> + <nat:3> \times <var:E> ^ <nat:3> + <nat:4> \times <var:E> ^ <nat:2> + <nat:3> \times <var:E> + <nat:1> \} \\ <var:o> := <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:6> + <nat:2> \times <var:E> ^ <nat:5> + <nat:3> \times <var:E> ^ <nat:4> + <nat:4> \times <var:E> ^ <nat:3> + <nat:3> \times <var:E> ^ <nat:2> + <nat:1> \times <var:E> + \} \\ <var:o> := <nat:4> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:7> + <nat:2> \times <var:E> ^ <nat:6> + <nat:3> \times <var:E> ^ <nat:5> + <nat:4> \times <var:E> ^ <nat:4> + <nat:3> \times <var:E> ^ <nat:3> + <nat:1> \times <var:E> ^ <nat:2> + <nat:4> \} \\ <var:o> := <nat:2> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:8> + <nat:2> \times <var:E> ^ <nat:7> + <nat:3> \times <var:E> ^ <nat:6> + <nat:4> \times <var:E> ^ <nat:5> + <nat:3> \times <var:E> ^ <nat:4> + <nat:1> \times <var:E> ^ <nat:3> + <nat:4> \times <var:E> + <nat:2> \} \\ <var:o> := <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:9> + <nat:2> \times <var:E> ^ <nat:8> + <nat:3> \times <var:E> ^ <nat:7> + <nat:4> \times <var:E> ^ <nat:6> + <nat:3> \times <var:E> ^ <nat:5> + <nat:1> \times <var:E> ^ <nat:4> + <nat:4> \times <var:E> ^ <nat:2> + <nat:2> \times <var:E> + \} \\ <var:o> := <nat:2> + <var:o> * <var:y> ; \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:10> + <nat:2> \times <var:E> ^ <nat:9> + <nat:3> \times <var:E> ^ <nat:8> + <nat:4> \times <var:E> ^ <nat:7> + <nat:3> \times <var:E> ^ <nat:6> + <nat:1> \times <var:E> ^ <nat:5> + <nat:4> \times <var:E> ^ <nat:3> + <nat:2> \times <var:E> ^ <nat:2> + <nat:2> \} \\ <var:o> := <nat:4> + <var:o> * <var:y> \\ \{ <var:y> = <var:E> \land <var:o> = <nat:4> \times <var:E> ^ <nat:11> + <nat:2> \times <var:E> ^ <nat:10> + <nat:3> \times <var:E> ^ <nat:9> + <nat:4> \times <var:E> ^ <nat:8> + <nat:3> \times <var:E> ^ <nat:7> + <nat:1> \times <var:E> ^ <nat:6> + <nat:4> \times <var:E> ^ <nat:4> + <nat:2> \times <var:E> ^ <nat:3> + <nat:2> \times <var:E> + <nat:4> \} \end{eqnarray} Thusly , our segment of code has been proven correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:E> : nat , {{ <var:y> = <var:E> }} <var:o> := <nat:4> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <nat:3> + <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> ; <var:o> := <nat:3> + <var:o> * <var:y> ; <var:o> := <nat:1> + <var:o> * <var:y> ; <var:o> := <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <var:o> * <var:y> ; <var:o> := <nat:2> + <var:o> * <var:y> ; <var:o> := <nat:4> + <var:o> * <var:y> {{ <var:o> = <nat:4> * <var:E> ^ <nat:11> + <nat:2> * <var:E> ^ <nat:10> + <nat:3> * <var:E> ^ <nat:9> + <nat:4> * <var:E> ^ <nat:8> + <nat:3> * <var:E> ^ <nat:7> + <nat:1> * <var:E> ^ <nat:6> + <nat:4> * <var:E> ^ <nat:4> + <nat:2> * <var:E> ^ <nat:3> + <nat:2> * <var:E> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:2> + <nat:2> * <var:E> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:3> + <nat:2> * <var:E> ^ <nat:2> + <nat:3> * <var:E> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:4> + <nat:2> * <var:E> ^ <nat:3> + <nat:3> * <var:E> ^ <nat:2> + <nat:4> * <var:E> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:5> + <nat:2> * <var:E> ^ <nat:4> + <nat:3> * <var:E> ^ <nat:3> + <nat:4> * <var:E> ^ <nat:2> + <nat:3> * <var:E> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:6> + <nat:2> * <var:E> ^ <nat:5> + <nat:3> * <var:E> ^ <nat:4> + <nat:4> * <var:E> ^ <nat:3> + <nat:3> * <var:E> ^ <nat:2> + <nat:1> * <var:E> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:7> + <nat:2> * <var:E> ^ <nat:6> + <nat:3> * <var:E> ^ <nat:5> + <nat:4> * <var:E> ^ <nat:4> + <nat:3> * <var:E> ^ <nat:3> + <nat:1> * <var:E> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:8> + <nat:2> * <var:E> ^ <nat:7> + <nat:3> * <var:E> ^ <nat:6> + <nat:4> * <var:E> ^ <nat:5> + <nat:3> * <var:E> ^ <nat:4> + <nat:1> * <var:E> ^ <nat:3> + <nat:4> * <var:E> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:9> + <nat:2> * <var:E> ^ <nat:8> + <nat:3> * <var:E> ^ <nat:7> + <nat:4> * <var:E> ^ <nat:6> + <nat:3> * <var:E> ^ <nat:5> + <nat:1> * <var:E> ^ <nat:4> + <nat:4> * <var:E> ^ <nat:2> + <nat:2> * <var:E> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:10> + <nat:2> * <var:E> ^ <nat:9> + <nat:3> * <var:E> ^ <nat:8> + <nat:4> * <var:E> ^ <nat:7> + <nat:3> * <var:E> ^ <nat:6> + <nat:1> * <var:E> ^ <nat:5> + <nat:4> * <var:E> ^ <nat:3> + <nat:2> * <var:E> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:y> = <var:E> /\ <var:o> = <nat:4> * <var:E> ^ <nat:11> + <nat:2> * <var:E> ^ <nat:10> + <nat:3> * <var:E> ^ <nat:9> + <nat:4> * <var:E> ^ <nat:8> + <nat:3> * <var:E> ^ <nat:7> + <nat:1> * <var:E> ^ <nat:6> + <nat:4> * <var:E> ^ <nat:4> + <nat:2> * <var:E> ^ <nat:3> + <nat:2> * <var:E> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 