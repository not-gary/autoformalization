\begin{document} \begin{definition} Define the following list of commands <def:poly> in \emph{Imp} as \begin{verbatim} <var:z> := <nat:4> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:1> + <var:z> * <var:O> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> ; <var:z> := <var:z> * <var:O> ; <var:z> := <nat:3> + <var:z> * <var:O> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> \end{verbatim} \end{definition} \begin{theorem} Given it to be that $ <var:O> = <var:n> $ --- for all natural coefficients $ <var:n> $ --- ahead of running our set of commands , it follows that we see $ <var:z> = <nat:4> \cdot <var:n> ^ <nat:9> + <nat:4> \cdot <var:n> ^ <nat:8> + <nat:1> \cdot <var:n> ^ <nat:7> + <nat:4> \cdot <var:n> ^ <nat:6> + <nat:2> \cdot <var:n> ^ <nat:5> + <nat:2> \cdot <var:n> ^ <nat:4> + <nat:3> \cdot <var:n> ^ <nat:2> + <nat:4> \cdot <var:n> + <nat:2> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:O> = <var:n> \} \\ <var:z> := <nat:4> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \} \\ <var:z> := <nat:4> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> + <nat:4> \} \\ <var:z> := <nat:1> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:1> \} \\ <var:z> := <nat:4> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:3> + <nat:4> \times <var:n> ^ <nat:2> + <nat:1> \times <var:n> + <nat:4> \} \\ <var:z> := <nat:2> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:4> + <nat:4> \times <var:n> ^ <nat:3> + <nat:1> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:2> \} \\ <var:z> := <nat:2> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:5> + <nat:4> \times <var:n> ^ <nat:4> + <nat:1> \times <var:n> ^ <nat:3> + <nat:4> \times <var:n> ^ <nat:2> + <nat:2> \times <var:n> + <nat:2> \} \\ <var:z> := <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:6> + <nat:4> \times <var:n> ^ <nat:5> + <nat:1> \times <var:n> ^ <nat:4> + <nat:4> \times <var:n> ^ <nat:3> + <nat:2> \times <var:n> ^ <nat:2> + <nat:2> \times <var:n> + \} \\ <var:z> := <nat:3> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:7> + <nat:4> \times <var:n> ^ <nat:6> + <nat:1> \times <var:n> ^ <nat:5> + <nat:4> \times <var:n> ^ <nat:4> + <nat:2> \times <var:n> ^ <nat:3> + <nat:2> \times <var:n> ^ <nat:2> + <nat:3> \} \\ <var:z> := <nat:4> + <var:z> * <var:O> ; \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:8> + <nat:4> \times <var:n> ^ <nat:7> + <nat:1> \times <var:n> ^ <nat:6> + <nat:4> \times <var:n> ^ <nat:5> + <nat:2> \times <var:n> ^ <nat:4> + <nat:2> \times <var:n> ^ <nat:3> + <nat:3> \times <var:n> + <nat:4> \} \\ <var:z> := <nat:2> + <var:z> * <var:O> \\ \{ <var:O> = <var:n> \land <var:z> = <nat:4> \times <var:n> ^ <nat:9> + <nat:4> \times <var:n> ^ <nat:8> + <nat:1> \times <var:n> ^ <nat:7> + <nat:4> \times <var:n> ^ <nat:6> + <nat:2> \times <var:n> ^ <nat:5> + <nat:2> \times <var:n> ^ <nat:4> + <nat:3> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:2> \} \end{eqnarray} Using Hoare techniques finishes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:z> := <nat:4> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:1> + <var:z> * <var:O> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> ; <var:z> := <var:z> * <var:O> ; <var:z> := <nat:3> + <var:z> * <var:O> ; <var:z> := <nat:4> + <var:z> * <var:O> ; <var:z> := <nat:2> + <var:z> * <var:O> . Theorem <genH:poly_code_correct> : forall <var:n> : nat , {{ <var:O> = <var:n> }} <def:poly> {{ <var:z> = <nat:4> * <var:n> ^ <nat:9> + <nat:4> * <var:n> ^ <nat:8> + <nat:1> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:2> * <var:n> ^ <nat:5> + <nat:2> * <var:n> ^ <nat:4> + <nat:3> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:3> + <nat:4> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:4> + <nat:4> * <var:n> ^ <nat:3> + <nat:1> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:4> * <var:n> ^ <nat:2> + <nat:2> * <var:n> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:6> + <nat:4> * <var:n> ^ <nat:5> + <nat:1> * <var:n> ^ <nat:4> + <nat:4> * <var:n> ^ <nat:3> + <nat:2> * <var:n> ^ <nat:2> + <nat:2> * <var:n> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:2> * <var:n> ^ <nat:3> + <nat:2> * <var:n> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:8> + <nat:4> * <var:n> ^ <nat:7> + <nat:1> * <var:n> ^ <nat:6> + <nat:4> * <var:n> ^ <nat:5> + <nat:2> * <var:n> ^ <nat:4> + <nat:2> * <var:n> ^ <nat:3> + <nat:3> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:O> = <var:n> /\ <var:z> = <nat:4> * <var:n> ^ <nat:9> + <nat:4> * <var:n> ^ <nat:8> + <nat:1> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:2> * <var:n> ^ <nat:5> + <nat:2> * <var:n> ^ <nat:4> + <nat:3> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 