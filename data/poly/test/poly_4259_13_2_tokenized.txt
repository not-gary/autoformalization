\begin{document} \begin{theorem} Take the following set of commands as follows \begin{verbatim} <var:D> := <nat:2> ; <var:D> := <nat:2> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:3> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> ; <var:D> := <nat:3> + <var:D> * <var:d> ; <var:D> := <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> \end{verbatim} Given --- given some $ <var:i> $ --- that $ <var:d> = <var:i> $ prior to evaluating this series of commands , it must be that $ <var:D> = <nat:2> \cdot <var:i> ^ <nat:12> + <nat:2> \cdot <var:i> ^ <nat:11> + <nat:4> \cdot <var:i> ^ <nat:10> + <nat:1> \cdot <var:i> ^ <nat:9> + <nat:4> \cdot <var:i> ^ <nat:8> + <nat:3> \cdot <var:i> ^ <nat:7> + <nat:4> \cdot <var:i> ^ <nat:6> + <nat:4> \cdot <var:i> ^ <nat:5> + <nat:1> \cdot <var:i> ^ <nat:3> + <nat:3> \cdot <var:i> ^ <nat:2> + <nat:1> $ once code is done . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:d> = <var:i> \} \\ <var:D> := <nat:2> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> \} \\ <var:D> := <nat:2> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> + <nat:2> \} \\ <var:D> := <nat:4> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:2> + <nat:2> * <var:i> + <nat:4> \} \\ <var:D> := <nat:1> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:3> + <nat:2> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:1> \} \\ <var:D> := <nat:4> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:4> + <nat:2> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:1> * <var:i> + <nat:4> \} \\ <var:D> := <nat:3> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:5> + <nat:2> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:1> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:3> \} \\ <var:D> := <nat:4> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:6> + <nat:2> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:1> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:3> * <var:i> + <nat:4> \} \\ <var:D> := <nat:4> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:7> + <nat:2> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:1> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:4> \} \\ <var:D> := <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:8> + <nat:2> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:1> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:3> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + \} \\ <var:D> := <nat:1> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:9> + <nat:2> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:1> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:3> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:1> \} \\ <var:D> := <nat:3> + <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:10> + <nat:2> * <var:i> ^ <nat:9> + <nat:4> * <var:i> ^ <nat:8> + <nat:1> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:3> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:1> * <var:i> + <nat:3> \} \\ <var:D> := <var:D> * <var:d> ; \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:11> + <nat:2> * <var:i> ^ <nat:10> + <nat:4> * <var:i> ^ <nat:9> + <nat:1> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:3> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:1> * <var:i> ^ <nat:2> + <nat:3> * <var:i> + \} \end{eqnarray} Advancing executing this series of commands gives : \begin{eqnarray} <var:D> := <nat:1> + <var:D> * <var:d> \\ \{ <var:d> = <var:i> \land <var:D> = <nat:2> * <var:i> ^ <nat:12> + <nat:2> * <var:i> ^ <nat:11> + <nat:4> * <var:i> ^ <nat:10> + <nat:1> * <var:i> ^ <nat:9> + <nat:4> * <var:i> ^ <nat:8> + <nat:3> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:1> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:1> \} \end{eqnarray} Utilizing usual Hoare logic finishes the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:i> : nat , {{ <var:d> = <var:i> }} <var:D> := <nat:2> ; <var:D> := <nat:2> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:3> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <nat:4> + <var:D> * <var:d> ; <var:D> := <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> ; <var:D> := <nat:3> + <var:D> * <var:d> ; <var:D> := <var:D> * <var:d> ; <var:D> := <nat:1> + <var:D> * <var:d> {{ <var:D> = <nat:2> * <var:i> ^ <nat:12> + <nat:2> * <var:i> ^ <nat:11> + <nat:4> * <var:i> ^ <nat:10> + <nat:1> * <var:i> ^ <nat:9> + <nat:4> * <var:i> ^ <nat:8> + <nat:3> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:1> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:2> + <nat:2> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:3> + <nat:2> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:4> + <nat:2> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:1> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:5> + <nat:2> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:1> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:6> + <nat:2> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:1> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:3> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:7> + <nat:2> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:1> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:8> + <nat:2> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:1> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:3> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:4> * <var:i> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:9> + <nat:2> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:1> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:3> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:4> * <var:i> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:10> + <nat:2> * <var:i> ^ <nat:9> + <nat:4> * <var:i> ^ <nat:8> + <nat:1> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:3> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:4> * <var:i> ^ <nat:3> + <nat:1> * <var:i> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:11> + <nat:2> * <var:i> ^ <nat:10> + <nat:4> * <var:i> ^ <nat:9> + <nat:1> * <var:i> ^ <nat:8> + <nat:4> * <var:i> ^ <nat:7> + <nat:3> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:4> * <var:i> ^ <nat:4> + <nat:1> * <var:i> ^ <nat:2> + <nat:3> * <var:i> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:d> = <var:i> /\ <var:D> = <nat:2> * <var:i> ^ <nat:12> + <nat:2> * <var:i> ^ <nat:11> + <nat:4> * <var:i> ^ <nat:10> + <nat:1> * <var:i> ^ <nat:9> + <nat:4> * <var:i> ^ <nat:8> + <nat:3> * <var:i> ^ <nat:7> + <nat:4> * <var:i> ^ <nat:6> + <nat:4> * <var:i> ^ <nat:5> + <nat:1> * <var:i> ^ <nat:3> + <nat:3> * <var:i> ^ <nat:2> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 