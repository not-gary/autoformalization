\begin{document} \begin{definition} Take the following set of instructions <def:poly> in \emph{Imp} : \begin{verbatim} <var:B> := <nat:3> ; <var:B> := <nat:4> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:3> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> \end{verbatim} \end{definition} \begin{theorem} Assume it to hold that $ <var:n> = <var:v> $ , assuming some positive integers $ <var:v> $ , before interpreting the code segment it holds that we have $ <var:B> = <nat:3> \times <var:v> ^ <nat:8> + <nat:4> \times <var:v> ^ <nat:7> + <nat:2> \times <var:v> ^ <nat:6> + <nat:1> \times <var:v> ^ <nat:5> + <nat:1> \times <var:v> ^ <nat:4> + <nat:3> \times <var:v> ^ <nat:3> + <nat:2> \times <var:v> ^ <nat:2> + <nat:1> \times <var:v> + <nat:2> $ when list of commands executes . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:n> = <var:v> \} \\ <var:B> := <nat:3> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \} \\ <var:B> := <nat:4> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> + <nat:4> \} \\ <var:B> := <nat:2> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:2> + <nat:4> \times <var:v> + <nat:2> \} \\ <var:B> := <nat:1> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:3> + <nat:4> \times <var:v> ^ <nat:2> + <nat:2> \times <var:v> + <nat:1> \} \\ <var:B> := <nat:1> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:4> + <nat:4> \times <var:v> ^ <nat:3> + <nat:2> \times <var:v> ^ <nat:2> + <nat:1> \times <var:v> + <nat:1> \} \\ <var:B> := <nat:3> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:5> + <nat:4> \times <var:v> ^ <nat:4> + <nat:2> \times <var:v> ^ <nat:3> + <nat:1> \times <var:v> ^ <nat:2> + <nat:1> \times <var:v> + <nat:3> \} \\ <var:B> := <nat:2> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:6> + <nat:4> \times <var:v> ^ <nat:5> + <nat:2> \times <var:v> ^ <nat:4> + <nat:1> \times <var:v> ^ <nat:3> + <nat:1> \times <var:v> ^ <nat:2> + <nat:3> \times <var:v> + <nat:2> \} \\ <var:B> := <nat:1> + <var:B> * <var:n> ; \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:7> + <nat:4> \times <var:v> ^ <nat:6> + <nat:2> \times <var:v> ^ <nat:5> + <nat:1> \times <var:v> ^ <nat:4> + <nat:1> \times <var:v> ^ <nat:3> + <nat:3> \times <var:v> ^ <nat:2> + <nat:2> \times <var:v> + <nat:1> \} \\ <var:B> := <nat:2> + <var:B> * <var:n> \\ \{ <var:n> = <var:v> \land <var:B> = <nat:3> \times <var:v> ^ <nat:8> + <nat:4> \times <var:v> ^ <nat:7> + <nat:2> \times <var:v> ^ <nat:6> + <nat:1> \times <var:v> ^ <nat:5> + <nat:1> \times <var:v> ^ <nat:4> + <nat:3> \times <var:v> ^ <nat:3> + <nat:2> \times <var:v> ^ <nat:2> + <nat:1> \times <var:v> + <nat:2> \} \end{eqnarray} Applying typical Hoare reasoning resolves the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:B> := <nat:3> ; <var:B> := <nat:4> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:3> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> ; <var:B> := <nat:1> + <var:B> * <var:n> ; <var:B> := <nat:2> + <var:B> * <var:n> . Theorem <genH:poly_code_correct> : forall <var:v> : nat , {{ <var:n> = <var:v> }} <def:poly> {{ <var:B> = <nat:3> * <var:v> ^ <nat:8> + <nat:4> * <var:v> ^ <nat:7> + <nat:2> * <var:v> ^ <nat:6> + <nat:1> * <var:v> ^ <nat:5> + <nat:1> * <var:v> ^ <nat:4> + <nat:3> * <var:v> ^ <nat:3> + <nat:2> * <var:v> ^ <nat:2> + <nat:1> * <var:v> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:2> + <nat:4> * <var:v> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:3> + <nat:4> * <var:v> ^ <nat:2> + <nat:2> * <var:v> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:4> + <nat:4> * <var:v> ^ <nat:3> + <nat:2> * <var:v> ^ <nat:2> + <nat:1> * <var:v> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:5> + <nat:4> * <var:v> ^ <nat:4> + <nat:2> * <var:v> ^ <nat:3> + <nat:1> * <var:v> ^ <nat:2> + <nat:1> * <var:v> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:6> + <nat:4> * <var:v> ^ <nat:5> + <nat:2> * <var:v> ^ <nat:4> + <nat:1> * <var:v> ^ <nat:3> + <nat:1> * <var:v> ^ <nat:2> + <nat:3> * <var:v> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:7> + <nat:4> * <var:v> ^ <nat:6> + <nat:2> * <var:v> ^ <nat:5> + <nat:1> * <var:v> ^ <nat:4> + <nat:1> * <var:v> ^ <nat:3> + <nat:3> * <var:v> ^ <nat:2> + <nat:2> * <var:v> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:n> = <var:v> /\ <var:B> = <nat:3> * <var:v> ^ <nat:8> + <nat:4> * <var:v> ^ <nat:7> + <nat:2> * <var:v> ^ <nat:6> + <nat:1> * <var:v> ^ <nat:5> + <nat:1> * <var:v> ^ <nat:4> + <nat:3> * <var:v> ^ <nat:3> + <nat:2> * <var:v> ^ <nat:2> + <nat:1> * <var:v> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 