\begin{document} \begin{theorem} Take a program as follows \begin{verbatim} <var:j> := <nat:2> ; <var:j> := <nat:2> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:2> + <var:j> * <var:e> ; <var:j> := <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:3> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> \end{verbatim} Assuming $ <var:e> = <var:n> $ before the execution of the list of instructions it must be that $ <var:j> = <nat:2> * <var:n> ^ <nat:13> + <nat:2> * <var:n> ^ <nat:12> + <nat:4> * <var:n> ^ <nat:11> + <nat:1> * <var:n> ^ <nat:10> + <nat:2> * <var:n> ^ <nat:9> + <nat:1> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:3> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:1> $ . \end{theorem} \begin{proof} Applying Hoare reasoning returns : \begin{eqnarray} \{ <var:e> = <var:n> \} \\ <var:j> := <nat:2> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \} \\ <var:j> := <nat:2> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> + <nat:2> \} \\ <var:j> := <nat:4> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:2> + <nat:2> \times <var:n> + <nat:4> \} \\ <var:j> := <nat:1> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:3> + <nat:2> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:1> \} \\ <var:j> := <nat:2> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:4> + <nat:2> \times <var:n> ^ <nat:3> + <nat:4> \times <var:n> ^ <nat:2> + <nat:1> \times <var:n> + <nat:2> \} \\ <var:j> := <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:5> + <nat:2> \times <var:n> ^ <nat:4> + <nat:4> \times <var:n> ^ <nat:3> + <nat:1> \times <var:n> ^ <nat:2> + <nat:2> \times <var:n> + \} \\ <var:j> := <nat:1> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:6> + <nat:2> \times <var:n> ^ <nat:5> + <nat:4> \times <var:n> ^ <nat:4> + <nat:1> \times <var:n> ^ <nat:3> + <nat:2> \times <var:n> ^ <nat:2> + <nat:1> \} \\ <var:j> := <nat:4> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:7> + <nat:2> \times <var:n> ^ <nat:6> + <nat:4> \times <var:n> ^ <nat:5> + <nat:1> \times <var:n> ^ <nat:4> + <nat:2> \times <var:n> ^ <nat:3> + <nat:1> \times <var:n> + <nat:4> \} \\ <var:j> := <nat:1> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:8> + <nat:2> \times <var:n> ^ <nat:7> + <nat:4> \times <var:n> ^ <nat:6> + <nat:1> \times <var:n> ^ <nat:5> + <nat:2> \times <var:n> ^ <nat:4> + <nat:1> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:1> \} \\ <var:j> := <nat:4> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:9> + <nat:2> \times <var:n> ^ <nat:8> + <nat:4> \times <var:n> ^ <nat:7> + <nat:1> \times <var:n> ^ <nat:6> + <nat:2> \times <var:n> ^ <nat:5> + <nat:1> \times <var:n> ^ <nat:3> + <nat:4> \times <var:n> ^ <nat:2> + <nat:1> \times <var:n> + <nat:4> \} \\ <var:j> := <nat:1> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:10> + <nat:2> \times <var:n> ^ <nat:9> + <nat:4> \times <var:n> ^ <nat:8> + <nat:1> \times <var:n> ^ <nat:7> + <nat:2> \times <var:n> ^ <nat:6> + <nat:1> \times <var:n> ^ <nat:4> + <nat:4> \times <var:n> ^ <nat:3> + <nat:1> \times <var:n> ^ <nat:2> + <nat:4> \times <var:n> + <nat:1> \} \end{eqnarray} Allowing interpreting our segment of code produces : \begin{eqnarray} <var:j> := <nat:3> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:11> + <nat:2> \times <var:n> ^ <nat:10> + <nat:4> \times <var:n> ^ <nat:9> + <nat:1> \times <var:n> ^ <nat:8> + <nat:2> \times <var:n> ^ <nat:7> + <nat:1> \times <var:n> ^ <nat:5> + <nat:4> \times <var:n> ^ <nat:4> + <nat:1> \times <var:n> ^ <nat:3> + <nat:4> \times <var:n> ^ <nat:2> + <nat:1> \times <var:n> + <nat:3> \} \\ <var:j> := <nat:1> + <var:j> * <var:e> ; \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:12> + <nat:2> \times <var:n> ^ <nat:11> + <nat:4> \times <var:n> ^ <nat:10> + <nat:1> \times <var:n> ^ <nat:9> + <nat:2> \times <var:n> ^ <nat:8> + <nat:1> \times <var:n> ^ <nat:6> + <nat:4> \times <var:n> ^ <nat:5> + <nat:1> \times <var:n> ^ <nat:4> + <nat:4> \times <var:n> ^ <nat:3> + <nat:1> \times <var:n> ^ <nat:2> + <nat:3> \times <var:n> + <nat:1> \} \\ <var:j> := <nat:1> + <var:j> * <var:e> \\ \{ <var:e> = <var:n> \land <var:j> = <nat:2> \times <var:n> ^ <nat:13> + <nat:2> \times <var:n> ^ <nat:12> + <nat:4> \times <var:n> ^ <nat:11> + <nat:1> \times <var:n> ^ <nat:10> + <nat:2> \times <var:n> ^ <nat:9> + <nat:1> \times <var:n> ^ <nat:7> + <nat:4> \times <var:n> ^ <nat:6> + <nat:1> \times <var:n> ^ <nat:5> + <nat:4> \times <var:n> ^ <nat:4> + <nat:1> \times <var:n> ^ <nat:3> + <nat:3> \times <var:n> ^ <nat:2> + <nat:1> \times <var:n> + <nat:1> \} \end{eqnarray} Therefore , our set of instructions has been shown to be correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:n> : nat , {{ <var:e> = <var:n> }} <var:j> := <nat:2> ; <var:j> := <nat:2> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:2> + <var:j> * <var:e> ; <var:j> := <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:4> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:3> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> ; <var:j> := <nat:1> + <var:j> * <var:e> {{ <var:j> = <nat:2> * <var:n> ^ <nat:13> + <nat:2> * <var:n> ^ <nat:12> + <nat:4> * <var:n> ^ <nat:11> + <nat:1> * <var:n> ^ <nat:10> + <nat:2> * <var:n> ^ <nat:9> + <nat:1> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:3> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:2> + <nat:2> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:3> + <nat:2> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:4> + <nat:2> * <var:n> ^ <nat:3> + <nat:4> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:5> + <nat:2> * <var:n> ^ <nat:4> + <nat:4> * <var:n> ^ <nat:3> + <nat:1> * <var:n> ^ <nat:2> + <nat:2> * <var:n> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:6> + <nat:2> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:2> * <var:n> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:7> + <nat:2> * <var:n> ^ <nat:6> + <nat:4> * <var:n> ^ <nat:5> + <nat:1> * <var:n> ^ <nat:4> + <nat:2> * <var:n> ^ <nat:3> + <nat:1> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:8> + <nat:2> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:5> + <nat:2> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:9> + <nat:2> * <var:n> ^ <nat:8> + <nat:4> * <var:n> ^ <nat:7> + <nat:1> * <var:n> ^ <nat:6> + <nat:2> * <var:n> ^ <nat:5> + <nat:1> * <var:n> ^ <nat:3> + <nat:4> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:10> + <nat:2> * <var:n> ^ <nat:9> + <nat:4> * <var:n> ^ <nat:8> + <nat:1> * <var:n> ^ <nat:7> + <nat:2> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:4> + <nat:4> * <var:n> ^ <nat:3> + <nat:1> * <var:n> ^ <nat:2> + <nat:4> * <var:n> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:11> + <nat:2> * <var:n> ^ <nat:10> + <nat:4> * <var:n> ^ <nat:9> + <nat:1> * <var:n> ^ <nat:8> + <nat:2> * <var:n> ^ <nat:7> + <nat:1> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:4> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:12> + <nat:2> * <var:n> ^ <nat:11> + <nat:4> * <var:n> ^ <nat:10> + <nat:1> * <var:n> ^ <nat:9> + <nat:2> * <var:n> ^ <nat:8> + <nat:1> * <var:n> ^ <nat:6> + <nat:4> * <var:n> ^ <nat:5> + <nat:1> * <var:n> ^ <nat:4> + <nat:4> * <var:n> ^ <nat:3> + <nat:1> * <var:n> ^ <nat:2> + <nat:3> * <var:n> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:e> = <var:n> /\ <var:j> = <nat:2> * <var:n> ^ <nat:13> + <nat:2> * <var:n> ^ <nat:12> + <nat:4> * <var:n> ^ <nat:11> + <nat:1> * <var:n> ^ <nat:10> + <nat:2> * <var:n> ^ <nat:9> + <nat:1> * <var:n> ^ <nat:7> + <nat:4> * <var:n> ^ <nat:6> + <nat:1> * <var:n> ^ <nat:5> + <nat:4> * <var:n> ^ <nat:4> + <nat:1> * <var:n> ^ <nat:3> + <nat:3> * <var:n> ^ <nat:2> + <nat:1> * <var:n> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 