\begin{document} \begin{definition} Let the following code segment <def:poly> in \emph{Imp} as \begin{verbatim} <var:A> := <nat:2> ; <var:A> := <nat:2> + <var:A> * <var:Y> ; <var:A> := <nat:4> + <var:A> * <var:Y> ; <var:A> := <nat:3> + <var:A> * <var:Y> ; <var:A> := <nat:4> + <var:A> * <var:Y> ; <var:A> := <nat:3> + <var:A> * <var:Y> ; <var:A> := <nat:1> + <var:A> * <var:Y> ; <var:A> := <nat:2> + <var:A> * <var:Y> ; <var:A> := <nat:1> + <var:A> * <var:Y> \end{verbatim} \end{definition} \begin{theorem} Take the following code segment <def:poly> in \emph{Imp} . Allowing for $ <var:S> \in \mathbb{N} $ that we have $ <var:Y> = <var:S> $ ahead of running the series of commands , it follows that we have $ <var:A> = <nat:2> \cdot <var:S> ^ <nat:8> + <nat:2> \cdot <var:S> ^ <nat:7> + <nat:4> \cdot <var:S> ^ <nat:6> + <nat:3> \cdot <var:S> ^ <nat:5> + <nat:4> \cdot <var:S> ^ <nat:4> + <nat:3> \cdot <var:S> ^ <nat:3> + <nat:1> \cdot <var:S> ^ <nat:2> + <nat:2> \cdot <var:S> + <nat:1> $ when list of commands finishes . \end{theorem} \begin{proof} Observe that the below decorated list of instructions . \begin{eqnarray} \{ <var:Y> = <var:S> \} \\ <var:A> := <nat:2> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \} \\ <var:A> := <nat:2> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> + <nat:2> \} \\ <var:A> := <nat:4> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:2> + <nat:2> \times <var:S> + <nat:4> \} \\ <var:A> := <nat:3> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:3> + <nat:2> \times <var:S> ^ <nat:2> + <nat:4> \times <var:S> + <nat:3> \} \\ <var:A> := <nat:4> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:4> + <nat:2> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:4> \} \\ <var:A> := <nat:3> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:5> + <nat:2> \times <var:S> ^ <nat:4> + <nat:4> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:4> \times <var:S> + <nat:3> \} \\ <var:A> := <nat:1> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:6> + <nat:2> \times <var:S> ^ <nat:5> + <nat:4> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:4> \times <var:S> ^ <nat:2> + <nat:3> \times <var:S> + <nat:1> \} \\ <var:A> := <nat:2> + <var:A> * <var:Y> ; \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:7> + <nat:2> \times <var:S> ^ <nat:6> + <nat:4> \times <var:S> ^ <nat:5> + <nat:3> \times <var:S> ^ <nat:4> + <nat:4> \times <var:S> ^ <nat:3> + <nat:3> \times <var:S> ^ <nat:2> + <nat:1> \times <var:S> + <nat:2> \} \\ <var:A> := <nat:1> + <var:A> * <var:Y> \\ \{ <var:Y> = <var:S> \land <var:A> = <nat:2> \times <var:S> ^ <nat:8> + <nat:2> \times <var:S> ^ <nat:7> + <nat:4> \times <var:S> ^ <nat:6> + <nat:3> \times <var:S> ^ <nat:5> + <nat:4> \times <var:S> ^ <nat:4> + <nat:3> \times <var:S> ^ <nat:3> + <nat:1> \times <var:S> ^ <nat:2> + <nat:2> \times <var:S> + <nat:1> \} \end{eqnarray} Utilizing standard Hoare logic solves the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:A> := <nat:2> ; <var:A> := <nat:2> + <var:A> * <var:Y> ; <var:A> := <nat:4> + <var:A> * <var:Y> ; <var:A> := <nat:3> + <var:A> * <var:Y> ; <var:A> := <nat:4> + <var:A> * <var:Y> ; <var:A> := <nat:3> + <var:A> * <var:Y> ; <var:A> := <nat:1> + <var:A> * <var:Y> ; <var:A> := <nat:2> + <var:A> * <var:Y> ; <var:A> := <nat:1> + <var:A> * <var:Y> . Theorem <genH:poly_code_correct> : forall <var:S> : nat , {{ <var:Y> = <var:S> }} <def:poly> {{ <var:A> = <nat:2> * <var:S> ^ <nat:8> + <nat:2> * <var:S> ^ <nat:7> + <nat:4> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:1> * <var:S> ^ <nat:2> + <nat:2> * <var:S> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:2> + <nat:2> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:3> + <nat:2> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:4> + <nat:2> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:5> + <nat:2> * <var:S> ^ <nat:4> + <nat:4> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:6> + <nat:2> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:7> + <nat:2> * <var:S> ^ <nat:6> + <nat:4> * <var:S> ^ <nat:5> + <nat:3> * <var:S> ^ <nat:4> + <nat:4> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:1> * <var:S> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Y> = <var:S> /\ <var:A> = <nat:2> * <var:S> ^ <nat:8> + <nat:2> * <var:S> ^ <nat:7> + <nat:4> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:1> * <var:S> ^ <nat:2> + <nat:2> * <var:S> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 