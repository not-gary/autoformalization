\begin{document} \begin{definition} We define a series of instructions <def:poly> in \emph{Imp} such that \begin{verbatim} <var:A> := <nat:1> ; <var:A> := <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <nat:2> + <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <nat:3> + <var:A> * <var:Q> ; <var:A> := <nat:3> + <var:A> * <var:Q> ; <var:A> := <nat:1> + <var:A> * <var:Q> ; <var:A> := <nat:2> + <var:A> * <var:Q> \end{verbatim} \end{definition} \begin{theorem} Define a series of commands <def:poly> . Given --- with some integers $ <var:z> $ --- that we have $ <var:Q> = <var:z> $ before interpreting our set of commands , it follows that we see $ <var:A> = <nat:1> \times <var:z> ^ <nat:10> + <nat:4> \times <var:z> ^ <nat:8> + <nat:4> \times <var:z> ^ <nat:6> + <nat:2> \times <var:z> ^ <nat:5> + <nat:4> \times <var:z> ^ <nat:4> + <nat:3> \times <var:z> ^ <nat:3> + <nat:3> \times <var:z> ^ <nat:2> + <nat:1> \times <var:z> + <nat:2> $ when segment of code has finished . \end{theorem} \begin{proof} See that this decorated set of commands . \begin{eqnarray} \{ <var:Q> = <var:z> \} \\ <var:A> := <nat:1> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> \} \\ <var:A> := <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> + \} \\ <var:A> := <nat:4> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:2> + <nat:4> \} \\ <var:A> := <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:3> + <nat:4> * <var:z> + \} \\ <var:A> := <nat:4> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:2> + <nat:4> \} \\ <var:A> := <nat:2> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:3> + <nat:4> * <var:z> + <nat:2> \} \\ <var:A> := <nat:4> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:6> + <nat:4> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:2> + <nat:2> * <var:z> + <nat:4> \} \\ <var:A> := <nat:3> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:7> + <nat:4> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:3> + <nat:2> * <var:z> ^ <nat:2> + <nat:4> * <var:z> + <nat:3> \} \\ <var:A> := <nat:3> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:8> + <nat:4> * <var:z> ^ <nat:6> + <nat:4> * <var:z> ^ <nat:4> + <nat:2> * <var:z> ^ <nat:3> + <nat:4> * <var:z> ^ <nat:2> + <nat:3> * <var:z> + <nat:3> \} \\ <var:A> := <nat:1> + <var:A> * <var:Q> ; \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:9> + <nat:4> * <var:z> ^ <nat:7> + <nat:4> * <var:z> ^ <nat:5> + <nat:2> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:3> + <nat:3> * <var:z> ^ <nat:2> + <nat:3> * <var:z> + <nat:1> \} \\ <var:A> := <nat:2> + <var:A> * <var:Q> \\ \{ <var:Q> = <var:z> \land <var:A> = <nat:1> * <var:z> ^ <nat:10> + <nat:4> * <var:z> ^ <nat:8> + <nat:4> * <var:z> ^ <nat:6> + <nat:2> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:4> + <nat:3> * <var:z> ^ <nat:3> + <nat:3> * <var:z> ^ <nat:2> + <nat:1> * <var:z> + <nat:2> \} \end{eqnarray} The application of standard Hoare reasoning ends the proof . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:A> := <nat:1> ; <var:A> := <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <nat:2> + <var:A> * <var:Q> ; <var:A> := <nat:4> + <var:A> * <var:Q> ; <var:A> := <nat:3> + <var:A> * <var:Q> ; <var:A> := <nat:3> + <var:A> * <var:Q> ; <var:A> := <nat:1> + <var:A> * <var:Q> ; <var:A> := <nat:2> + <var:A> * <var:Q> . Theorem <genH:poly_code_correct> : forall <var:z> : nat , {{ <var:Q> = <var:z> }} <def:poly> {{ <var:A> = <nat:1> * <var:z> ^ <nat:10> + <nat:4> * <var:z> ^ <nat:8> + <nat:4> * <var:z> ^ <nat:6> + <nat:2> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:4> + <nat:3> * <var:z> ^ <nat:3> + <nat:3> * <var:z> ^ <nat:2> + <nat:1> * <var:z> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:3> + <nat:4> * <var:z> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:3> + <nat:4> * <var:z> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:6> + <nat:4> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:2> + <nat:2> * <var:z> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:7> + <nat:4> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:3> + <nat:2> * <var:z> ^ <nat:2> + <nat:4> * <var:z> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:8> + <nat:4> * <var:z> ^ <nat:6> + <nat:4> * <var:z> ^ <nat:4> + <nat:2> * <var:z> ^ <nat:3> + <nat:4> * <var:z> ^ <nat:2> + <nat:3> * <var:z> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:9> + <nat:4> * <var:z> ^ <nat:7> + <nat:4> * <var:z> ^ <nat:5> + <nat:2> * <var:z> ^ <nat:4> + <nat:4> * <var:z> ^ <nat:3> + <nat:3> * <var:z> ^ <nat:2> + <nat:3> * <var:z> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Q> = <var:z> /\ <var:A> = <nat:1> * <var:z> ^ <nat:10> + <nat:4> * <var:z> ^ <nat:8> + <nat:4> * <var:z> ^ <nat:6> + <nat:2> * <var:z> ^ <nat:5> + <nat:4> * <var:z> ^ <nat:4> + <nat:3> * <var:z> ^ <nat:3> + <nat:3> * <var:z> ^ <nat:2> + <nat:1> * <var:z> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 