\begin{document} \begin{definition} We define the following code <def:poly> in \emph{Imp} : \begin{verbatim} <var:T> := <nat:1> ; <var:T> := <nat:4> + <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <nat:2> + <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:4> + <var:T> * <var:L> \end{verbatim} \end{definition} \begin{theorem} We define a code segment <def:poly> in \emph{Imp} . Allowing --- given any positive integers $ <var:S> $ --- it to be that $ <var:L> = <var:S> $ prior to running this set of commands it follows that we have $ <var:T> = <nat:1> * <var:S> ^ <nat:11> + <nat:4> * <var:S> ^ <nat:10> + <nat:3> * <var:S> ^ <nat:9> + <nat:1> * <var:S> ^ <nat:8> + <nat:1> * <var:S> ^ <nat:6> + <nat:2> * <var:S> ^ <nat:5> + <nat:1> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> $ when code has executed . \end{theorem} \begin{proof} The application of Hoare logic produces : \begin{eqnarray} \{ <var:L> = <var:S> \} \\ <var:T> := <nat:1> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \} \\ <var:T> := <nat:4> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> + <nat:4> \} \\ <var:T> := <nat:3> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:2> + <nat:4> \cdot <var:S> + <nat:3> \} \\ <var:T> := <nat:1> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:3> + <nat:4> \cdot <var:S> ^ <nat:2> + <nat:3> \cdot <var:S> + <nat:1> \} \\ <var:T> := <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:4> + <nat:4> \cdot <var:S> ^ <nat:3> + <nat:3> \cdot <var:S> ^ <nat:2> + <nat:1> \cdot <var:S> + \} \\ <var:T> := <nat:1> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:5> + <nat:4> \cdot <var:S> ^ <nat:4> + <nat:3> \cdot <var:S> ^ <nat:3> + <nat:1> \cdot <var:S> ^ <nat:2> + <nat:1> \} \\ <var:T> := <nat:2> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:6> + <nat:4> \cdot <var:S> ^ <nat:5> + <nat:3> \cdot <var:S> ^ <nat:4> + <nat:1> \cdot <var:S> ^ <nat:3> + <nat:1> \cdot <var:S> + <nat:2> \} \\ <var:T> := <nat:1> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:7> + <nat:4> \cdot <var:S> ^ <nat:6> + <nat:3> \cdot <var:S> ^ <nat:5> + <nat:1> \cdot <var:S> ^ <nat:4> + <nat:1> \cdot <var:S> ^ <nat:2> + <nat:2> \cdot <var:S> + <nat:1> \} \\ <var:T> := <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:8> + <nat:4> \cdot <var:S> ^ <nat:7> + <nat:3> \cdot <var:S> ^ <nat:6> + <nat:1> \cdot <var:S> ^ <nat:5> + <nat:1> \cdot <var:S> ^ <nat:3> + <nat:2> \cdot <var:S> ^ <nat:2> + <nat:1> \cdot <var:S> + \} \\ <var:T> := <nat:3> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:9> + <nat:4> \cdot <var:S> ^ <nat:8> + <nat:3> \cdot <var:S> ^ <nat:7> + <nat:1> \cdot <var:S> ^ <nat:6> + <nat:1> \cdot <var:S> ^ <nat:4> + <nat:2> \cdot <var:S> ^ <nat:3> + <nat:1> \cdot <var:S> ^ <nat:2> + <nat:3> \} \\ <var:T> := <nat:3> + <var:T> * <var:L> ; \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:10> + <nat:4> \cdot <var:S> ^ <nat:9> + <nat:3> \cdot <var:S> ^ <nat:8> + <nat:1> \cdot <var:S> ^ <nat:7> + <nat:1> \cdot <var:S> ^ <nat:5> + <nat:2> \cdot <var:S> ^ <nat:4> + <nat:1> \cdot <var:S> ^ <nat:3> + <nat:3> \cdot <var:S> + <nat:3> \} \\ <var:T> := <nat:4> + <var:T> * <var:L> \\ \{ <var:L> = <var:S> \land <var:T> = <nat:1> \cdot <var:S> ^ <nat:11> + <nat:4> \cdot <var:S> ^ <nat:10> + <nat:3> \cdot <var:S> ^ <nat:9> + <nat:1> \cdot <var:S> ^ <nat:8> + <nat:1> \cdot <var:S> ^ <nat:6> + <nat:2> \cdot <var:S> ^ <nat:5> + <nat:1> \cdot <var:S> ^ <nat:4> + <nat:3> \cdot <var:S> ^ <nat:2> + <nat:3> \cdot <var:S> + <nat:4> \} \end{eqnarray} Thusly , the set of commands has been shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:T> := <nat:1> ; <var:T> := <nat:4> + <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <nat:2> + <var:T> * <var:L> ; <var:T> := <nat:1> + <var:T> * <var:L> ; <var:T> := <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:3> + <var:T> * <var:L> ; <var:T> := <nat:4> + <var:T> * <var:L> . Theorem <genH:poly_code_correct> : forall <var:S> : nat , {{ <var:L> = <var:S> }} <def:poly> {{ <var:T> = <nat:1> * <var:S> ^ <nat:11> + <nat:4> * <var:S> ^ <nat:10> + <nat:3> * <var:S> ^ <nat:9> + <nat:1> * <var:S> ^ <nat:8> + <nat:1> * <var:S> ^ <nat:6> + <nat:2> * <var:S> ^ <nat:5> + <nat:1> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:2> + <nat:4> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:3> + <nat:4> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:4> + <nat:4> * <var:S> ^ <nat:3> + <nat:3> * <var:S> ^ <nat:2> + <nat:1> * <var:S> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:5> + <nat:4> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:3> + <nat:1> * <var:S> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:6> + <nat:4> * <var:S> ^ <nat:5> + <nat:3> * <var:S> ^ <nat:4> + <nat:1> * <var:S> ^ <nat:3> + <nat:1> * <var:S> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:7> + <nat:4> * <var:S> ^ <nat:6> + <nat:3> * <var:S> ^ <nat:5> + <nat:1> * <var:S> ^ <nat:4> + <nat:1> * <var:S> ^ <nat:2> + <nat:2> * <var:S> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:8> + <nat:4> * <var:S> ^ <nat:7> + <nat:3> * <var:S> ^ <nat:6> + <nat:1> * <var:S> ^ <nat:5> + <nat:1> * <var:S> ^ <nat:3> + <nat:2> * <var:S> ^ <nat:2> + <nat:1> * <var:S> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:9> + <nat:4> * <var:S> ^ <nat:8> + <nat:3> * <var:S> ^ <nat:7> + <nat:1> * <var:S> ^ <nat:6> + <nat:1> * <var:S> ^ <nat:4> + <nat:2> * <var:S> ^ <nat:3> + <nat:1> * <var:S> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:10> + <nat:4> * <var:S> ^ <nat:9> + <nat:3> * <var:S> ^ <nat:8> + <nat:1> * <var:S> ^ <nat:7> + <nat:1> * <var:S> ^ <nat:5> + <nat:2> * <var:S> ^ <nat:4> + <nat:1> * <var:S> ^ <nat:3> + <nat:3> * <var:S> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:L> = <var:S> /\ <var:T> = <nat:1> * <var:S> ^ <nat:11> + <nat:4> * <var:S> ^ <nat:10> + <nat:3> * <var:S> ^ <nat:9> + <nat:1> * <var:S> ^ <nat:8> + <nat:1> * <var:S> ^ <nat:6> + <nat:2> * <var:S> ^ <nat:5> + <nat:1> * <var:S> ^ <nat:4> + <nat:3> * <var:S> ^ <nat:2> + <nat:3> * <var:S> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 