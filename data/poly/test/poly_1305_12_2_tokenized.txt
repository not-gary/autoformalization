\begin{document} \begin{definition} Take the following set of instructions <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:k> := <nat:2> ; <var:k> := <var:k> * <var:h> ; <var:k> := <var:k> * <var:h> ; <var:k> := <nat:3> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:2> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:1> + <var:k> * <var:h> ; <var:k> := <nat:1> + <var:k> * <var:h> ; <var:k> := <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> \end{verbatim} \end{definition} \begin{theorem} Assuming that $ <var:h> = <var:V> $ before executing the series of commands , it holds that we have $ <var:k> = <nat:2> \cdot <var:V> ^ <nat:11> + <nat:3> \cdot <var:V> ^ <nat:8> + <nat:4> \cdot <var:V> ^ <nat:7> + <nat:2> \cdot <var:V> ^ <nat:6> + <nat:4> \cdot <var:V> ^ <nat:5> + <nat:1> \cdot <var:V> ^ <nat:4> + <nat:1> \cdot <var:V> ^ <nat:3> + <nat:4> \cdot <var:V> + <nat:4> $ once code finishes . \end{theorem} \begin{proof} Utilizing usual Hoare reasoning produces : \begin{eqnarray} \{ <var:h> = <var:V> \} \\ <var:k> := <nat:2> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> \} \\ <var:k> := <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> + \} \\ <var:k> := <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:2> + \} \\ <var:k> := <nat:3> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:3> + <nat:3> \} \\ <var:k> := <nat:4> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:4> + <nat:3> * <var:V> + <nat:4> \} \\ <var:k> := <nat:2> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:5> + <nat:3> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:2> \} \\ <var:k> := <nat:4> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:6> + <nat:3> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:2> * <var:V> + <nat:4> \} \\ <var:k> := <nat:1> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:7> + <nat:3> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:2> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:1> \} \\ <var:k> := <nat:1> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:8> + <nat:3> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:2> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + <nat:1> \} \\ <var:k> := <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:9> + <nat:3> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:5> + <nat:2> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + \} \\ <var:k> := <nat:4> + <var:k> * <var:h> ; \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:10> + <nat:3> * <var:V> ^ <nat:7> + <nat:4> * <var:V> ^ <nat:6> + <nat:2> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:4> \} \\ <var:k> := <nat:4> + <var:k> * <var:h> \\ \{ <var:h> = <var:V> \land <var:k> = <nat:2> * <var:V> ^ <nat:11> + <nat:3> * <var:V> ^ <nat:8> + <nat:4> * <var:V> ^ <nat:7> + <nat:2> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:5> + <nat:1> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:4> * <var:V> + <nat:4> \} \end{eqnarray} Thusly , the code is shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:k> := <nat:2> ; <var:k> := <var:k> * <var:h> ; <var:k> := <var:k> * <var:h> ; <var:k> := <nat:3> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:2> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:1> + <var:k> * <var:h> ; <var:k> := <nat:1> + <var:k> * <var:h> ; <var:k> := <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> ; <var:k> := <nat:4> + <var:k> * <var:h> . Theorem <genH:poly_code_correct> : forall <var:V> : nat , {{ <var:h> = <var:V> }} <def:poly> {{ <var:k> = <nat:2> * <var:V> ^ <nat:11> + <nat:3> * <var:V> ^ <nat:8> + <nat:4> * <var:V> ^ <nat:7> + <nat:2> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:5> + <nat:1> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:4> * <var:V> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:3> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:4> + <nat:3> * <var:V> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:5> + <nat:3> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:6> + <nat:3> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:2> * <var:V> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:7> + <nat:3> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:2> * <var:V> ^ <nat:2> + <nat:4> * <var:V> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:8> + <nat:3> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:2> * <var:V> ^ <nat:3> + <nat:4> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:9> + <nat:3> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:5> + <nat:2> * <var:V> ^ <nat:4> + <nat:4> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:1> * <var:V> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:10> + <nat:3> * <var:V> ^ <nat:7> + <nat:4> * <var:V> ^ <nat:6> + <nat:2> * <var:V> ^ <nat:5> + <nat:4> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:1> * <var:V> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:h> = <var:V> /\ <var:k> = <nat:2> * <var:V> ^ <nat:11> + <nat:3> * <var:V> ^ <nat:8> + <nat:4> * <var:V> ^ <nat:7> + <nat:2> * <var:V> ^ <nat:6> + <nat:4> * <var:V> ^ <nat:5> + <nat:1> * <var:V> ^ <nat:4> + <nat:1> * <var:V> ^ <nat:3> + <nat:4> * <var:V> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 