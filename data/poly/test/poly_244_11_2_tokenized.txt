\begin{document} \begin{theorem} We define the following segment of code in \emph{Imp} such that \begin{verbatim} <var:s> := <nat:4> ; <var:s> := <nat:3> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:3> + <var:s> * <var:z> ; <var:s> := <nat:2> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:2> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:1> + <var:s> * <var:z> ; <var:s> := <nat:1> + <var:s> * <var:z> \end{verbatim} Assume that $ <var:z> = <var:h> $ --- for integers $ <var:h> $ --- prior to executing the set of instructions , then $ <var:s> = <nat:4> \cdot <var:h> ^ <nat:10> + <nat:3> \cdot <var:h> ^ <nat:9> + <nat:3> \cdot <var:h> ^ <nat:7> + <nat:2> \cdot <var:h> ^ <nat:6> + <nat:2> \cdot <var:h> ^ <nat:3> + <nat:1> \cdot <var:h> + <nat:1> $ when series of commands has finished executing . \end{theorem} \begin{proof} Observe the below decorated set of commands . \begin{eqnarray} \{ <var:z> = <var:h> \} \\ <var:s> := <nat:4> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \} \\ <var:s> := <nat:3> + <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> + <nat:3> \} \\ <var:s> := <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:2> + <nat:3> \times <var:h> + \} \\ <var:s> := <nat:3> + <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:3> + <nat:3> \times <var:h> ^ <nat:2> + <nat:3> \} \\ <var:s> := <nat:2> + <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:4> + <nat:3> \times <var:h> ^ <nat:3> + <nat:3> \times <var:h> + <nat:2> \} \\ <var:s> := <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:5> + <nat:3> \times <var:h> ^ <nat:4> + <nat:3> \times <var:h> ^ <nat:2> + <nat:2> \times <var:h> + \} \\ <var:s> := <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:6> + <nat:3> \times <var:h> ^ <nat:5> + <nat:3> \times <var:h> ^ <nat:3> + <nat:2> \times <var:h> ^ <nat:2> + \} \\ <var:s> := <nat:2> + <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:7> + <nat:3> \times <var:h> ^ <nat:6> + <nat:3> \times <var:h> ^ <nat:4> + <nat:2> \times <var:h> ^ <nat:3> + <nat:2> \} \\ <var:s> := <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:8> + <nat:3> \times <var:h> ^ <nat:7> + <nat:3> \times <var:h> ^ <nat:5> + <nat:2> \times <var:h> ^ <nat:4> + <nat:2> \times <var:h> + \} \\ <var:s> := <nat:1> + <var:s> * <var:z> ; \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:9> + <nat:3> \times <var:h> ^ <nat:8> + <nat:3> \times <var:h> ^ <nat:6> + <nat:2> \times <var:h> ^ <nat:5> + <nat:2> \times <var:h> ^ <nat:2> + <nat:1> \} \\ <var:s> := <nat:1> + <var:s> * <var:z> \\ \{ <var:z> = <var:h> \land <var:s> = <nat:4> \times <var:h> ^ <nat:10> + <nat:3> \times <var:h> ^ <nat:9> + <nat:3> \times <var:h> ^ <nat:7> + <nat:2> \times <var:h> ^ <nat:6> + <nat:2> \times <var:h> ^ <nat:3> + <nat:1> \times <var:h> + <nat:1> \} \end{eqnarray} Applying usual Hoare techniques finalizes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:h> : nat , {{ <var:z> = <var:h> }} <var:s> := <nat:4> ; <var:s> := <nat:3> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:3> + <var:s> * <var:z> ; <var:s> := <nat:2> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:2> + <var:s> * <var:z> ; <var:s> := <var:s> * <var:z> ; <var:s> := <nat:1> + <var:s> * <var:z> ; <var:s> := <nat:1> + <var:s> * <var:z> {{ <var:s> = <nat:4> * <var:h> ^ <nat:10> + <nat:3> * <var:h> ^ <nat:9> + <nat:3> * <var:h> ^ <nat:7> + <nat:2> * <var:h> ^ <nat:6> + <nat:2> * <var:h> ^ <nat:3> + <nat:1> * <var:h> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:2> + <nat:3> * <var:h> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:3> + <nat:3> * <var:h> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:4> + <nat:3> * <var:h> ^ <nat:3> + <nat:3> * <var:h> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:5> + <nat:3> * <var:h> ^ <nat:4> + <nat:3> * <var:h> ^ <nat:2> + <nat:2> * <var:h> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:6> + <nat:3> * <var:h> ^ <nat:5> + <nat:3> * <var:h> ^ <nat:3> + <nat:2> * <var:h> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:7> + <nat:3> * <var:h> ^ <nat:6> + <nat:3> * <var:h> ^ <nat:4> + <nat:2> * <var:h> ^ <nat:3> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:8> + <nat:3> * <var:h> ^ <nat:7> + <nat:3> * <var:h> ^ <nat:5> + <nat:2> * <var:h> ^ <nat:4> + <nat:2> * <var:h> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:9> + <nat:3> * <var:h> ^ <nat:8> + <nat:3> * <var:h> ^ <nat:6> + <nat:2> * <var:h> ^ <nat:5> + <nat:2> * <var:h> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:z> = <var:h> /\ <var:s> = <nat:4> * <var:h> ^ <nat:10> + <nat:3> * <var:h> ^ <nat:9> + <nat:3> * <var:h> ^ <nat:7> + <nat:2> * <var:h> ^ <nat:6> + <nat:2> * <var:h> ^ <nat:3> + <nat:1> * <var:h> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 