\begin{document} \begin{definition} Define the following code segment <def:poly> in \emph{Imp} such that \begin{verbatim} <var:L> := <nat:4> ; <var:L> := <nat:4> + <var:L> * <var:Z> ; <var:L> := <nat:4> + <var:L> * <var:Z> ; <var:L> := <nat:2> + <var:L> * <var:Z> ; <var:L> := <nat:1> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:2> + <var:L> * <var:Z> \end{verbatim} \end{definition} \begin{theorem} Take the following code segment <def:poly> in \emph{Imp} . Allow that we have $ <var:Z> = <var:q> $ ahead of the evaluation of our code segment it holds that $ <var:L> = <nat:4> \cdot <var:q> ^ <nat:8> + <nat:4> \cdot <var:q> ^ <nat:7> + <nat:4> \cdot <var:q> ^ <nat:6> + <nat:2> \cdot <var:q> ^ <nat:5> + <nat:1> \cdot <var:q> ^ <nat:4> + <nat:3> \cdot <var:q> ^ <nat:3> + <nat:3> \cdot <var:q> ^ <nat:2> + <nat:3> \cdot <var:q> + <nat:2> $ . \end{theorem} \begin{proof} Proving correctness for our code segment is demonstrated through the following decorated list of commands . \begin{eqnarray} \{ <var:Z> = <var:q> \} \\ <var:L> := <nat:4> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \} \\ <var:L> := <nat:4> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> + <nat:4> \} \\ <var:L> := <nat:4> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:2> + <nat:4> \cdot <var:q> + <nat:4> \} \\ <var:L> := <nat:2> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:3> + <nat:4> \cdot <var:q> ^ <nat:2> + <nat:4> \cdot <var:q> + <nat:2> \} \\ <var:L> := <nat:1> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:4> + <nat:4> \cdot <var:q> ^ <nat:3> + <nat:4> \cdot <var:q> ^ <nat:2> + <nat:2> \cdot <var:q> + <nat:1> \} \\ <var:L> := <nat:3> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:5> + <nat:4> \cdot <var:q> ^ <nat:4> + <nat:4> \cdot <var:q> ^ <nat:3> + <nat:2> \cdot <var:q> ^ <nat:2> + <nat:1> \cdot <var:q> + <nat:3> \} \\ <var:L> := <nat:3> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:6> + <nat:4> \cdot <var:q> ^ <nat:5> + <nat:4> \cdot <var:q> ^ <nat:4> + <nat:2> \cdot <var:q> ^ <nat:3> + <nat:1> \cdot <var:q> ^ <nat:2> + <nat:3> \cdot <var:q> + <nat:3> \} \\ <var:L> := <nat:3> + <var:L> * <var:Z> ; \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:7> + <nat:4> \cdot <var:q> ^ <nat:6> + <nat:4> \cdot <var:q> ^ <nat:5> + <nat:2> \cdot <var:q> ^ <nat:4> + <nat:1> \cdot <var:q> ^ <nat:3> + <nat:3> \cdot <var:q> ^ <nat:2> + <nat:3> \cdot <var:q> + <nat:3> \} \\ <var:L> := <nat:2> + <var:L> * <var:Z> \\ \{ <var:Z> = <var:q> \land <var:L> = <nat:4> \cdot <var:q> ^ <nat:8> + <nat:4> \cdot <var:q> ^ <nat:7> + <nat:4> \cdot <var:q> ^ <nat:6> + <nat:2> \cdot <var:q> ^ <nat:5> + <nat:1> \cdot <var:q> ^ <nat:4> + <nat:3> \cdot <var:q> ^ <nat:3> + <nat:3> \cdot <var:q> ^ <nat:2> + <nat:3> \cdot <var:q> + <nat:2> \} \end{eqnarray} Applying standard Hoare techniques finalizes the argument . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:L> := <nat:4> ; <var:L> := <nat:4> + <var:L> * <var:Z> ; <var:L> := <nat:4> + <var:L> * <var:Z> ; <var:L> := <nat:2> + <var:L> * <var:Z> ; <var:L> := <nat:1> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:3> + <var:L> * <var:Z> ; <var:L> := <nat:2> + <var:L> * <var:Z> . Theorem <genH:poly_code_correct> : forall <var:q> : nat , {{ <var:Z> = <var:q> }} <def:poly> {{ <var:L> = <nat:4> * <var:q> ^ <nat:8> + <nat:4> * <var:q> ^ <nat:7> + <nat:4> * <var:q> ^ <nat:6> + <nat:2> * <var:q> ^ <nat:5> + <nat:1> * <var:q> ^ <nat:4> + <nat:3> * <var:q> ^ <nat:3> + <nat:3> * <var:q> ^ <nat:2> + <nat:3> * <var:q> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:2> + <nat:4> * <var:q> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:3> + <nat:4> * <var:q> ^ <nat:2> + <nat:4> * <var:q> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:4> + <nat:4> * <var:q> ^ <nat:3> + <nat:4> * <var:q> ^ <nat:2> + <nat:2> * <var:q> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:5> + <nat:4> * <var:q> ^ <nat:4> + <nat:4> * <var:q> ^ <nat:3> + <nat:2> * <var:q> ^ <nat:2> + <nat:1> * <var:q> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:6> + <nat:4> * <var:q> ^ <nat:5> + <nat:4> * <var:q> ^ <nat:4> + <nat:2> * <var:q> ^ <nat:3> + <nat:1> * <var:q> ^ <nat:2> + <nat:3> * <var:q> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:7> + <nat:4> * <var:q> ^ <nat:6> + <nat:4> * <var:q> ^ <nat:5> + <nat:2> * <var:q> ^ <nat:4> + <nat:1> * <var:q> ^ <nat:3> + <nat:3> * <var:q> ^ <nat:2> + <nat:3> * <var:q> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:Z> = <var:q> /\ <var:L> = <nat:4> * <var:q> ^ <nat:8> + <nat:4> * <var:q> ^ <nat:7> + <nat:4> * <var:q> ^ <nat:6> + <nat:2> * <var:q> ^ <nat:5> + <nat:1> * <var:q> ^ <nat:4> + <nat:3> * <var:q> ^ <nat:3> + <nat:3> * <var:q> ^ <nat:2> + <nat:3> * <var:q> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 