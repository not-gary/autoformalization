\begin{document} \begin{theorem} Let the following code as follows \begin{verbatim} <var:c> := <nat:1> ; <var:c> := <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <var:c> * <var:x> ; <var:c> := <nat:3> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:2> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> \end{verbatim} Allow that we have $ <var:x> = <var:m> $ before executing the code , it follows that we have $ <var:c> = <nat:1> \cdot <var:m> ^ <nat:11> + <nat:4> \cdot <var:m> ^ <nat:9> + <nat:3> \cdot <var:m> ^ <nat:7> + <nat:4> \cdot <var:m> ^ <nat:6> + <nat:1> \cdot <var:m> ^ <nat:5> + <nat:1> \cdot <var:m> ^ <nat:4> + <nat:4> \cdot <var:m> ^ <nat:3> + <nat:2> \cdot <var:m> ^ <nat:2> + <nat:4> \cdot <var:m> + <nat:1> $ . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:x> = <var:m> \} \\ <var:c> := <nat:1> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \} \\ <var:c> := <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> + \} \\ <var:c> := <nat:4> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:2> + <nat:4> \} \\ <var:c> := <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:3> + <nat:4> \times <var:m> + \} \\ <var:c> := <nat:3> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:4> + <nat:4> \times <var:m> ^ <nat:2> + <nat:3> \} \\ <var:c> := <nat:4> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:5> + <nat:4> \times <var:m> ^ <nat:3> + <nat:3> \times <var:m> + <nat:4> \} \\ <var:c> := <nat:1> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:6> + <nat:4> \times <var:m> ^ <nat:4> + <nat:3> \times <var:m> ^ <nat:2> + <nat:4> \times <var:m> + <nat:1> \} \\ <var:c> := <nat:1> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:7> + <nat:4> \times <var:m> ^ <nat:5> + <nat:3> \times <var:m> ^ <nat:3> + <nat:4> \times <var:m> ^ <nat:2> + <nat:1> \times <var:m> + <nat:1> \} \\ <var:c> := <nat:4> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:8> + <nat:4> \times <var:m> ^ <nat:6> + <nat:3> \times <var:m> ^ <nat:4> + <nat:4> \times <var:m> ^ <nat:3> + <nat:1> \times <var:m> ^ <nat:2> + <nat:1> \times <var:m> + <nat:4> \} \\ <var:c> := <nat:2> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:9> + <nat:4> \times <var:m> ^ <nat:7> + <nat:3> \times <var:m> ^ <nat:5> + <nat:4> \times <var:m> ^ <nat:4> + <nat:1> \times <var:m> ^ <nat:3> + <nat:1> \times <var:m> ^ <nat:2> + <nat:4> \times <var:m> + <nat:2> \} \\ <var:c> := <nat:4> + <var:c> * <var:x> ; \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:10> + <nat:4> \times <var:m> ^ <nat:8> + <nat:3> \times <var:m> ^ <nat:6> + <nat:4> \times <var:m> ^ <nat:5> + <nat:1> \times <var:m> ^ <nat:4> + <nat:1> \times <var:m> ^ <nat:3> + <nat:4> \times <var:m> ^ <nat:2> + <nat:2> \times <var:m> + <nat:4> \} \\ <var:c> := <nat:1> + <var:c> * <var:x> \\ \{ <var:x> = <var:m> \land <var:c> = <nat:1> \times <var:m> ^ <nat:11> + <nat:4> \times <var:m> ^ <nat:9> + <nat:3> \times <var:m> ^ <nat:7> + <nat:4> \times <var:m> ^ <nat:6> + <nat:1> \times <var:m> ^ <nat:5> + <nat:1> \times <var:m> ^ <nat:4> + <nat:4> \times <var:m> ^ <nat:3> + <nat:2> \times <var:m> ^ <nat:2> + <nat:4> \times <var:m> + <nat:1> \} \end{eqnarray} Applying usual Hoare logic finishes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:m> : nat , {{ <var:x> = <var:m> }} <var:c> := <nat:1> ; <var:c> := <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <var:c> * <var:x> ; <var:c> := <nat:3> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:2> + <var:c> * <var:x> ; <var:c> := <nat:4> + <var:c> * <var:x> ; <var:c> := <nat:1> + <var:c> * <var:x> {{ <var:c> = <nat:1> * <var:m> ^ <nat:11> + <nat:4> * <var:m> ^ <nat:9> + <nat:3> * <var:m> ^ <nat:7> + <nat:4> * <var:m> ^ <nat:6> + <nat:1> * <var:m> ^ <nat:5> + <nat:1> * <var:m> ^ <nat:4> + <nat:4> * <var:m> ^ <nat:3> + <nat:2> * <var:m> ^ <nat:2> + <nat:4> * <var:m> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:3> + <nat:4> * <var:m> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:4> + <nat:4> * <var:m> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:5> + <nat:4> * <var:m> ^ <nat:3> + <nat:3> * <var:m> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:6> + <nat:4> * <var:m> ^ <nat:4> + <nat:3> * <var:m> ^ <nat:2> + <nat:4> * <var:m> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:7> + <nat:4> * <var:m> ^ <nat:5> + <nat:3> * <var:m> ^ <nat:3> + <nat:4> * <var:m> ^ <nat:2> + <nat:1> * <var:m> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:8> + <nat:4> * <var:m> ^ <nat:6> + <nat:3> * <var:m> ^ <nat:4> + <nat:4> * <var:m> ^ <nat:3> + <nat:1> * <var:m> ^ <nat:2> + <nat:1> * <var:m> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:9> + <nat:4> * <var:m> ^ <nat:7> + <nat:3> * <var:m> ^ <nat:5> + <nat:4> * <var:m> ^ <nat:4> + <nat:1> * <var:m> ^ <nat:3> + <nat:1> * <var:m> ^ <nat:2> + <nat:4> * <var:m> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:10> + <nat:4> * <var:m> ^ <nat:8> + <nat:3> * <var:m> ^ <nat:6> + <nat:4> * <var:m> ^ <nat:5> + <nat:1> * <var:m> ^ <nat:4> + <nat:1> * <var:m> ^ <nat:3> + <nat:4> * <var:m> ^ <nat:2> + <nat:2> * <var:m> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:x> = <var:m> /\ <var:c> = <nat:1> * <var:m> ^ <nat:11> + <nat:4> * <var:m> ^ <nat:9> + <nat:3> * <var:m> ^ <nat:7> + <nat:4> * <var:m> ^ <nat:6> + <nat:1> * <var:m> ^ <nat:5> + <nat:1> * <var:m> ^ <nat:4> + <nat:4> * <var:m> ^ <nat:3> + <nat:2> * <var:m> ^ <nat:2> + <nat:4> * <var:m> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 