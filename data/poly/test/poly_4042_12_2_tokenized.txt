\begin{document} \begin{theorem} Let the following code such that \begin{verbatim} <var:V> := <nat:2> ; <var:V> := <nat:1> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> ; <var:V> := <nat:3> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> ; <var:V> := <nat:3> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> \end{verbatim} Allow that we have $ <var:b> = <var:y> $ taking natural numbers $ <var:y> $ prior to interpreting our program , then $ <var:V> = <nat:2> * <var:y> ^ <nat:11> + <nat:1> * <var:y> ^ <nat:10> + <nat:4> * <var:y> ^ <nat:7> + <nat:3> * <var:y> ^ <nat:6> + <nat:4> * <var:y> ^ <nat:4> + <nat:3> * <var:y> ^ <nat:3> + <nat:4> $ once series of instructions has exited . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:b> = <var:y> \} \\ <var:V> := <nat:2> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \} \\ <var:V> := <nat:1> + <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> + <nat:1> \} \\ <var:V> := <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:2> + <nat:1> \times <var:y> + \} \\ <var:V> := <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:3> + <nat:1> \times <var:y> ^ <nat:2> + \} \\ <var:V> := <nat:4> + <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:4> + <nat:1> \times <var:y> ^ <nat:3> + <nat:4> \} \\ <var:V> := <nat:3> + <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:5> + <nat:1> \times <var:y> ^ <nat:4> + <nat:4> \times <var:y> + <nat:3> \} \\ <var:V> := <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:6> + <nat:1> \times <var:y> ^ <nat:5> + <nat:4> \times <var:y> ^ <nat:2> + <nat:3> \times <var:y> + \} \\ <var:V> := <nat:4> + <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:7> + <nat:1> \times <var:y> ^ <nat:6> + <nat:4> \times <var:y> ^ <nat:3> + <nat:3> \times <var:y> ^ <nat:2> + <nat:4> \} \\ <var:V> := <nat:3> + <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:8> + <nat:1> \times <var:y> ^ <nat:7> + <nat:4> \times <var:y> ^ <nat:4> + <nat:3> \times <var:y> ^ <nat:3> + <nat:4> \times <var:y> + <nat:3> \} \\ <var:V> := <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:9> + <nat:1> \times <var:y> ^ <nat:8> + <nat:4> \times <var:y> ^ <nat:5> + <nat:3> \times <var:y> ^ <nat:4> + <nat:4> \times <var:y> ^ <nat:2> + <nat:3> \times <var:y> + \} \\ <var:V> := <var:V> * <var:b> ; \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:10> + <nat:1> \times <var:y> ^ <nat:9> + <nat:4> \times <var:y> ^ <nat:6> + <nat:3> \times <var:y> ^ <nat:5> + <nat:4> \times <var:y> ^ <nat:3> + <nat:3> \times <var:y> ^ <nat:2> + \} \\ <var:V> := <nat:4> + <var:V> * <var:b> \\ \{ <var:b> = <var:y> \land <var:V> = <nat:2> \times <var:y> ^ <nat:11> + <nat:1> \times <var:y> ^ <nat:10> + <nat:4> \times <var:y> ^ <nat:7> + <nat:3> \times <var:y> ^ <nat:6> + <nat:4> \times <var:y> ^ <nat:4> + <nat:3> \times <var:y> ^ <nat:3> + <nat:4> \} \end{eqnarray} Utilizing Hoare reasoning finishes the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:y> : nat , {{ <var:b> = <var:y> }} <var:V> := <nat:2> ; <var:V> := <nat:1> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> ; <var:V> := <nat:3> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> ; <var:V> := <nat:3> + <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <var:V> * <var:b> ; <var:V> := <nat:4> + <var:V> * <var:b> {{ <var:V> = <nat:2> * <var:y> ^ <nat:11> + <nat:1> * <var:y> ^ <nat:10> + <nat:4> * <var:y> ^ <nat:7> + <nat:3> * <var:y> ^ <nat:6> + <nat:4> * <var:y> ^ <nat:4> + <nat:3> * <var:y> ^ <nat:3> + <nat:4> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:2> + <nat:1> * <var:y> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:3> + <nat:1> * <var:y> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:4> + <nat:1> * <var:y> ^ <nat:3> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:5> + <nat:1> * <var:y> ^ <nat:4> + <nat:4> * <var:y> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:6> + <nat:1> * <var:y> ^ <nat:5> + <nat:4> * <var:y> ^ <nat:2> + <nat:3> * <var:y> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:7> + <nat:1> * <var:y> ^ <nat:6> + <nat:4> * <var:y> ^ <nat:3> + <nat:3> * <var:y> ^ <nat:2> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:8> + <nat:1> * <var:y> ^ <nat:7> + <nat:4> * <var:y> ^ <nat:4> + <nat:3> * <var:y> ^ <nat:3> + <nat:4> * <var:y> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:9> + <nat:1> * <var:y> ^ <nat:8> + <nat:4> * <var:y> ^ <nat:5> + <nat:3> * <var:y> ^ <nat:4> + <nat:4> * <var:y> ^ <nat:2> + <nat:3> * <var:y> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:10> + <nat:1> * <var:y> ^ <nat:9> + <nat:4> * <var:y> ^ <nat:6> + <nat:3> * <var:y> ^ <nat:5> + <nat:4> * <var:y> ^ <nat:3> + <nat:3> * <var:y> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:b> = <var:y> /\ <var:V> = <nat:2> * <var:y> ^ <nat:11> + <nat:1> * <var:y> ^ <nat:10> + <nat:4> * <var:y> ^ <nat:7> + <nat:3> * <var:y> ^ <nat:6> + <nat:4> * <var:y> ^ <nat:4> + <nat:3> * <var:y> ^ <nat:3> + <nat:4> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 