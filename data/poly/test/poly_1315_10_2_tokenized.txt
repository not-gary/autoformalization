\begin{document} \begin{definition} We define the following set of commands <def:poly> in \emph{Imp} as follows \begin{verbatim} <var:V> := <nat:4> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:1> + <var:V> * <var:M> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:2> + <var:V> * <var:M> ; <var:V> := <nat:1> + <var:V> * <var:M> ; <var:V> := <var:V> * <var:M> ; <var:V> := <var:V> * <var:M> ; <var:V> := <nat:2> + <var:V> * <var:M> \end{verbatim} \end{definition} \begin{theorem} Consider a set of instructions <def:poly> . Assuming that we have $ <var:M> = <var:r> $ , with some $ <var:r> \in \mathbb{N} $ , before the evaluation of the series of commands it must be that $ <var:V> = <nat:4> \times <var:r> ^ <nat:9> + <nat:3> \times <var:r> ^ <nat:8> + <nat:1> \times <var:r> ^ <nat:7> + <nat:3> \times <var:r> ^ <nat:6> + <nat:3> \times <var:r> ^ <nat:5> + <nat:2> \times <var:r> ^ <nat:4> + <nat:1> \times <var:r> ^ <nat:3> + <nat:2> $ once list of instructions has exited . \end{theorem} \begin{proof} \begin{eqnarray} \{ <var:M> = <var:r> \} \\ <var:V> := <nat:4> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> \} \\ <var:V> := <nat:3> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> + <nat:3> \} \\ <var:V> := <nat:1> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> \} \\ <var:V> := <nat:3> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + <nat:3> \} \\ <var:V> := <nat:3> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> \} \\ <var:V> := <nat:2> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:2> \} \\ <var:V> := <nat:1> + <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:1> \} \\ <var:V> := <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:1> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + \} \\ <var:V> := <var:V> * <var:M> ; \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:1> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + \} \\ <var:V> := <nat:2> + <var:V> * <var:M> \\ \{ <var:M> = <var:r> \land <var:V> = <nat:4> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:1> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> \} \end{eqnarray} Applying typical Hoare reasoning solves the goal . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:V> := <nat:4> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:1> + <var:V> * <var:M> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:3> + <var:V> * <var:M> ; <var:V> := <nat:2> + <var:V> * <var:M> ; <var:V> := <nat:1> + <var:V> * <var:M> ; <var:V> := <var:V> * <var:M> ; <var:V> := <var:V> * <var:M> ; <var:V> := <nat:2> + <var:V> * <var:M> . Theorem <genH:poly_code_correct> : forall <var:r> : nat , {{ <var:M> = <var:r> }} <def:poly> {{ <var:V> = <nat:4> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:1> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:1> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:1> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:M> = <var:r> /\ <var:V> = <nat:4> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:1> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 