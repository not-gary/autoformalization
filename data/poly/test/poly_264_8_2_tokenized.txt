\begin{document} \begin{definition} Consider the following code segment <def:poly> in \emph{Imp} such that \begin{verbatim} <var:r> := <nat:3> ; <var:r> := <nat:4> + <var:r> * <var:V> ; <var:r> := <nat:3> + <var:r> * <var:V> ; <var:r> := <var:r> * <var:V> ; <var:r> := <nat:1> + <var:r> * <var:V> ; <var:r> := <nat:2> + <var:r> * <var:V> ; <var:r> := <nat:4> + <var:r> * <var:V> ; <var:r> := <nat:1> + <var:r> * <var:V> \end{verbatim} \end{definition} \begin{theorem} Let the following list of commands <def:poly> . Given given all whole numbers $ <var:M> $ that we have $ <var:V> = <var:M> $ before interpreting our set of commands , it holds that $ <var:r> = <nat:3> \cdot <var:M> ^ <nat:7> + <nat:4> \cdot <var:M> ^ <nat:6> + <nat:3> \cdot <var:M> ^ <nat:5> + <nat:1> \cdot <var:M> ^ <nat:3> + <nat:2> \cdot <var:M> ^ <nat:2> + <nat:4> \cdot <var:M> + <nat:1> $ when code has terminated . \end{theorem} \begin{proof} Using usual Hoare logic gives : \begin{eqnarray} \{ <var:V> = <var:M> \} \\ <var:r> := <nat:3> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> \} \\ <var:r> := <nat:4> + <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> + <nat:4> \} \\ <var:r> := <nat:3> + <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:2> + <nat:4> * <var:M> + <nat:3> \} \\ <var:r> := <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:3> + <nat:4> * <var:M> ^ <nat:2> + <nat:3> * <var:M> + \} \\ <var:r> := <nat:1> + <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:4> + <nat:4> * <var:M> ^ <nat:3> + <nat:3> * <var:M> ^ <nat:2> + <nat:1> \} \\ <var:r> := <nat:2> + <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:5> + <nat:4> * <var:M> ^ <nat:4> + <nat:3> * <var:M> ^ <nat:3> + <nat:1> * <var:M> + <nat:2> \} \\ <var:r> := <nat:4> + <var:r> * <var:V> ; \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:6> + <nat:4> * <var:M> ^ <nat:5> + <nat:3> * <var:M> ^ <nat:4> + <nat:1> * <var:M> ^ <nat:2> + <nat:2> * <var:M> + <nat:4> \} \\ <var:r> := <nat:1> + <var:r> * <var:V> \\ \{ <var:V> = <var:M> \land <var:r> = <nat:3> * <var:M> ^ <nat:7> + <nat:4> * <var:M> ^ <nat:6> + <nat:3> * <var:M> ^ <nat:5> + <nat:1> * <var:M> ^ <nat:3> + <nat:2> * <var:M> ^ <nat:2> + <nat:4> * <var:M> + <nat:1> \} \end{eqnarray} Given the above , our list of commands is shown correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Definition <def:poly> := <var:r> := <nat:3> ; <var:r> := <nat:4> + <var:r> * <var:V> ; <var:r> := <nat:3> + <var:r> * <var:V> ; <var:r> := <var:r> * <var:V> ; <var:r> := <nat:1> + <var:r> * <var:V> ; <var:r> := <nat:2> + <var:r> * <var:V> ; <var:r> := <nat:4> + <var:r> * <var:V> ; <var:r> := <nat:1> + <var:r> * <var:V> . Theorem <genH:poly_code_correct> : forall <var:M> : nat , {{ <var:V> = <var:M> }} <def:poly> {{ <var:r> = <nat:3> * <var:M> ^ <nat:7> + <nat:4> * <var:M> ^ <nat:6> + <nat:3> * <var:M> ^ <nat:5> + <nat:1> * <var:M> ^ <nat:3> + <nat:2> * <var:M> ^ <nat:2> + <nat:4> * <var:M> + <nat:1> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:2> + <nat:4> * <var:M> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:3> + <nat:4> * <var:M> ^ <nat:2> + <nat:3> * <var:M> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:4> + <nat:4> * <var:M> ^ <nat:3> + <nat:3> * <var:M> ^ <nat:2> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:5> + <nat:4> * <var:M> ^ <nat:4> + <nat:3> * <var:M> ^ <nat:3> + <nat:1> * <var:M> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:6> + <nat:4> * <var:M> ^ <nat:5> + <nat:3> * <var:M> ^ <nat:4> + <nat:1> * <var:M> ^ <nat:2> + <nat:2> * <var:M> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:V> = <var:M> /\ <var:r> = <nat:3> * <var:M> ^ <nat:7> + <nat:4> * <var:M> ^ <nat:6> + <nat:3> * <var:M> ^ <nat:5> + <nat:1> * <var:M> ^ <nat:3> + <nat:2> * <var:M> ^ <nat:2> + <nat:4> * <var:M> + <nat:1> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 