\begin{document} \begin{theorem} Define a list of commands such that \begin{verbatim} <var:e> := <nat:4> ; <var:e> := <nat:1> + <var:e> * <var:E> ; <var:e> := <nat:4> + <var:e> * <var:E> ; <var:e> := <nat:2> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:1> + <var:e> * <var:E> ; <var:e> := <var:e> * <var:E> ; <var:e> := <nat:2> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> \end{verbatim} Given , for all $ <var:r> $ , $ <var:E> = <var:r> $ before evaluating our list of commands , then we see $ <var:e> = <nat:4> \times <var:r> ^ <nat:13> + <nat:1> \times <var:r> ^ <nat:12> + <nat:4> \times <var:r> ^ <nat:11> + <nat:2> \times <var:r> ^ <nat:10> + <nat:3> \times <var:r> ^ <nat:9> + <nat:3> \times <var:r> ^ <nat:8> + <nat:3> \times <var:r> ^ <nat:7> + <nat:3> \times <var:r> ^ <nat:5> + <nat:3> \times <var:r> ^ <nat:4> + <nat:1> \times <var:r> ^ <nat:3> + <nat:2> \times <var:r> + <nat:3> $ . \end{theorem} \begin{proof} Applying typical Hoare reasoning results in : \begin{eqnarray} \{ <var:E> = <var:r> \} \\ <var:e> := <nat:4> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> \} \\ <var:e> := <nat:1> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> + <nat:1> \} \\ <var:e> := <nat:4> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + <nat:4> \} \\ <var:e> := <nat:2> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:2> \} \\ <var:e> := <nat:3> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:3> \} \\ <var:e> := <nat:3> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> \} \\ <var:e> := <nat:3> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:6> + <nat:1> * <var:r> ^ <nat:5> + <nat:4> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> \} \\ <var:e> := <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:7> + <nat:1> * <var:r> ^ <nat:6> + <nat:4> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + \} \\ <var:e> := <nat:3> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:8> + <nat:1> * <var:r> ^ <nat:7> + <nat:4> * <var:r> ^ <nat:6> + <nat:2> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> \} \\ <var:e> := <nat:3> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:9> + <nat:1> * <var:r> ^ <nat:8> + <nat:4> * <var:r> ^ <nat:7> + <nat:2> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> + <nat:3> \} \\ <var:e> := <nat:1> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:10> + <nat:1> * <var:r> ^ <nat:9> + <nat:4> * <var:r> ^ <nat:8> + <nat:2> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> \} \\ <var:e> := <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:11> + <nat:1> * <var:r> ^ <nat:10> + <nat:4> * <var:r> ^ <nat:9> + <nat:2> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + \} \\ <var:e> := <nat:2> + <var:e> * <var:E> ; \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:12> + <nat:1> * <var:r> ^ <nat:11> + <nat:4> * <var:r> ^ <nat:10> + <nat:2> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:2> \} \\ <var:e> := <nat:3> + <var:e> * <var:E> \\ \{ <var:E> = <var:r> \land <var:e> = <nat:4> * <var:r> ^ <nat:13> + <nat:1> * <var:r> ^ <nat:12> + <nat:4> * <var:r> ^ <nat:11> + <nat:2> * <var:r> ^ <nat:10> + <nat:3> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> * <var:r> + <nat:3> \} \end{eqnarray} As a result , this set of commands is proven correct . \end{proof} \end{document} 
~
Require Import String . From PLF Require Import Imp . From PLF Require Import Hoare . Theorem <genH:poly_code_correct> : forall <var:r> : nat , {{ <var:E> = <var:r> }} <var:e> := <nat:4> ; <var:e> := <nat:1> + <var:e> * <var:E> ; <var:e> := <nat:4> + <var:e> * <var:E> ; <var:e> := <nat:2> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> ; <var:e> := <nat:1> + <var:e> * <var:E> ; <var:e> := <var:e> * <var:E> ; <var:e> := <nat:2> + <var:e> * <var:E> ; <var:e> := <nat:3> + <var:e> * <var:E> {{ <var:e> = <nat:4> * <var:r> ^ <nat:13> + <nat:1> * <var:r> ^ <nat:12> + <nat:4> * <var:r> ^ <nat:11> + <nat:2> * <var:r> ^ <nat:10> + <nat:3> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> * <var:r> + <nat:3> }} . Proof . intros . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + <nat:4> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:4> * <var:r> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:4> * <var:r> ^ <nat:2> + <nat:2> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:5> + <nat:1> * <var:r> ^ <nat:4> + <nat:4> * <var:r> ^ <nat:3> + <nat:2> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:6> + <nat:1> * <var:r> ^ <nat:5> + <nat:4> * <var:r> ^ <nat:4> + <nat:2> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:7> + <nat:1> * <var:r> ^ <nat:6> + <nat:4> * <var:r> ^ <nat:5> + <nat:2> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:8> + <nat:1> * <var:r> ^ <nat:7> + <nat:4> * <var:r> ^ <nat:6> + <nat:2> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:9> + <nat:1> * <var:r> ^ <nat:8> + <nat:4> * <var:r> ^ <nat:7> + <nat:2> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> + <nat:3> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:10> + <nat:1> * <var:r> ^ <nat:9> + <nat:4> * <var:r> ^ <nat:8> + <nat:2> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:2> + <nat:3> * <var:r> + <nat:1> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:11> + <nat:1> * <var:r> ^ <nat:10> + <nat:4> * <var:r> ^ <nat:9> + <nat:2> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:3> + <nat:3> * <var:r> ^ <nat:2> + <nat:1> * <var:r> + ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:12> + <nat:1> * <var:r> ^ <nat:11> + <nat:4> * <var:r> ^ <nat:10> + <nat:2> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:6> + <nat:3> * <var:r> ^ <nat:4> + <nat:3> * <var:r> ^ <nat:3> + <nat:1> * <var:r> ^ <nat:2> + <nat:2> ) ) %assertion ) . apply hoare_seq with ( Q := ( ( <var:E> = <var:r> /\ <var:e> = <nat:4> * <var:r> ^ <nat:13> + <nat:1> * <var:r> ^ <nat:12> + <nat:4> * <var:r> ^ <nat:11> + <nat:2> * <var:r> ^ <nat:10> + <nat:3> * <var:r> ^ <nat:9> + <nat:3> * <var:r> ^ <nat:8> + <nat:3> * <var:r> ^ <nat:7> + <nat:3> * <var:r> ^ <nat:5> + <nat:3> * <var:r> ^ <nat:4> + <nat:1> * <var:r> ^ <nat:3> + <nat:2> * <var:r> + <nat:3> ) ) %assertion ) . all : eapply hoare_consequence_pre ; try ( apply hoare_asgn || assn_auto'' ) . Qed . 